---
phase: 40-config-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/config/__init__.py
  - kb_server/config/loader.py
  - kb_server/config/models.py
  - kb_server/config/router.py
  - tests/test_config_api.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
must_haves:
  truths:
    - SQLite-backed config storage with REST API
    - ConfigLoader wraps os.getenv with SQLite override chain
    - CRUD endpoints for config management
  artifacts:
    - path: "kb_server/config/loader.py"
      provides: "Config loader with env fallback"
      contains: "ConfigLoader"
      min_lines: 100
    - path: "kb_server/config/router.py"
      provides: "Config REST API"
      contains: "api/v1/config"
      min_lines: 40
    - path: "tests/test_config_api.py"
      provides: "Config test coverage"
      contains: "test_config_"
      min_lines: 100
---

<objective>
Add a SQLite-backed config storage table with a ConfigLoader class that wraps os.getenv with SQLite override chain, and expose CRUD operations through a FastAPI REST router at /api/v1/config.

Purpose: Phase 40 provides the foundation for runtime configuration management. The config table in kb_metadata.db stores key/value/type/group entries. ConfigLoader provides async get/set/delete/reset with typed conversion and env var fallback. The REST router exposes these operations via HTTP.

Output: Config models, ConfigLoader class, FastAPI router, and tests covering all CRUD operations including env fallback.
</objective>

<execution_context>
@/home/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
@kb_server/config/ (to be created)
@tests/test_config_api.py (to be created)
@ingest/core/metadata.py
</context>

<tasks>

<task type="auto">
  <name>Add config table and ConfigLoader</name>
  <files>kb_server/config/__init__.py, kb_server/config/models.py, kb_server/config/loader.py, tests/test_config_api.py</files>
  <read_first>
    docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
    ingest/core/metadata.py
  </read_first>
  <action>
    Create kb_server/config/__init__.py (empty), kb_server/config/models.py (create_config_table function with SQLite schema), and kb_server/config/loader.py (ConfigLoader class with async get/set/delete/reset/get_all, typed conversion via TYPE_MAP, and os.getenv fallback). Write tests in tests/test_config_api.py covering set/get, get_all, fallback_to_env, and delete.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - Config table creates with correct schema (key PK, value, type, group_name, description, timestamps)
    - ConfigLoader.set stores typed values; ConfigLoader.get retrieves them with correct type conversion
    - ConfigLoader.get falls back to os.getenv when key not in DB
    - ConfigLoader.delete removes keys; get returns None after delete
    - ConfigLoader.get_all returns all entries ordered by group/key
  </acceptance_criteria>
  <done>Config table and ConfigLoader implemented with all CRUD operations and env fallback</done>
</task>

<task type="auto">
  <name>Config REST API router</name>
  <files>kb_server/config/router.py, tests/test_config_api.py</files>
  <read_first>
    docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
    kb_server/config/loader.py
  </read_first>
  <action>
    Create kb_server/config/router.py with a FastAPI APIRouter (prefix /api/v1/config, tags config). Implement endpoints: GET / (list all), GET /{key} (get single), PUT /{key} (set with ConfigUpdate body), DELETE /{key} (delete), POST /reset (reset all). Endpoints pull ConfigLoader from request.app.state.config_loader. Add tests using httpx AsyncClient with ASGITransport covering all five endpoints.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_api.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/config returns list of config entries
    - PUT /api/v1/config/{key} creates/updates entry and returns it
    - GET /api/v1/config/{key} returns single entry
    - DELETE /api/v1/config/{key} removes entry
    - POST /api/v1/config/reset clears all entries
    - Missing key returns 404
  </acceptance_criteria>
  <done>Config REST API router with full CRUD and reset endpoints</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Config API | Internal clients can read/write config values through REST endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | Config API PUT/DELETE | monitor | No auth layer per project scope; internal-only deployment assumed |
| T-40-02 | Information Disclosure | Config API GET | monitor | Config values readable via API; internal-only deployment |
| T-40-03 | Spoofing | env var fallback chain | accept | SQLite override intentionally precedes env; documented behavior |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_config_api.py -x -v` (all CRUD + REST API tests pass)
</verification>

<success_criteria>
- SQLite-backed config storage operational with full CRUD
- ConfigLoader provides typed access with env fallback
- REST API serves CRUD at /api/v1/config
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/40-config-backlog/40-01-SUMMARY.md`
</output>
