---
phase: 54
plan: 02
wave: 1
depends_on: []
files_modified:
  - kb_server/ui/templates/admin/tab_profile.html
  - kb_server/ui/templates/admin/tab_analytics.html
autonomous: false
requirements: [UI-05, UI-06]
---

# Phase 54-02: Heading Hierarchy & Typography

**Goal:** Fix broken heading outline hierarchy that is screen-reader hostile across admin pages.

## Tasks

### Task 1: Fix heading hierarchy on profile config section

**File:** `kb_server/ui/templates/admin/tab_profile.html:5`

Change `<h4 class="h6">Config</h4>` to `<h4>Config</h4>` — a subsection under h2 content should be a real h4, not overridden to appear as h6. Remove the `.h6` class to let the semantic heading size match the hierarchy.

**Verification:** The "Config" heading renders at the correct semantic level without visual down-classing.

### Task 2: Fix data section headings on analytics tab

**File:** `kb_server/ui/templates/admin/tab_analytics.html:20,41,63`

Change three `<h3 class="h5">` headings to `<h3>` (remove `.h5` class):
- `Popular Queries` (line 20)
- `Content Gaps` (line 41)
- `Latency Statistics` (line 63)

These are subsections under `<h2>` content — h3 is semantically correct. The `.h5` class makes them look like sub-subsections and breaks the visual hierarchy.

**Verification:** All three section headings render at h3 semantic and visual size; no `.h5` class on h3 elements in this file.

## Verification

**typography_check:**
```bash
grep -n 'h[1-6].*class="[^"]*\.h[1-6]"' kb_server/ui/templates/admin/tab_profile.html kb_server/ui/templates/admin/tab_analytics.html
```

## must_haves

truths:
  - Profile config heading uses correct semantic h4
  - Analytics section headings use correct semantic h3
  - No heading hierarchy skips or contradictory class overrides
prohibitions:
  - statement: No regression in existing test suite
    status: enforced
    verification: pytest tests/ -x passes
