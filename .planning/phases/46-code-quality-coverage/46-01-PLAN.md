---
phase: 46-code-quality-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Q-01: datetime.utcnow() → datetime.now(timezone.utc).replace(tzinfo=None)
  - ingest/reclassify_engine.py
  - kb_server/health.py
  - kb_server/analytics/query_analyzer.py
  - kb_server/telemetry/query_logger.py
  - kb_server/ui/routes_admin.py
  - kb_server/evaluation/exporter.py
  - kb_server/auth/service.py
  - kb_server/auth/erasure.py
  - kb_server/optimization/result_store.py
  - kb_server/auth/models.py
  - tests/test_query_logger.py
  # Q-05: Unused import removal (F401)
  - ingest/cli/connectors.py
  - ingest/cli/evaluate.py
  - ingest/connectors/__init__.py
  - ingest/connectors/models.py
  - ingest/connectors/staging.py
  - ingest/docling_utils.py
  - ingest/graph_builder.py
  - kb_server/circuit_breaker.py
  - kb_server/prompts.py
  - kb_server/provider_budget.py
  - kb_server/ui/app.py
  # Q-04: Integration markers + pyproject.toml config
  - tests/test_smoke.py
  - tests/test_server_terms.py
  - pyproject.toml
autonomous: true
requirements: [Q-01, Q-04, Q-05]
user_setup: []
must_haves:
  truths:
    - "No calls to deprecated datetime.utcnow() in kb_server/, ingest/, or tests/"
    - "flak8 F401 exits with code 0 on kb_server/ and ingest/"
    - "The 5 pre-existing failing tests are tagged with @pytest.mark.integration"
    - "Running `pytest` without flags skips integration-tagged tests by default"
    - "Full test suite continues to pass after all changes"
  artifacts:
    - path: "ingest/reclassify_engine.py"
      provides: "No datetime.utcnow() calls"
      contains: "datetime.now(timezone.utc).replace(tzinfo=None)"
    - path: "pyproject.toml"
      provides: "Default integration test skip"
      contains: "addopts = \"-m 'not integration'\""
    - path: "tests/test_smoke.py"
      provides: "Integration markers on 3 tests"
      contains: "@pytest.mark.integration"
    - path: "tests/test_server_terms.py"
      provides: "Integration markers on 2 tests"
      contains: "@pytest.mark.integration"
  key_links:
    - from: "pyproject.toml"
      to: "pytest addopts"
      via: "default marker filter"
      pattern: "addopts.*not integration"
    - from: "tests/test_smoke.py"
      to: "@pytest.mark.integration"
      via: "decorator on test functions"
      pattern: "@pytest.mark.integration"
    - from: "tests/test_server_terms.py"
      to: "@pytest.mark.integration"
      via: "decorator on test functions"
      pattern: "@pytest.mark.integration"
---

<objective>
Fix the code quality baseline across the project — migrate deprecated `datetime.utcnow()` calls to the timezone-aware API, clean up all unused imports (flake8 F401) in production code, and tag pre-existing test failures as integration tests with a default skip.

**Purpose:** Eliminate Python 3.12+ deprecation warnings, reduce flake8 noise, and make the test suite green-by-default for developers who don't have Qdrant running.

**Output:** 11 source files with `datetime.utcnow()` replaced, 11 files with unused imports removed, 2 test files with integration markers, and pyproject.toml configured for default integration-test skipping.
</objective>

<execution_context>
@/home/admin/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# CONTEXT.md provides the locked decisions for this phase
@.planning/phases/46-code-quality-coverage/46-CONTEXT.md

# Canonical refs from ROADMAP.md Phase 46
# Q-01: utcnow → timezone-aware UTC
# Q-04: Integration test tagging
# Q-05: Unused import removal
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all datetime.utcnow() calls with timezone-aware UTC equivalent</name>
  <read_first>
    @ingest/reclassify_engine.py (lines 12, 250, 305, 388)
    @kb_server/health.py (lines 22, 448)
    @kb_server/analytics/query_analyzer.py (lines 67, 70, 159, 204)
    @kb_server/telemetry/query_logger.py (lines 5, 89, 117)
    @kb_server/ui/routes_admin.py (lines 273, 275)
    @kb_server/evaluation/exporter.py (lines 10, 62, 83)
    @kb_server/auth/service.py (lines 4, 150, 197)
    @kb_server/auth/erasure.py (lines 2, 35, 62, 67, 100, 106)
    @kb_server/optimization/result_store.py (lines 11, 58)
    @kb_server/auth/models.py (lines 2, 49, 54, 55, 88, 104, 132)
    @tests/test_query_logger.py (lines 4, 102, 110, 170, 177)
  </read_first>
  <files>
    ingest/reclassify_engine.py
    kb_server/health.py
    kb_server/analytics/query_analyzer.py
    kb_server/telemetry/query_logger.py
    kb_server/ui/routes_admin.py
    kb_server/evaluation/exporter.py
    kb_server/auth/service.py
    kb_server/auth/erasure.py
    kb_server/optimization/result_store.py
    kb_server/auth/models.py
    tests/test_query_logger.py
  </files>
  <action>
    Per D-01: Replace every occurrence of `datetime.utcnow()` with `datetime.now(timezone.utc).replace(tzinfo=None)` across all 11 files.

    This produces timezone-naive UTC datetimes (compatible with SQLite/SQLAlchemy) while using the non-deprecated `datetime.now(timezone.utc)` API and stripping the tzinfo to match the existing behavior of `utcnow()`.

    For each file, also add `timezone` to the `from datetime import ...` line:

    | File | Current Import | Updated Import |
    |------|---------------|----------------|
    | `ingest/reclassify_engine.py` | `from datetime import datetime, timedelta` | `from datetime import datetime, timedelta, timezone` |
    | `kb_server/health.py` | `from datetime import datetime` | `from datetime import datetime, timezone` |
    | `kb_server/analytics/query_analyzer.py` | `from datetime import datetime, timedelta` (inline, line 67) | `from datetime import datetime, timedelta, timezone` |
    | `kb_server/telemetry/query_logger.py` | `from datetime import datetime, timedelta` | `from datetime import datetime, timedelta, timezone` |
    | `kb_server/ui/routes_admin.py` | `from datetime import datetime, timedelta` (inline, line 273) | `from datetime import datetime, timedelta, timezone` |
    | `kb_server/evaluation/exporter.py` | `from datetime import datetime` | `from datetime import datetime, timezone` |
    | `kb_server/auth/service.py` | `from datetime import datetime, timedelta` | `from datetime import datetime, timedelta, timezone` |
    | `kb_server/auth/erasure.py` | `from datetime import datetime` | `from datetime import datetime, timezone` |
    | `kb_server/optimization/result_store.py` | `from datetime import datetime` | `from datetime import datetime, timezone` |
    | `kb_server/auth/models.py` | `from datetime import datetime` | `from datetime import datetime, timezone` |
    | `tests/test_query_logger.py` | `from datetime import datetime, timedelta` | `from datetime import datetime, timedelta, timezone` |

    For the lambda defaults in `kb_server/auth/models.py`, replace:
    - `default=lambda: datetime.utcnow()` → `default=lambda: datetime.now(timezone.utc).replace(tzinfo=None)`
    - `onupdate=lambda: datetime.utcnow()` → `onupdate=lambda: datetime.now(timezone.utc).replace(tzinfo=None)`

    For `query_analyzer.py` and `routes_admin.py`, the import is inline inside a function body — update the inline import statement, not a module-level import.

    Use sed or a script to make all replacements efficiently. After replacement, run `grep -rn "datetime\.utcnow\(\)" kb_server/ ingest/ tests/` to confirm zero remaining occurrences.
  </action>
  <verify>
    <automated>
      grep -rn "datetime\.utcnow\(\)" kb_server/ ingest/ tests/ | grep -v "^Binary" ; test $(grep -rn "datetime\.utcnow\(\)" kb_server/ ingest/ tests/ | grep -cv "^Binary") -eq 0
    </automated>
  </verify>
  <done>
    - Zero occurrences of `datetime.utcnow()` remain in `kb_server/`, `ingest/`, or `tests/`
    - Every replacement file compiles without syntax errors (verified via `python -c "import ast; ast.parse(open(f).read())"` for each file)
    - All existing tests continue to pass after the change
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused imports flagged by flake8 F401</name>
  <read_first>
    @ingest/cli/connectors.py (line 8, 14, 19)
    @ingest/cli/evaluate.py (line 9)
    @ingest/connectors/__init__.py (line 2)
    @ingest/connectors/models.py (line 9)
    @ingest/connectors/staging.py (line 13)
    @ingest/docling_utils.py (line 32)
    @ingest/graph_builder.py (line 15)
    @kb_server/circuit_breaker.py (line 14)
    @kb_server/prompts.py (line 13)
    @kb_server/provider_budget.py (line 14)
    @kb_server/ui/app.py (line 8)
    @kb_server/ui/routes_admin.py (line 12)
  </read_first>
  <files>
    ingest/cli/connectors.py
    ingest/cli/evaluate.py
    ingest/connectors/__init__.py
    ingest/connectors/models.py
    ingest/connectors/staging.py
    ingest/docling_utils.py
    ingest/graph_builder.py
    kb_server/circuit_breaker.py
    kb_server/prompts.py
    kb_server/provider_budget.py
    kb_server/ui/app.py
    kb_server/ui/routes_admin.py
  </files>
  <action>
    Per D-03: Remove all imports flagged by flake8 F401 across `kb_server/` and `ingest/`. Below is the exact list of 16 violatoins across 12 files:

    | File | Line(s) | Unused Import | Action |
    |------|---------|--------------|--------|
    | `ingest/cli/connectors.py` | 8 | `asyncio` | Remove line |
    | `ingest/cli/connectors.py` | 14 | `ingest.connectors.factory.create_connector` | Remove line |
    | `ingest/cli/connectors.py` | 19 | `ingest.connectors.staging.stage_documents` | Remove line |
    | `ingest/cli/evaluate.py` | 9 | `typing.Any` | Remove line |
    | `ingest/connectors/__init__.py` | 2 | `ingest.connectors.confluence` | Remove entire import line (the whole comma-separated list on this line) |
    | `ingest/connectors/__init__.py` | 2 | `ingest.connectors.jira` | (same line as above — remove entire line) |
    | `ingest/connectors/__init__.py` | 2 | `ingest.connectors.git` | (same line as above — remove entire line) |
    | `ingest/connectors/models.py` | 9 | `datetime.datetime` | Remove line |
    | `ingest/connectors/staging.py` | 13 | `uuid` | Remove line |
    | `ingest/docling_utils.py` | 32 | `AcceleratorDevice` | Remove the import (verify indentation — may be multi-line import) |
    | `ingest/graph_builder.py` | 15 | `string` | Remove line |
    | `kb_server/circuit_breaker.py` | 14 | `typing.Optional` | Remove line |
    | `kb_server/prompts.py` | 13 | `typing.Any` | Remove line |
    | `kb_server/provider_budget.py` | 14 | `typing.List` | Remove line |
    | `kb_server/ui/app.py` | 8 | `fastapi.responses.Response` | Remove this import from the `from fastapi.responses import ...` line (keep other imports on the same line) |
    | `kb_server/ui/routes_admin.py` | 12 | `fastapi.responses.JSONResponse` | Remove `JSONResponse` from the import line (keep `HTMLResponse`, `Response`) |

    For multi-import lines (__init__.py, app.py, routes_admin.py), carefully only remove the specific unused symbol while keeping others intact.

    After removing all imports, run `flake8 --select=F401 kb_server/ ingest/` to verify zero F401 violations remain. Also run the full test suite to confirm nothing breaks.
  </action>
  <verify>
    <automated>
      pip install flake8 -q 2>/dev/null && flake8 --select=F401 kb_server/ ingest/ 2>&1 | grep -q . && echo "F401 violations still present" || echo "F401 check passed"
    </automated>
  </verify>
  <done>
    - `flake8 --select=F401 kb_server/ ingest/` exits with code 0 (zero violations)
    - Full test suite passes with same number of passed/skipped as baseline
  </done>
</task>

<task type="auto">
  <name>Task 3: Tag 5 pre-existing failing tests as integration and configure pytest default skip</name>
  <read_first>
    @tests/test_smoke.py (lines 183-259)
    @tests/test_server_terms.py (lines 1-31)
    @pyproject.toml (lines 1-36)
  </read_first>
  <files>
    tests/test_smoke.py
    tests/test_server_terms.py
    pyproject.toml
  </files>
  <action>
    Per D-02: Tag the 5 pre-existing failing tests that require Qdrant with `@pytest.mark.integration` and configure pytest to skip integration-tagged tests by default.

    **Step 1: Add markers to test_smoke.py (3 tests)**

    Add `@pytest.mark.integration` decorator above each of these test functions:
    - `test_search_kb_returns_text_content_on_results` (line 183)
    - `test_search_kb_returns_no_results_message_when_store_empty` (line 209)
    - `test_search_kb_passes_filters_to_store` (line 228)

    The file already imports pytest at line 9, so no additional import is needed.

    **Step 2: Add markers to test_server_terms.py (2 tests)**

    Add `@pytest.mark.integration` decorator above each:
    - `test_list_tools_search_kb_has_module_param` (line 9)
    - `test_list_tools_list_docs_has_module_param` (line 22)

    This file already imports pytest at line 5.

    **Step 3: Configure pyproject.toml to skip integration by default**

    Add to the `[tool.pytest.ini_options]` section:
    ```toml
    addopts = "-m 'not integration'"
    ```

    This makes `pytest` (without any flags) skip all integration-tagged tests. Developers can still run them explicitly with `pytest -m integration`.

    **Step 4: Verify**

    Run `pytest tests/test_smoke.py tests/test_server_terms.py -v --no-header 2>&1 | head -20` to confirm the markers exist and tests are skipped by default (will show "skipped" or "deselected" depending on `--co`). Then run `pytest -m integration tests/test_smoke.py tests/test_server_terms.py -v --no-header 2>&1 | head -20` to confirm the tests are selectable with the explicit marker. Run the full test suite to ensure no new failures.
  </action>
  <verify>
    <automated>
      # Verify integration tag exists and default skip works
      pytest tests/test_smoke.py tests/test_server_terms.py --collect-only -q 2>&1 | grep -c "integration" && echo "integration markers present"
      # Verify full suite still green
      python -m pytest --co -q 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - `@pytest.mark.integration` decorator present on all 5 tests
    - `pytest` alone skips these 5 tests (deselected)
    - `pytest -m integration` selects these 5 tests
    - Full test suite passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are introduced in this phase — Q-01, Q-04, and Q-05 are all internal code quality improvements with no runtime impact on data flow or authorization.

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | `datetime.utcnow()` → `datetime.now(timezone.utc).replace(tzinfo=None)` in 11 files | mitigate | Verify behavioral equivalence: `.replace(tzinfo=None)` produces identical timezone-naive UTC as `utcnow()`. All timestamps stored in SQLite/SQLAlchemy remain naive UTC. Run full test suite after replacement. |
| T-46-02 | Tampering | Unused import removal across 12 files | mitigate | Each removal is visually verified: the removed symbol is not referenced anywhere else in the file (grep or flake8 F401 already guarantees this). Run full test suite after removal. |
| T-46-03 | Tampering | Integration marker + addopts config | mitigate | Both forward and inverse selection verified: `pytest` (default) skips tagged tests, `pytest -m integration` runs them. |
</threat_model>

<verification>
1. `grep -rn "datetime\.utcnow\(\)" kb_server/ ingest/ tests/ | grep -cv "^Binary"` → 0
2. `flake8 --select=F401 kb_server/ ingest/` → exit code 0
3. `pytest --co -q 2>&1` shows 5 deselected tests (the 5 integration-tagged ones)
4. `pytest -m integration tests/test_smoke.py tests/test_server_terms.py --co -q 2>&1` selects exactly 5 tests
5. Full suite: `python -m pytest -x --timeout 60 2>&1` passes (same baseline pass/fail/skip counts)
</verification>

<success_criteria>
- [ ] Zero `datetime.utcnow()` calls remain in kb_server/, ingest/, or tests/
- [ ] flake8 F401 exits with code 0 on kb_server/ and ingest/
- [ ] 5 tests tagged with `@pytest.mark.integration` in test_smoke.py and test_server_terms.py
- [ ] `pytest` (no flags) skips integration-tagged tests by default (5 deselected)
- [ ] `pytest -m integration` runs only the 5 integration-tagged tests
- [ ] Full test suite passes without regressions
</success_criteria>

<output>
Create `.planning/phases/46-code-quality-coverage/46-01-SUMMARY.md` when done.
</output>
