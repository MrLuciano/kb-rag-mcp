---
phase: 05-sse-stability-python-3-13-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sse_handler.py
  - requirements.in
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - SSE-01
  - SSE-02
must_haves:
  truths:
    - "The SSE handler returns an HTTP response (not None) after client disconnect"
    - "POST /messages/ returns 202 Accepted with no redirect chain"
    - "The starlette minimum version is pinned and documented so teams know compatible ranges"
  artifacts:
    - path: "tests/test_sse_handler.py"
      provides: "Unit and integration regression tests for SSE handler fix"
      min_lines: 80
      contains: "TestClient"
    - path: "requirements.in"
      provides: "Pinned minimum starlette version"
      contains: "starlette>=1.0.0"
    - path: ".planning/codebase/STACK.md"
      provides: "Documented starlette minimum version"
      contains: ">=1.0.0"
  key_links:
    - from: "tests/test_sse_handler.py"
      to: "kb_server/server.py"
      via: "import of SseServerTransport patching the"
      pattern: "patch.*connect_sse|TestClient.*/sse"
    - from: "requirements.in"
      to: "requirements.txt"
      via: "pip-compile generates lockfile from pinned minimum"
      pattern: "starlette"
---

<objective>
Add regression tests for the already-applied SSE handler fix and pin starlette minimum version.

Purpose: The NoneType crash (fixed in Phase 5 code drop) lacked regression tests — this plan adds both unit-level and integration-level coverage to prevent recurrence. Also pins starlette >=1.0.0 so teams know the baseline.

Output: `tests/test_sse_handler.py`, updated `requirements.in`, updated `.planning/codebase/STACK.md`
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-sse-stability-python-3-13-compatibility/05-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md

<interfaces>
From `kb_server/server.py`:
- `from mcp.server.sse import SseServerTransport` — line 22
- `TRANSPORT = os.getenv("MCP_TRANSPORT", "stdio")` — line 41
- SSE block (lines 649–686): creates `SseServerTransport("/messages/")`, defines nested `handle_sse(request)`, creates `Starlette(routes=[Route("/sse", endpoint=handle_sse), Mount("/messages/", app=sse.handle_post_message)])`, runs via uvicorn

From `requirements.in` (current starlette line):
- `starlette>=0.37.0` — line 31 (must be updated to `>=1.0.0`)

From `.planning/codebase/STACK.md`:
- `starlette==1.0.0` — line 28 (current pinned version in summary; document minimum)

From `tests/test_health.py` (TestClient pattern reference):
```python
from fastapi.testclient import TestClient
from kb_server.health_server import app
client = TestClient(app)
response = client.get("/health")
assert response.status_code == 200
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit test — handle_sse returns Response() on disconnect</name>
  <files>tests/test_sse_handler.py</files>
  <behavior>
    - Create a minimal Starlette app with Route("/sse") invoking the same handler logic as server.py
    - Mock `SseServerTransport.connect_sse` via `unittest.mock.patch` to yield `([io.BytesIO()], [io.BytesIO()])` immediately without real async context
    - Use Starlette `TestClient` to GET /sse
    - Verify response.status_code is 200 (not a NoneType crash)
  </behavior>
  <action>

Create `tests/test_sse_handler.py` with a unit test (requires `@pytest.mark.asyncio` per strict mode) that:

1. Builds a standalone Starlette app that replicates the SSE handler pattern from `server.py` lines 657–669:
   - Creates `SseServerTransport("/messages/")`
   - Defines `async def handle_sse(request)` with the `async with sse.connect_sse(...)` block followed by `return Response()`
   - Creates `Starlette(routes=[Route("/sse", endpoint=handle_sse), Mount("/messages/", app=sse.handle_post_message)])`

2. Mocks `SseServerTransport.connect_sse` using `unittest.mock.patch` so the `async with` block yields immediately without an actual SSE connection. The mock must be on `mcp.server.sse.SseServerTransport.connect_sse` (the real import target in `server.py` — per D-02: mock at the lowest possible level per v1.0 retro lesson).

3. Uses `from starlette.testclient import TestClient` (not `fastapi.testclient` — this is a pure Starlette app, not FastAPI):
   - `client = TestClient(app)`
   - `response = client.get("/sse")`
   - Assert `response.status_code == 200`

**Key details:**
- Use `@pytest.mark.asyncio` decorator on the test function
- Do NOT call `bootstrap_env()` — the mock does not need env vars
- Import inside the test function (not module-level) to avoid collection-time import side effects, following the pattern from `test_health.py`
- Use context-manager `with patch(...)` for the mock, not decorator, so the test body stays readable
- The mock `connect_sse` must be an async context manager — use `AsyncMock` with `__aenter__` returning `([MagicMock()], [MagicMock()])`
</action>
  <verify>
    <automated>python -m pytest tests/test_sse_handler.py::test_handle_sse_returns_response -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Unit test passes: GET /sse with mocked connection returns 200, no crash, no exception.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — SSE connect/disconnect produces no crash + POST /messages/ returns 202</name>
  <files>tests/test_sse_handler.py</files>
  <behavior>
    - Integration test using TestClient with the starlette app
    - GET /sse → client disconnects → verify no exception propagated
    - POST /messages/ → verify 202 Accepted (no 307 redirect) per SSE-02
  </behavior>
  <action>

Add to the same `tests/test_sse_handler.py`:

**Test A — Client disconnect does not crash (per D-03):**
- Use `TestClient` against the same SSE app
- Open GET /sse with `stream=True` so the connection is held open
- Read a small chunk (the SSE handshake)
- Close the client/response
- Verify no `TypeError: NoneType` or any unhandled exception was raised
- In a try/except, after close assert the response is complete without exception

Since `TestClient` raises exceptions in the client thread, wrap the connection and disconnect in a try/except that catches everything: `pytest.fail(f"SSE disconnect raised: {e}")` if anything propagates.

**Test B — POST /messages/ returns 202 (per SSE-02):**
- POST to `/messages/` (with trailing slash — the `Mount` path in `server.py` is `/messages/` which is consistent with `SseServerTransport("/messages/")`)
- Assert `response.status_code == 202`
- Assert no redirect (status is not 307)

**Implementation notes:**
- The POST test does NOT need a real SSE session — `handle_post_message` will handle the request. The response may indicate "no session" which is expected — what matters is no redirect loop.
- Mark the disconnect test as `@pytest.mark.asyncio`
- The POST test does not need asyncio (TestClient handles sync requests to async apps)
- For the disconnect test, set `TestClient(app, raise_server_exceptions=False)` to capture any server-side crash without aborting the test
</action>
  <verify>
    <automated>python -m pytest tests/test_sse_handler.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Both tests pass: SSE disconnect produces no crash; POST /messages/ returns 202 with no redirect.</done>
</task>

<task type="auto">
  <name>Task 3: Pin starlette minimum version and document in STACK.md</name>
  <files>requirements.in, .planning/codebase/STACK.md</files>
  <action>

Two small changes per D-07 and D-08:

**A) Update `requirements.in` line 31:**
Change `starlette>=0.37.0` to `starlette>=1.0.0`

Reason: The fix depends on starlette 1.0.0 behavior (Response() after SSE handler, trailing-slash handling). Older versions may not have the needed fixes.

**B) Update `.planning/codebase/STACK.md` line 28:**
Change `- \`starlette==1.0.0\` — SSE transport layer for MCP (\`server/server.py\`)` to:

```
- \`starlette>=1.0.0\` — SSE transport layer for MCP (\`kb_server/server.py\`); pinned minimum
```

This documents the minimum version constraint for contributors and operators (per D-08).

No need to recompile `requirements.txt` yet — that is done by the main deploy pipeline. Just update the source of truth (`requirements.in`) and the docs.
</action>
  <verify>
    <automated>grep -q 'starlette>=1.0.0' requirements.in && echo "requirements.in OK" && grep -q 'starlette>=' .planning/codebase/STACK.md && echo "STACK.md OK"</automated>
  </verify>
  <done>requirements.in has `starlette>=1.0.0`; STACK.md documents the minimum version.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → SSE handler | Untrusted HTTP request enters the SSE endpoint |
| HTTP client → POST /messages/ | Untrusted POST data enters the message handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | SSE `handle_sse` | mitigate | Handler always returns `Response()` after `async with` completes or raises; no bare `except:` — crash terminates the async context manager cleanly per starlette 1.0.0 |
| T-05-02 | Denial of Service | SSE endpoint | accept | Internal-only tool on trusted network; no auth layer planned; rate-limiting out of scope |
| T-05-SC | Tampering | `starlette` pip dependency | mitigate | Version pinned `>=1.0.0` in `requirements.in`; compiled `requirements.txt` locks exact version |
</threat_model>

<verification>

1. `python -m pytest tests/test_sse_handler.py -x -v` — all 3 tests pass
2. `grep -q 'starlette>=1.0.0' requirements.in` — pin applied
3. `grep -q 'starlette>=' .planning/codebase/STACK.md` — doc updated
</verification>

<success_criteria>

- [ ] Unit test verifies `handle_sse` returns Response() (200) when connect_sse yields
- [ ] Integration test verifies SSE disconnect produces no crash
- [ ] Integration test verifies POST /messages/ returns 202 with no redirect
- [ ] `requirements.in` has `starlette>=1.0.0`
- [ ] `STACK.md` documents the minimum starlette version
</success_criteria>

<output>
Create `.planning/phases/05-sse-stability-python-3-13-compatibility/05-01-SUMMARY.md` when done
</output>
