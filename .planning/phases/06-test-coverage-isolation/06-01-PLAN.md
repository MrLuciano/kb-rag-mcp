---
phase: 06-test-coverage-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - pyproject.toml
autonomous: true
requirements: [TEST-02, TEST-03]
user_setup: []
must_haves:
  truths:
    - "pytest -m 'not integration' runs all unit tests without Qdrant, LM Studio, or Redis running"
    - "import-level stubs prevent ModuleNotFoundError for optional deps (httpx, redis, qdrant_client, fastembed)"
    - "Custom pytest markers (integration, fase12, cli) are registered and produce no warnings"
    - "Tests that require live services are consistently tagged @pytest.mark.integration"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Session-scoped mock fixtures for Qdrant, embed client, and Redis"
      min_lines: 60
    - path: "pyproject.toml"
      provides: "Registered markers and standardized test config"
      contains: "markers"
  key_links:
    - from: "tests/conftest.py"
      to: "all test files importing kb_server.* modules"
      via: "autouse fixtures / mock patching"
      pattern: "import|from.*kb_server"
    - from: "pyproject.toml"
      to: "pytest marker resolution"
      via: "markers config"
      pattern: "integration"
---

<objective>
Create the shared mock infrastructure so that all unit tests can run without
Qdrant, LM Studio, or Redis. Every test file that imports `kb_server.*` or
`ingest.*` modules must either (a) receive mocked clients via conftest
fixtures, or (b) stub heavy dependencies at module level so collection
succeeds without live infrastructure.

Purpose: Satisfy TEST-02 (unit tests require no external services) and
  TEST-03 (integration tests clearly marked). Without this plan,
  ~13 test files fail collection when Qdrant/LM Studio/Redis are absent.
Output:
  - Updated `tests/conftest.py` with auto-use session fixtures
  - Updated `pyproject.toml` with registered markers + testpaths
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@tests/conftest.py
@tests/e2e/conftest.py
@pyproject.toml

# Existing mocking patterns for reference
@tests/test_vector_store_unit.py
@tests/test_smoke.py
@tests/test_collection_manager.py

<interfaces>
From tests/conftest.py (current, 11 lines):
```python
@pytest.fixture(scope='session', autouse=True)
def load_dotenv_once():
    """Loads .env from project root before any tests execute."""
```

From pyproject.toml (current pytest section):
```ini
[tool.pytest.ini_options]
asyncio_mode = "strict"
pythonpath = ["."]
```

Current unknown marks producing PytestUnknownMarkWarning:
- `fase12` — used in test_hybrid_search.py, test_hybrid_search_minimal.py, test_reranker.py, test_payload_indexes.py
- `integration` — used in test_payload_indexes.py (2 tests)
- `cli` — used in test_payload_indexes.py
  
Current pattern for module-level stubs (test_smoke.py, test_vector_store_unit.py):
```python
def _ensure_stubs():
    for mod_name in ["qdrant_client", "qdrant_client.async_qdrant_client", ...]:
        sys.modules.setdefault(mod_name, types.ModuleType(mod_name))
```
</interfaces>

Current test count: 547 tests collected in venv, 13 collection errors without venv (import failures).
Current tests tagged @pytest.mark.integration: 2.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register custom pytest markers in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
    Append a `markers` section to `[tool.pytest.ini_options]` that registers:
    - `integration`: Tests that require live infrastructure (Qdrant, LM Studio, Redis). Skipped via `pytest -m "not integration"`.
    - `fase12`: Tests targeting FASE 12 features (hybrid search, reranking).
    - `cli`: CLI integration tests that exercise click commands end-to-end.

    Also add `testpaths = ["tests"]` and `filterwarnings = ["ignore::pytest.PytestUnknownMarkWarning"]` to
    `[tool.pytest.ini_options]`.
    
    Do NOT remove any existing config lines. Add the new keys at the end of the section.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` `[tool.pytest.ini_options]` section contains `markers` key listing `integration`, `fase12`, `cli`
    - `pyproject.toml` contains `testpaths = ["tests"]`
    - `pyproject.toml` contains `filterwarnings` suppressing PytestUnknownMarkWarning
    - `python -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'markers' in d['tool']['pytest']['ini_options']"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); m=d['tool']['pytest']['ini_options']['markers']; assert 'integration' in str(m) and 'fase12' in str(m) and 'cli' in str(m)"</automated>
  </verify>
  <done>
    pyproject.toml has registered integration, fase12, and cli markers; no PytestUnknownMarkWarning appears in test collection
  </done>
</task>

<task type="auto">
  <name>Task 2: Add session-scoped mock fixtures to tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py</read_first>
  <action>
    Extend `tests/conftest.py` with the following auto-use session fixtures that
    prevent real connections to external infrastructure:

    1. **`mock_qdrant_client`** (scope="session", autouse=True) — patches
       `qdrant_client.AsyncQdrantClient` so that ALL test files that import
       `kb_server.vector_store` or `kb_server.collections.*` get a mock client
       that does not attempt to connect to localhost:6333.
       
       Use `unittest.mock.patch` with a `AsyncMock` that returns canned responses:
       - `get_collections.return_value` → MagicMock with `.collections = []`
       - `search.return_value` → `[]`
       - `scroll.return_value` → `([], None)`
       - `count.return_value` → MagicMock(count=0)
       - `collection_exists.return_value` → `True`
       
       Register the patch at the Qdrant module boundary:
       `patch("qdrant_client.AsyncQdrantClient")`
       
       **Do NOT** modify or wrap the existing `load_dotenv_once` fixture — add new
       fixtures after it. Each fixture must have a distinct scope to avoid
       `fixture "mock_qdrant_client" already exists` errors.

    2. **`mock_embed_client`** (scope="session", autouse=True) — patches
       `kb_server.embed_client.get_embedding` and `get_embeddings_batch` to
       return fixed 384-dim float vectors without calling any backend.
       
       Use `patch("kb_server.embed_client.get_embedding", return_value=[0.1]*384)` and
       `patch("kb_server.embed_client.get_embeddings_batch", return_value=[[0.1]*384])`.
       
       Use `patch.object(kb_server.embed_client, "BACKEND", "test")` 
       and `patch.object(kb_server.embed_client, "MODEL", "test-model")`.

    3. **`mock_redis_cache`** (scope="session", autouse=True) — patches
       `kb_server.cache.redis.RedisCache` to return a MagicMock that does
       not connect to Redis. Use:
       `patch("kb_server.cache.redis.RedisCache", autospec=True)`

    All patches should use `scope="session"` and `autouse=True` so every test
    automatically gets isolation.
    
    Use `try/except ImportError` guards around any imports that may not be
    available in the test environment (e.g., `kb_server.embed_client` may not
    be installed via setup.py develop).
    
    Keep `load_dotenv_once` as-is — it's a separate fixture.
    
    Add a module-level comment explaining the purpose: "Session-scoped mock
    fixtures that prevent tests from connecting to live Qdrant, LM Studio,
    or Redis. Added in Phase 6 to satisfy TEST-02."
  </action>
  <acceptance_criteria>
    - `tests/conftest.py` contains at least 60 lines
    - `tests/conftest.py` defines `mock_qdrant_client`, `mock_embed_client`, `mock_redis_cache` fixtures
    - All three fixtures are `scope="session"` and `autouse=True`
    - Existing `load_dotenv_once` fixture is preserved unchanged
    - No syntax errors: `python3 -c "import ast; ast.parse(open('tests/conftest.py').read())"` succeeds
    - ImportError-guarded imports prevent ModuleNotFoundError when optional deps missing
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('tests/conftest.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    tests/conftest.py has 3 new session-scoped auto-use fixtures; all unit tests can collect without live services
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → external infra | Test code must not reach Qdrant/LM Studio/Redis ports during unit test runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | tests/conftest.py mock fixtures | mitigate | Session-scoped autouse patches ensure ALL test sessions are isolated by default |
| T-06-02 | Information Disclosure | pyproject.toml markers | accept | Marker names are public; no sensitive info in pytest config |
| T-06-SC | Tampering | pip installs | mitigate | No new packages introduced — only stdlib unittest.mock |
</threat_model>

<verification>
1. Run `python3 -m pytest tests/ --co -q 2>&1 | grep -c "ERROR"` — must be 0
2. Run `python3 -m pytest tests/ -m "not integration" --co -q 2>&1 | tail -3` — must show collected tests, no errors
3. Run `python3 -m pytest tests/ --co -q 2>&1 | grep -c "UnknownMarkWarning"` — must be 0
</verification>

<success_criteria>
- pyproject.toml has registered markers (integration, fase12, cli)
- tests/conftest.py has 3 session-scoped auto-use fixtures mocking Qdrant, embed, and Redis
- `pytest -m "not integration" --co` shows no ERROR and no UnknownMarkWarning
- All existing 547 tests still collect successfully in venv
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage-isolation/06-01-SUMMARY.md` when done
</output>
