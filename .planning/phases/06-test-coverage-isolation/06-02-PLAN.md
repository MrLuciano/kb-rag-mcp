---
phase: 06-test-coverage-isolation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/test_classifier.py
  - tests/test_collection_manager.py
  - tests/test_collection_router.py
  - tests/test_cache_redis.py
  - tests/test_embed_client_unit.py
  - tests/test_search_integration.py
  - tests/test_payload_indexes.py
  - tests/test_hybrid_search.py
  - tests/test_reranker.py
  - tests/test_hybrid_search_minimal.py
autonomous: true
requirements: [TEST-01, TEST-03]
user_setup: []
must_haves:
  truths:
    - "Every Python module in kb_server/ has a corresponding unit test file"
    - "Tests requiring live Qdrant/LM Studio/Redis are tagged @pytest.mark.integration"
    - "ingest/classifier.py has a dedicated unit test file"
    - "pytest -m 'not integration' covers all kb_server unit tests"
  artifacts:
    - path: "tests/test_classifier.py"
      provides: "Unit tests for ingest/classifier.py"
      min_lines: 80
    - path: "tests/test_collection_router.py"
      provides: "Integration tag for collection_router tests that need Qdrant"
      contains: "@pytest.mark.integration"
    - path: "tests/test_search_integration.py"
      provides: "Integration tag for search tests that need live services"
      contains: "@pytest.mark.integration"
  key_links:
    - from: "tests/test_classifier.py"
      to: "ingest/classifier.py"
      via: "import"
      pattern: "from ingest.classifier import"
    - from: "@pytest.mark.integration tags"
      to: "pytest -m 'not integration' filter"
      via: "marker registration from Plan 6-01"
      pattern: "mark.integration"
---

<objective>
Write the missing `test_classifier.py` for `ingest/classifier.py` and audit
all existing kb_server/* tests to ensure tests that require live Qdrant, LM
Studio, or Redis are consistently tagged `@pytest.mark.integration`.

Purpose: Satisfy TEST-01 (every module has a test file) for the one truly
  uncovered module, and begin satisfying TEST-03 (clear integration test
  tagging) by auditing kb_server-related test files.
Output:
  - New `tests/test_classifier.py` with ≥80 lines of unit tests
  - Updated test files with `@pytest.mark.integration` tags where needed
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@ingest/classifier.py
@tests/test_hybrid_search.py
@tests/test_reranker.py
@tests/test_payload_indexes.py
@tests/test_search_integration.py
@tests/test_collection_routing_integration.py
@tests/test_health.py
@tests/test_server_extra.py
@tests/test_server_tools.py
@tests/test_collection_manager.py
@tests/test_cache_redis.py
@tests/test_embed_client_unit.py
@tests/test_vector_store.py

<interfaces>
From ingest/classifier.py (key exports):
```python
# DOC_TYPE_RULES: list[tuple[int, str, list[str]]]
# Priority-ordered rules: (priority, doc_type, [regex_patterns])

# Key functions inferred from code:
# - classify_from_filename(filename: str) -> dict with keys: product, doc_type, version
# - PRODUCT_PATTERNS: dict[str, list[str]] mapping product names to regex patterns
```

From tests/test_classifier references: No existing test file — this is a new file.

Current mock patterns (for writing isolated tests):
- `tests/test_collection_manager.py` — uses `AsyncMock`, `MagicMock` directly
- `tests/test_vector_store_unit.py` — uses module-level `_ensure_stubs()` + `@patch`
- `tests/conftest.py` — after Plan 6-01, has session-scoped mocks for Qdrant, embed, Redis

Current integration-tagged tests (only 2 exist, both in test_payload_indexes.py):
- `test_payload_indexes.py:154` — `@pytest.mark.integration`
- `test_payload_indexes.py` — (second one at line ~244)

Moduels that likely need @pytest.mark.integration:
- test_search_integration.py — name implies integration, uses mocked store but may need real Qdrant for full coverage
- test_collection_routing_integration.py — imports server.py which requires real Qdrant
- test_health.py — exercises health_server which may query live services
- test_server_extra.py — exercises server with real imports
- test_server_tools.py — exercises MCP tool dispatch which may need Qdrant
- test_collection_manager.py — uses AsyncMock, fine as unit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_classifier.py for ingest/classifier.py</name>
  <files>tests/test_classifier.py, ingest/classifier.py</files>
  <read_first>ingest/classifier.py</read_first>
  <behavior>
    - Test 1: classify_from_filename identifies "WebReport" from filename containing "WebReport" or "Content Server WebReport"
    - Test 2: classify_from_filename identifies "xECM" from filename containing "xECM" or "ECM"
    - Test 3: classify_from_filename identifies "Workflow" from filename containing "Workflow" or "WF"
    - Test 4: classify_from_filename returns unknown product for unrecognized filename
    - Test 5: classify_from_filename identifies doc_type "admin_guide" from "Admin Guide" in filename
    - Test 6: classify_from_filename identifies doc_type "standard" from "ISO" in filename
    - Test 7: classify_from_filename identifies doc_type "training" from "Training" in filename
    - Test 8: classify_from_filename identifies version from patterns like "23.4" or "v2.1" in filename
    - Test 9: classify_from_filename returns product=None, doc_type=None, version=None for empty input
    - Test 10: classify_from_filename extracts product from parent directory path
  </behavior>
  <action>
    Create `tests/test_classifier.py` with unit tests for `ingest/classifier.py`.

    Test file requirements:
    - Module docstring: `"""Unit tests for ingest/classifier.py — auto-tagging heuristics."""`
    - Import `ingest.classifier` functions after path setup (project root in pythonpath per pyproject.toml)
    - **Do NOT modify ingest/classifier.py** — it already works correctly. Only write tests.
    
    Cover these classification dimensions:
    1. **Product detection** — filename patterns for OTCS products:
       - `"3-0117 Content Server WebReport Design"` → product="WebReports"
       - `"OTCS-xECM-Admin-Guide"` → product="xECM"
       - `"Workflow-Designer-Manual"` → product="Workflow"
       - `"CSIDE-Developer-Guide"` → product="CSIDE"
       - `"Content-Server-Admin"` → product="Content Server"
       - `"unrecognized_file_name"` → product is None (classified correctly)
    
    2. **Doc type detection** — filename patterns:
       - `"Admin Guide"` or `"admin_guide"` → doc_type="admin_guide"
       - `"ISO 9001 Standard"` → doc_type="standard"
       - `"Training Manual"` → doc_type="training"
       - `"Release Notes"` → doc_type="release_notes"
       - No recognized pattern → doc_type is None
    
    3. **Version extraction** — version patterns:
       - `"v23.4"` → version="23.4"
       - `"version_2.1"` → version="2.1"
       - No version pattern → version is None
    
    4. **Edge cases**:
       - Empty string → all None
       - None input → graceful handling (no TypeError)
       - Path with product subdirectory → product inferred from directory
    
    Each test must be a standalone function (not a class), using pytest assertions.
    Use `@pytest.mark.asyncio` only if testing async functions (classifier.py is sync).
    No external dependencies — classifier.py uses only `re` and `pathlib`.
    
    Expected minimum: 10 test functions covering the behaviors above.
  </action>
  <acceptance_criteria>
    - `tests/test_classifier.py` exists with ≥80 lines
    - Contains ≥10 test functions
    - All tests import from `ingest.classifier` and use pytest assertions
    - `python3 -m pytest tests/test_classifier.py -v 2>&1` shows ≥10 PASSED, 0 FAILED, 0 ERROR
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_classifier.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    tests/test_classifier.py has ≥10 passing tests covering product, doc_type, version classification
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit kb_server test files and add @pytest.mark.integration tags</name>
  <files>
    tests/test_search_integration.py,
    tests/test_collection_routing_integration.py,
    tests/test_server_extra.py,
    tests/test_server_tools.py,
    tests/test_vector_store.py,
    tests/test_health.py,
    tests/test_hybrid_search.py,
    tests/test_reranker.py,
    tests/test_hybrid_search_minimal.py
  </files>
  <read_first>
    tests/test_search_integration.py,
    tests/test_collection_routing_integration.py,
    tests/test_server_extra.py,
    tests/test_server_tools.py,
    tests/test_vector_store.py,
    tests/test_health.py
  </read_first>
  <action>
    For each test file listed in `<files>`, determine whether any test requires
    live Qdrant, LM Studio, or Redis to run. Annotate as follows:

    **If the file already works with mocks** (imports are stubbed, no live
    connection attempt): No change needed. Just verify it runs under
    `-m "not integration"`.

    **If the file or any test in it requires live services:**
    - Add `import pytest` at the top (if not present)
    - Add a module-level `pytestmark = pytest.mark.integration` if ALL tests
      in the file require live services. This tags every function automatically.
    - If only specific tests need live services, add `@pytest.mark.integration`
      to those individual functions.
    - **Do NOT** use `__all__` or rename existing functions.
    - **Do NOT** modify test logic — only add markers.

    Audit each file:
    1. `test_search_integration.py` — currently mocks VectorStore (no live Qdrant).
       Check if `_search_kb` or any test reaches real Qdrant. If mock-only, skip.
       If it reaches real Qdrant, tag the file.
    2. `test_collection_routing_integration.py` — name suggests integration.
       Check if it needs Qdrant via server.py imports. Tag if so.
    3. `test_server_extra.py` — imports server.py. Check if server.py attempts
       Qdrant connection at import time. Tag if needed.
    4. `test_server_tools.py` — Same assessment.
    5. `test_vector_store.py` — imports VectorStore which needs Qdrant.
       Might already use mocks. Tag tests that still reach real Qdrant.
    6. `test_health.py` — imports health_server. Check if it needs live infra.
    7. `test_hybrid_search.py` — may or may not need cross-encoder model.
    8. `test_reranker.py` — may need sentence_transformers model.
    9. `test_hybrid_search_minimal.py` — check.

    **Important rule:** Do NOT add `@pytest.mark.integration` to tests that
    already work with mocks. The marker means "this test CANNOT run without
    live infrastructure." If a test uses `unittest.mock.patch` or
    `_ensure_stubs()` to isolate dependencies, it should NOT be tagged
    `integration`.

    **For files in `tests/` that clearly require live infrastructure at
    module-import time** (cannot be stubbed during collection): add
    `pytestmark = pytest.mark.integration` at module level.
  </action>
  <acceptance_criteria>
    - Every test in the listed files that requires live Qdrant/LM Studio/Redis is tagged `@pytest.mark.integration`
    - No test that already works with mocks is incorrectly tagged
    - `python3 -m pytest tests/list_of_tagged_files -m "not integration" --co 2>&1 | grep -c ERROR` is 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/ -m "not integration" --co -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    All kb_server test files have correct integration tags; unit tests collect without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source module | Test must not modify production code behavior via imports |
| test → external network | No live service calls during unit test execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | tests/test_classifier.py | mitigate | Tests use pytest assertions only; no side effects on production module |
| T-06-02-02 | Information Disclosure | integration markers | accept | Markers are public test metadata; no sensitive data exposed |
| T-06-02-SC | Tampering | pip installs | mitigate | No new packages added — only stdlib pytest + unittest.mock |
</threat_model>

<verification>
1. `python3 -m pytest tests/test_classifier.py -v` — all tests pass
2. `python3 -m pytest tests/ -m "not integration" --co -q` — no ERROR, 545+ tests collected
3. Grep for `@pytest.mark.integration` in all modified files — correct per audit
</verification>

<success_criteria>
- tests/test_classifier.py exists with ≥10 passing tests
- All kb_server tests that need live services are tagged @pytest.mark.integration
- No unit test falsely tagged as integration
- `pytest -m "not integration"` collects ≥545 tests with 0 errors
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage-isolation/06-02-SUMMARY.md` when done
</output>
