---
phase: 14-health-dashboard
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - docker-compose.yml
  - deployment/config/grafana-provisioning/datasources/prometheus.yml
  - deployment/config/grafana-provisioning/dashboards/kb-rag.yml
  - tests/e2e/test_docker_compose.py
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - "docker-compose up starts 4 services: Qdrant, kb-rag-mcp, Prometheus, Grafana"
    - "Prometheus scrapes kb-rag-mcp at /metrics every 15s"
    - "Grafana auto-provisions Prometheus datasource and KB-RAG dashboard"
    - "Dashboard is accessible at http://localhost:3000"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Prometheus + Grafana service definitions"
      contains: "prometheus:"
    - path: "deployment/config/grafana-provisioning/datasources/prometheus.yml"
      provides: "Grafana datasource provisioning"
      min_lines: 10
    - path: "deployment/config/grafana-provisioning/dashboards/kb-rag.yml"
      provides: "Grafana dashboard provisioning"
      min_lines: 8
    - path: "tests/e2e/test_docker_compose.py"
      provides: "Docker Compose validation test"
      min_lines: 40
  key_links:
    - from: "prometheus"
      to: "kb-rag-mcp:8000/metrics"
      via: "scrape_configs"
      pattern: "targets:.*kb-rag-mcp:8000"
    - from: "grafana"
      to: "prometheus:9090"
      via: "datasource provisioning"
      pattern: "url:.*prometheus:9090"
---

<objective>
Integrate Prometheus and Grafana into Docker Compose deployment for local/dev monitoring.

**Purpose:** Enable developers to run `docker-compose up` and instantly get a fully-provisioned monitoring stack.

**Output:**
- 2 new services in `docker-compose.yml` (prometheus, grafana)
- Grafana provisioning configs for datasource and dashboard
- E2E test validating Docker Compose service health
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14/CONTEXT.md
@.planning/phases/14/14-01-SUMMARY.md
@docker-compose.yml
@deployment/config/prometheus.yml
@deployment/config/grafana-dashboard.json
</context>

<interfaces>
<!-- Current docker-compose.yml structure -->

From docker-compose.yml (lines 1-41):
```yaml
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: kb-qdrant
    ports: ["6333:6333", "6334:6334"]
    volumes: ["./data/qdrant:/qdrant/storage"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]

  kb-rag-mcp:
    build: .
    container_name: kb-rag-mcp
    depends_on:
      qdrant: {condition: service_healthy}
    ports: ["8000:8000", "8080:8080"]
    env_file: [".env"]
    environment: ["QDRANT_URL=http://qdrant:6333"]
    volumes: ["./data:/app/data", "./logs:/app/logs"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
```

Prometheus scrape config from deployment/config/prometheus.yml (lines 24-49):
```yaml
scrape_configs:
  - job_name: 'kb-rag'
    static_configs:
      - targets: ['localhost:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
```

Grafana provisioning structure (from deployment/config/grafana-provisioning/):
- `datasources/*.yml` — define Prometheus datasource
- `dashboards/*.yml` — define dashboard provider (points to JSON files)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Prometheus service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <behavior>
    - Test 1: docker-compose config validates successfully (no YAML errors)
    - Test 2: Prometheus service definition exists with correct image
    - Test 3: Prometheus service exposes port 9090
    - Test 4: Prometheus config volume mount points to deployment/config/prometheus.yml
    - Test 5: Prometheus service depends on kb-rag-mcp service
  </behavior>
  <action>
Add Prometheus service to `docker-compose.yml` after kb-rag-mcp service:

```yaml
  prometheus:
    image: prom/prometheus:latest
    container_name: kb-prometheus
    restart: unless-stopped
    depends_on:
      kb-rag-mcp:
        condition: service_healthy
    ports:
      - "9090:9090"
    volumes:
      - ./deployment/config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
```

Also add named volume at the bottom of docker-compose.yml:

```yaml
volumes:
  prometheus-data:
```

**Update prometheus.yml scrape target:** Change `targets: ['localhost:8000']` to `targets: ['kb-rag-mcp:8000']` so Prometheus uses Docker internal networking.
  </action>
  <verify>
    <automated>docker-compose config 2>&1 | grep -q "prometheus:" && echo "Prometheus service defined"</automated>
  </verify>
  <done>
docker-compose config shows prometheus service; YAML is valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Grafana service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Add Grafana service to `docker-compose.yml` after prometheus service:

```yaml
  grafana:
    image: grafana/grafana:latest
    container_name: kb-grafana
    restart: unless-stopped
    depends_on:
      prometheus:
        condition: service_healthy
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./deployment/config/grafana-provisioning:/etc/grafana/provisioning:ro
      - ./deployment/config:/etc/grafana/dashboards:ro
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
      - GF_USERS_ALLOW_SIGN_UP=false
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3
```

Add to volumes section:

```yaml
  grafana-data:
```

**Why this config:**
- Anonymous access enabled for easy local viewing (auth optional later)
- Provisioning directory mounted for auto-config
- Dashboard JSON mounted at `/etc/grafana/dashboards` (referenced by provisioning config)
- Default credentials: admin/admin (standard Grafana defaults)
  </action>
  <verify>
    <automated>docker-compose config 2>&1 | grep -q "grafana:" && echo "Grafana service defined"</automated>
  </verify>
  <done>
docker-compose config shows grafana service; 4 services total (qdrant, kb-rag-mcp, prometheus, grafana)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Grafana datasource provisioning config</name>
  <files>deployment/config/grafana-provisioning/datasources/prometheus.yml</files>
  <action>
Create Grafana Prometheus datasource provisioning file:

1. Ensure directory exists: `mkdir -p deployment/config/grafana-provisioning/datasources`

2. Create `deployment/config/grafana-provisioning/datasources/prometheus.yml`:

```yaml
# Grafana datasource provisioning for Prometheus
# Auto-provisions Prometheus datasource on Grafana startup

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
      httpMethod: "POST"
```

**Why this config:**
- `url: http://prometheus:9090` uses Docker internal networking
- `isDefault: true` makes it the default datasource for all dashboards
- `editable: false` prevents accidental modification in Grafana UI
- `httpMethod: POST` is recommended for large queries
  </action>
  <verify>
    <automated>test -f deployment/config/grafana-provisioning/datasources/prometheus.yml && echo "Datasource config created"</automated>
  </verify>
  <done>
Datasource provisioning file exists; YAML is valid
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Grafana dashboard provisioning config</name>
  <files>deployment/config/grafana-provisioning/dashboards/kb-rag.yml</files>
  <action>
Create Grafana dashboard provisioning file:

1. Ensure directory exists: `mkdir -p deployment/config/grafana-provisioning/dashboards`

2. Create `deployment/config/grafana-provisioning/dashboards/kb-rag.yml`:

```yaml
# Grafana dashboard provisioning for KB-RAG
# Auto-loads dashboards from /etc/grafana/dashboards directory

apiVersion: 1

providers:
  - name: 'KB-RAG Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
```

**Why this config:**
- `path: /etc/grafana/dashboards` matches the volume mount in docker-compose.yml
- `allowUiUpdates: true` allows editing in Grafana UI (changes not persisted to JSON unless manually exported)
- `updateIntervalSeconds: 30` checks for dashboard file changes every 30s
  </action>
  <verify>
    <automated>test -f deployment/config/grafana-provisioning/dashboards/kb-rag.yml && echo "Dashboard provisioning config created"</automated>
  </verify>
  <done>
Dashboard provisioning file exists; YAML is valid
  </done>
</task>

<task type="auto">
  <name>Task 5: Create Docker Compose E2E test</name>
  <files>tests/e2e/test_docker_compose.py</files>
  <action>
Create E2E test for Docker Compose service health:

1. Create `tests/e2e/test_docker_compose.py`:

```python
"""
E2E test for Docker Compose deployment.

Validates that docker-compose.yml is valid and services are correctly defined.
"""
import subprocess
import pytest
import yaml
from pathlib import Path

COMPOSE_FILE = Path("docker-compose.yml")

def test_docker_compose_yaml_is_valid():
    """docker-compose.yml is valid YAML."""
    with open(COMPOSE_FILE) as f:
        config = yaml.safe_load(f)
    assert isinstance(config, dict)
    assert "services" in config

def test_required_services_defined():
    """All 4 required services are defined."""
    with open(COMPOSE_FILE) as f:
        config = yaml.safe_load(f)
    services = config["services"]
    required = ["qdrant", "kb-rag-mcp", "prometheus", "grafana"]
    for service in required:
        assert service in services, f"Missing service: {service}"

def test_prometheus_scrape_target():
    """Prometheus is configured to scrape kb-rag-mcp."""
    prom_config = Path("deployment/config/prometheus.yml")
    with open(prom_config) as f:
        config = yaml.safe_load(f)
    targets = config["scrape_configs"][0]["static_configs"][0]["targets"]
    assert "kb-rag-mcp:8000" in targets, f"Prometheus not scraping kb-rag-mcp, found: {targets}"

def test_grafana_datasource_provisioning():
    """Grafana datasource provisioning config exists."""
    ds_config = Path("deployment/config/grafana-provisioning/datasources/prometheus.yml")
    assert ds_config.exists(), "Datasource provisioning config missing"
    with open(ds_config) as f:
        config = yaml.safe_load(f)
    assert config["datasources"][0]["url"] == "http://prometheus:9090"

def test_grafana_dashboard_provisioning():
    """Grafana dashboard provisioning config exists."""
    db_config = Path("deployment/config/grafana-provisioning/dashboards/kb-rag.yml")
    assert db_config.exists(), "Dashboard provisioning config missing"
    with open(db_config) as f:
        config = yaml.safe_load(f)
    assert config["providers"][0]["options"]["path"] == "/etc/grafana/dashboards"

@pytest.mark.skipif(
    subprocess.run(["which", "docker-compose"], capture_output=True).returncode != 0,
    reason="docker-compose not installed"
)
def test_docker_compose_config_validates():
    """docker-compose config validates without errors."""
    result = subprocess.run(
        ["docker-compose", "config"],
        cwd=Path.cwd(),
        capture_output=True,
        text=True
    )
    assert result.returncode == 0, f"docker-compose config failed: {result.stderr}"
```

2. Ensure PyYAML is in requirements.txt (likely already present for other configs)
  </action>
  <verify>
    <automated>pytest tests/e2e/test_docker_compose.py -v</automated>
  </verify>
  <done>
6 tests pass; Docker Compose configuration validated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker network→services | Internal Docker networking; trusted within compose stack |
| Host→Grafana UI | Grafana UI exposed on localhost:3000; anonymous access enabled for local dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Elevation of Privilege | Grafana anonymous access | accept | Local dev only; production deployments should disable anonymous access via environment override |
| T-14-06 | Information Disclosure | Prometheus/Grafana on host network | accept | Ports bound to localhost by default; internal deployment only per project constraints |
| T-14-07 | Denial of Service | Unbounded Prometheus data retention | mitigate | Set `--storage.tsdb.retention.time=15d` flag to limit disk usage |
| T-14-SC | Tampering | Prometheus/Grafana images | accept | Using official images from Docker Hub (prom/prometheus, grafana/grafana); no version pins for latest features |
</threat_model>

<verification>
## Automated Verification

```bash
# Validate YAML syntax
docker-compose config

# Run E2E tests
pytest tests/e2e/test_docker_compose.py -v
```

## Manual Verification

```bash
# Start full stack
docker-compose up -d

# Verify services are healthy
docker-compose ps

# Check Prometheus targets
curl http://localhost:9090/api/v1/targets | jq '.data.activeTargets[0].health'
# Expected: "up"

# Check Grafana health
curl http://localhost:3000/api/health
# Expected: {"database":"ok","version":"..."}

# Open Grafana dashboard
open http://localhost:3000/d/kb-rag/kb-rag-mcp-monitoring
```
</verification>

<success_criteria>
- [ ] docker-compose.yml has 4 services (qdrant, kb-rag-mcp, prometheus, grafana)
- [ ] Prometheus service scrapes kb-rag-mcp:8000/metrics
- [ ] Grafana service auto-provisions Prometheus datasource
- [ ] Grafana service auto-loads KB-RAG dashboard from JSON
- [ ] All services have health checks defined
- [ ] E2E tests validate Docker Compose configuration (6 tests pass)
- [ ] `docker-compose up` brings up all 4 services without errors
- [ ] Grafana UI is accessible at http://localhost:3000
</success_criteria>

<output>
Create `.planning/phases/14/14-03-SUMMARY.md` when done
</output>
