---
phase: 41-provider-alias
plan: 01
type: execute
wave: 2
depends_on:
  - 40-config-backlog
files_modified:
  - kb_server/config/loader.py
  - kb_server/config/hotreload.py
  - kb_server/embed_client.py
  - tests/test_config_api.py
autonomous: true
requirements:
  - PROV-01
must_haves:
  truths:
    - Provider aliases resolve through config table
    - Config changes detected via mtime polling
  artifacts:
    - path: "kb_server/config/loader.py"
      provides: "Provider alias resolution"
      contains: "resolve_provider"
      min_lines: 5
    - path: "kb_server/config/hotreload.py"
      provides: "Config change watcher"
      contains: "ConfigWatcher"
      min_lines: 40
---

<objective>
Add provider alias resolution to ConfigLoader so embed_client.py can resolve friendly names (e.g., "aliyun" → "dashscope") through config entries, and add a ConfigWatcher that detects SQLite database mtime changes for hot-reload.

Purpose: Phase 41 extends the config system with provider alias lookup (config entries with group provider_alias) and a lightweight mtime-based change detection mechanism for config hot-reload.

Output: resolve_provider method on ConfigLoader, _resolve_backend integration in embed_client.py, ConfigWatcher class, and tests for both features.
</objective>

<execution_context>
@/home/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
@kb_server/config/loader.py
@kb_server/config/hotreload.py (to be created)
@kb_server/embed_client.py
@tests/test_config_api.py
</context>

<tasks>

<task type="auto">
  <name>Provider Alias integration</name>
  <files>kb_server/config/loader.py, kb_server/embed_client.py, tests/test_config_api.py</files>
  <read_first>
    docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
    kb_server/config/loader.py
    kb_server/embed_client.py
  </read_first>
  <action>
    Add async resolve_provider method to ConfigLoader in loader.py: reads config key "provider_alias.{name}", returns alias if found, otherwise returns the original name. Add a synchronous _resolve_backend function in embed_client.py that queries the config table directly (avoiding async at module level) and replaces all os.getenv("EMBED_BACKEND", ...) references. Write tests for alias resolution and passthrough for unaliased names.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_api.py::test_config_resolve_provider_alias tests/test_config_api.py::test_config_resolve_provider_no_alias -x -v</automated>
  </verify>
  <acceptance_criteria>
    - resolve_provider returns alias when config entry provider_alias.{name} exists
    - resolve_provider returns original name when no alias configured
    - embed_client.py resolves EMBED_BACKEND through alias table at startup
  </acceptance_criteria>
  <done>Provider alias resolution integrated into ConfigLoader and embed_client</done>
</task>

<task type="auto">
  <name>Hot-reload mechanism</name>
  <files>kb_server/config/hotreload.py, tests/test_config_api.py</files>
  <read_first>
    docs/superpowers/plans/2026-06-13-phase40-41-config-provider-alias.md
    kb_server/config/hotreload.py (to be created)
  </read_first>
  <action>
    Create kb_server/config/hotreload.py with ConfigWatcher class. Constructor accepts optional db_path (defaults to METADATA_DB env var). check_for_changes method compares current file mtime against stored _last_mtime. get_changed_keys method takes a previous_snapshot dict and returns list of keys whose values differ. Write tests for change detection before/after writes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_api.py::test_hotreload_detect_change -x -v</automated>
  </verify>
  <acceptance_criteria>
    - ConfigWatcher.check_for_changes returns False when no writes occurred
    - ConfigWatcher.check_for_changes returns True after a config entry is written
    - get_changed_keys identifies only keys that actually changed
  </acceptance_criteria>
  <done>ConfigWatcher detects SQLite mtime changes and identifies changed keys</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config entries → Provider resolution | provider_alias entries control which embedding backend is used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | provider_alias config entries | monitor | Unvalidated alias values could redirect to wrong backend; internal-only deployment |
| T-41-02 | Denial of Service | rapid config writes → mtime polling | accept | mtime-based check is lightweight; no rate limit on watcher |
| T-41-03 | Elevation of Privilege | alias→backend redirection | accept | Project has no auth layer; deployment assumed trusted internal network |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_config_api.py -x -v` (all config + alias + hot-reload tests pass)
</verification>

<success_criteria>
- Provider aliases resolve through config table entries
- embed_client.py uses resolved backend name at startup
- ConfigWatcher detects SQLite mtime changes
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/41-provider-alias/41-01-SUMMARY.md`
</output>
