---
phase: 14-health-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/health_server.py
  - tests/test_health_server.py
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "GET /metrics returns Prometheus-formatted metrics"
    - "Metrics endpoint exposes all 28 metrics from observability/metrics.py"
    - "Prometheus can scrape /metrics without errors"
  artifacts:
    - path: "kb_server/health_server.py"
      provides: "/metrics endpoint implementation"
      exports: ["@app.get('/metrics')"]
    - path: "tests/test_health_server.py"
      provides: "Unit tests for /metrics endpoint"
      min_lines: 50
  key_links:
    - from: "kb_server/health_server.py"
      to: "observability/metrics.py"
      via: "prometheus_client.generate_latest()"
      pattern: "from prometheus_client import.*generate_latest"
---

<objective>
Add a `/metrics` endpoint to the health server that exposes all Prometheus metrics for scraping.

**Purpose:** Enable Prometheus to scrape kb-rag-mcp metrics for monitoring and alerting.

**Output:** 
- `/metrics` endpoint at http://localhost:8000/metrics returning Prometheus text format
- Unit tests verifying metrics exposure
- All 28 metrics from `observability/metrics.py` accessible via HTTP
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14/CONTEXT.md
@kb_server/health_server.py
@observability/metrics.py
</context>

<interfaces>
<!-- Key exports from observability/metrics.py that the executor needs -->

From observability/metrics.py (lines 9-15):
```python
from prometheus_client import (
    CONTENT_TYPE_LATEST,
    Counter,
    Gauge,
    Histogram,
    generate_latest,
)
```

MetricsCollector class (lines 20-59) provides access to all 28 metrics:
- Job metrics: jobs_created, jobs_completed, jobs_active, job_duration
- File metrics: files_processed, files_processing_time, chunks_generated
- Worker metrics: worker_pool_size, worker_pool_queue_size, worker_pool_utilization
- Rate limiter: rate_limiter_tokens, rate_limiter_waits, rate_limiter_wait_time
- API metrics: api_requests, api_latency
- Cache metrics: cache_hits, cache_misses, cache_evictions, cache_size_bytes, cache_entries
- Batch metrics: batch_embeddings_total, batch_embedding_texts, batch_embedding_duration, etc.

From kb_server/health_server.py (lines 47-51):
```python
app = FastAPI(
    title="KB-RAG Health Check",
    description="Health check endpoints for KB-RAG services",
    version="1.0.0",
)
```

Existing endpoints: /health, /health/detailed, /ready, /alive (lines 54-109)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /metrics endpoint with tests</name>
  <files>kb_server/health_server.py, tests/test_health_server.py</files>
  <behavior>
    - Test 1: GET /metrics returns 200 status code
    - Test 2: Response Content-Type is "text/plain; version=0.0.4; charset=utf-8" (Prometheus format)
    - Test 3: Response body contains "kb_ingest_jobs_created_total" metric
    - Test 4: Response body contains "kb_rag_cache_hits_total" metric
    - Test 5: Response body contains "# HELP" and "# TYPE" comment lines
    - Test 6: Endpoint returns valid Prometheus text format (parseable by prometheus_client)
  </behavior>
  <action>
Add `/metrics` endpoint to `kb_server/health_server.py`:

1. Import prometheus_client exports (after line 22):
   - `from prometheus_client import generate_latest, CONTENT_TYPE_LATEST`

2. Create `/metrics` endpoint (after line 109, before `if __name__`):
   - `@app.get("/metrics")` decorator
   - Function: `async def metrics_endpoint()`
   - Docstring: "Prometheus metrics endpoint. Returns all metrics in Prometheus text format. Used by: Prometheus scraper"
   - Body: `return Response(generate_latest(), media_type=CONTENT_TYPE_LATEST)`
   - Import Response from fastapi if not already imported

3. Create `tests/test_health_server.py` with pytest-asyncio tests:
   - Use `from fastapi.testclient import TestClient`
   - Import app: `from kb_server.health_server import app`
   - Test fixture: `client = TestClient(app)`
   - 6 test functions covering behavior cases above
   - Use `response = client.get("/metrics")` pattern (TestClient is sync, not async)
   - Verify Content-Type header matches Prometheus format
   - Verify response body contains sample metric names from different categories
   - Parse response with `from prometheus_client.parser import text_string_to_metric_families` to validate format

**Why this approach:**
- `generate_latest()` automatically collects all registered Prometheus metrics (the 28 metrics in observability/metrics.py are module-level globals, registered at import time)
- No need to manually instantiate MetricsCollector or pass metrics — prometheus_client maintains a process-global registry
- `CONTENT_TYPE_LATEST` ensures correct Prometheus text format Content-Type header
- TestClient provides sync interface to FastAPI app — no async context needed in tests
  </action>
  <verify>
    <automated>pytest tests/test_health_server.py -v</automated>
  </verify>
  <done>
All 6 tests pass; `curl localhost:8000/metrics` (manual test) returns Prometheus format with all metric families
  </done>
</task>

<task type="auto">
  <name>Task 2: Update health server logging</name>
  <files>kb_server/health_server.py</files>
  <action>
Add startup log message indicating /metrics endpoint is available:

In the `if __name__ == "__main__":` block (line 113), after existing log.info call:
- Add: `log.info("Metrics endpoint available at /metrics (Prometheus scrape target)")`

This helps operators confirm metrics are enabled during startup.
  </action>
  <verify>
    <automated>python kb_server/health_server.py 2>&1 | grep "Metrics endpoint available"</automated>
  </verify>
  <done>
Startup logs show metrics endpoint availability message; no errors on server start
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP→health_server | /metrics endpoint exposes operational metrics (no sensitive data, but reveals system activity patterns) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | /metrics endpoint | accept | Internal deployment only per project constraints; metrics reveal no PII or secrets, only operational telemetry |
| T-14-02 | Denial of Service | /metrics scraping | accept | Prometheus default scrape interval (15s) is low-frequency; no expensive computation in generate_latest() |
| T-14-SC | Tampering | prometheus_client (pip) | mitigate | Already in requirements.txt (0.25.0); no new dependencies added |
</threat_model>

<verification>
## Manual Verification

1. Start health server: `python kb_server/health_server.py`
2. Curl metrics endpoint: `curl -v http://localhost:8000/metrics`
3. Verify response contains:
   - `Content-Type: text/plain; version=0.0.4; charset=utf-8`
   - Multiple metric families (jobs, files, cache, etc.)
   - `# HELP` and `# TYPE` comment lines

## Automated Verification

```bash
pytest tests/test_health_server.py -v
```

Expected: 6 tests pass, 0 failures
</verification>

<success_criteria>
- [ ] `/metrics` endpoint returns HTTP 200
- [ ] Response Content-Type is Prometheus text format
- [ ] Response body contains all 28 metric names from observability/metrics.py
- [ ] Unit tests cover endpoint behavior (6 test cases)
- [ ] No regressions in existing health endpoints (/health, /ready, /alive)
- [ ] Startup logs confirm metrics endpoint availability
</success_criteria>

<output>
Create `.planning/phases/14/14-01-SUMMARY.md` when done
</output>
