---
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - kb_server/evaluation/ragas_pipeline.py
  - kb_server/evaluation/__init__.py
requirements:
  - EVAL-01
autonomous: true
---

# Plan 24-01: RAGAS Core Evaluator

## Objective
Implement the core RAGAS evaluation pipeline with 4 metrics (faithfulness, answer_relevancy, context_precision, context_recall) using the existing embedding backend (LM Studio/Ollama).

## Tasks

### Task 1: Add RAGAS dependency
<read_first>
- requirements.txt
- setup.py
</read_first>
<action>
Add `ragas==0.2.14` (latest stable) to requirements.txt. Verify it pins transitive dependencies (langchain, datasets, numpy, pandas) without conflicts. Run `pip install ragas==0.2.14` in `.venv/` to verify install succeeds.
</action>
<acceptance_criteria>
- `pip show ragas` returns version 0.2.x
- `python -c "from ragas.metrics import faithfulness, answer_relevancy, context_precision, context_recall; print('OK')"` exits 0
- No version conflicts with existing dependencies (httpx, pydantic, datasets)
</acceptance_criteria>

### Task 2: Implement RAGASEvaluator.evaluate()
<read_first>
- kb_server/evaluation/ragas_pipeline.py
- kb_server/evaluation/dataset.py
- kb_server/embed_client.py
- kb_server/vector_store.py
- kb_server/server.py (search_kb handler)
</read_first>
<action>
Replace NotImplementedError in RAGASEvaluator.evaluate() with actual RAGAS integration:
1. Import ragas.metrics (faithfulness, answer_relevancy, context_precision, context_recall)
2. For each example in dataset:
   a. Query VectorStore.search() or kb_server.server._search_kb() to get contexts
   b. Generate answer using retrieved contexts (or use expected_answer as ground_truth)
   c. Build ragas EvaluationDataset with question, answer, contexts, ground_truth
3. Run each metric via ragas.evaluate()
4. Return dict of metric_name -> mean_score

Reuse embed_client.py for LLM backend: pass the same httpx client/config to RAGAS's Langchain LLM wrapper.
</action>
<acceptance_criteria>
- `RAGASEvaluator.evaluate()` returns dict with keys: faithfulness, answer_relevancy, context_precision, context_recall
- Each value is a float between 0.0 and 1.0
- Method does not raise NotImplementedError
- Uses existing EMBED_BACKEND env var configuration (no hardcoded LLM URLs)
</acceptance_criteria>

### Task 3: Add unit tests for evaluator
<read_first>
- tests/test_server_extra.py (mocking patterns)
- kb_server/evaluation/ragas_pipeline.py
</read_first>
<action>
Create tests/test_ragas_evaluator.py with:
1. Test evaluate() returns correct dict structure
2. Test evaluate() with empty dataset returns empty dict
3. Test metric values are bounded [0, 1]
4. Mock VectorStore and embed_client to avoid needing live Qdrant/LLM
</action>
<acceptance_criteria>
- `pytest tests/test_ragas_evaluator.py -v` passes (3+ tests)
- Tests run without requiring live Qdrant or LLM backend (mocked)
- Coverage for RAGASEvaluator.__init__ and .evaluate()
</acceptance_criteria>

## Verification Criteria

- [ ] `python -c "from kb_server.evaluation.ragas_pipeline import RAGASEvaluator; e = RAGASEvaluator.__new__(RAGASEvaluator); print(type(e.evaluate))"` prints `<class 'method'>` (no NotImplementedError)
- [ ] `pytest tests/test_ragas_evaluator.py` passes
- [ ] RAGAS dependency resolves without conflicts

## must_haves
- EVAL-01: 4 core metrics (faithfulness, answer_relevancy, context_precision, context_recall)
