---
phase: 12-english-comments-docstrings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/classifier.py
  - ingest/ingest.py
autonomous: true
requirements: []
---

<objective>
Translate all Portuguese inline comments, log messages, and section headers in `ingest/` to English.

Purpose: Phase 12 goal — ensure all inline comments, docstrings, log messages, and internal documentation across Python source files are written in English for consistency and open-source readiness.

Output: Two ingest files with all Portuguese content translated to English.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
From ingest/classifier.py:
```python
# Section header pattern: # ── <Portuguese label> ──
# Inline comments: # <Portuguese text> preceed rule groups
# Inline comments within functions: # <Portuguese text> before code block
# Log: log.info(f"..."), log.warning(f"..."), log.error(f"...")
```

From ingest/ingest.py:
```python
# Critical section header: # ── CRÍTICO: ... ──
# Comment pattern: # <Portuguese text>
# Log messages: log.info(f"..."), log.warning(f"..."), log.error(f"...")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate ingest/classifier.py inline comments and section headers</name>
  <files>ingest/classifier.py</files>
  <read_first>
    Read ingest/classifier.py fully before editing — Portuguese inline comments span the entire file from section headers to within-function comments.
  </read_first>
  <action>
    Find and translate every Portuguese comment and section header in ingest/classifier.py to English. Do NOT change any code, function signatures, docstrings that are already English, variable names, string patterns used in regex matching, or data values (product names, doc_type names, alias entries).

    **Section header comments** — These are the `# ──` section separators at lines 21, 247, 283, 313, 343, 357, 361, 582:
    - Line 21: `# ── Taxonomia de doc_type ──────────────` → `# ── Doc type taxonomy ──`
    - Line 247: `# ── Mapeamento de pasta raiz → produto ──` → `# ── Root folder → product mapping ──`
    - Line 248: `# Complementa a detecção automática por pasta com aliases` → `# Supplements automatic folder detection with aliases`
    - Line 283: `# Padrões de produto inferíveis do próprio nome do arquivo` → `# Product patterns inferable from the filename itself`
    - Line 313: `# ── Vendor Mapping (Phase 11) ──` → already English
    - Line 343: `# ── Subsystem Patterns (Phase 11) ──` → already English
    - Line 357: `# Skip-list directories for subsystem inference` → already English
    - Line 361-363: `# ── FUNÇÕES PÚBLICAS ──` → `# ── PUBLIC FUNCTIONS ──`

    **DOC_TYPE_RULES group comments** (lines 23-24, 27, 55, 66, 83, 97, 114, 127, 145, 165, 186, 206, 221, 233):
    Each `# <Portuguese label>` comment before a rule tuple. Translate all to English:
    - `# Cada entrada: (prioridade, doc_type, [padrões regex no nome do arquivo])` → `# Each entry: (priority, doc_type, [regex patterns on filename])`
    - `# Maior prioridade = verificado primeiro.` → `# Highest priority = checked first.`
    - `# Padrões ISO / normas / regulamentos` → `# ISO standards / norms / regulations`
    - `# Guias de upgrade / migração` → `# Upgrade / migration guides`
    - `# Guias de instalação` → `# Installation guides`
    - `# Guias de administração` → `# Administration guides`
    - `# Guias de configuração / cenários` → `# Configuration guides / scenarios`
    - `# Guias de usuário` → `# User guides`
    - `# Guias de API / SDK / programação` → `# API / SDK / programming guides`
    - `# Treinamentos / apresentações educacionais` → `# Training / educational presentations`
    - `# Apresentações / visão geral` → `# Presentations / overviews`
    - `# Documentos de referência técnica / terminologia` → `# Technical reference documents / terminology`
    - `# Notas de reunião / sessões gravadas` → `# Meeting notes / recorded sessions`
    - `# Artefatos binários / pacotes` → `# Binary artifacts / packages`

    **Function-level inline comments** (lines 379, 381, 384, 395, 427, 551, 561, 568, 570):
    - Line 379: `# Usa nome + caminho completo como texto de busca, tudo em minúsculas` → `# Use name + full path as search text, all lowercase`
    - Line 381: `# Substitui separadores por espaço para facilitar matching` → `# Replace separators with spaces for easier matching`
    - Line 384: `# Extensão de arquivo sem conteúdo textual → artifact` → `# File extension without textual content → artifact`
    - Line 395: `# Avalia regras por ordem de prioridade (maior primeiro)` → `# Evaluate rules by priority order (highest first)`
    - Line 427: `# Normalize filename + full path text` → already English
    - Line 551: `# Pasta raiz` → `# Root folder`
    - Line 561: `# Lookup direto no alias map` → `# Direct lookup in alias map`
    - Line 568: `# Usa o nome da pasta com capitalização se não estiver no alias` → `# Use the folder name with capitalization if not in alias`
    - Line 570: `# preserva capitalização original` → `# preserve original capitalization`

    **PRODUCT_ALIASES entry comment** (line 260-261): The `"varios": "geral"` and `"templates": "geral"` entries and their comment — the data values `"varios"`, `"geral"`, `"templates"` are alias data and should NOT be changed. Only the comment above these entries if any.

    **DOCUMENT METADATA EXTRACTION section** (lines 582-584): Already English.

    Verify every comment in the file is English by the end. Do NOT change: any regex patterns, priority numbers, doc_type strings like "admin_guide", product/alias dict values, function names, class names, or `VENDOR_MAP` values.
  </action>
  <acceptance_criteria>
    - `grep -c 'Taxonomia\|Cada entrada\|Maior prioridade\|Padrões ISO\|Guias de upgrade\|Guias de instalação\|Guias de administração\|Guias de configuração\|Guias de usuário\|Guias de API\|Treinamentos\|Apresentações\|Documentos de referência\|Notas de reunião\|Artefatos binários\|Mapeamento de pasta\|Complementa a\|Padrões de produto\|FUNÇÕES PÚBLICAS\|Usa nome\|Substitui separadores\|Extensão de arquivo\|Avalia regras\|Pasta raiz\|Lookup direto\|capitalização' ingest/classifier.py` equals 0
    - `ingest/classifier.py` is valid Python AST
    - No regex patterns, priority numbers, doc_type strings, alias dict values, or function names were modified
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('ingest/classifier.py').read())" && echo "AST_OK"</automated>
  </verify>
  <done>All Portuguese comments and section headers in classifier.py translated to English; file remains valid Python AST</done>
</task>

<task type="auto">
  <name>Task 2: Translate ingest/ingest.py comments and log messages</name>
  <files>ingest/ingest.py</files>
  <read_first>
    Read ingest/ingest.py fully before editing.
  </read_first>
  <action>
    Find and translate every Portuguese comment and log message in ingest/ingest.py to English:

    1. **Critical section header** (line 25):
       - `# ── CRÍTICO: carrega o .env ANTES de qualquer import que leia variáveis ──────` → `# ── CRITICAL: load .env BEFORE any import that reads variables ──`

    2. **Other inline comments** (lines 31, 42, 334):
       - Line 31: `# Agora adiciona o server/ ao path para imports de embed_client e vector_store` → `# Now add server/ to path for embed_client and vector_store imports`
       - Line 42: `# ── Extensões suportadas` → `# ── Supported extensions`
       - Line 334: `# Fallback manual se langchain não estiver instalado` → `# Manual fallback if langchain is not installed`

    3. **Log messages** (lines 138, 145, 288, 424, 430, 434):
       - Line 138: `log.info(f"  PyMuPDF OK: {path.name} ({len(chunks_raw)} páginas)")` → `log.info(f"  PyMuPDF OK: {path.name} ({len(chunks_raw)} pages)")`
       - Line 145: `log.error(f"  Erro ao extrair PDF: {e}")` → `log.error(f"  Error extracting PDF: {e}")`
       - Line 288: `log.error(f"  Erro ao ler código: {e}")` → `log.error(f"  Error reading code: {e}")`
       - Line 424: `log.info(f"[{doc_type:16}] [{product:20}] {source_file}  (forçado)")` → `log.info(f"[{doc_type:16}] [{product:20}] {source_file}  (forced)")`
       - Line 430: `log.warning(f"  Sem conteúdo extraído: {source_file}")` → `log.warning(f"  No content extracted: {source_file}")`
       - Line 434: `"sem conteúdo extraído"` → `"no content extracted"`

    Do NOT change: any Python identifiers, f-string variable interpolation, import statements, `sys.path.insert`, the `bootstrap_env()` call order/pattern (the critical .env loading order must be preserved), env variable names, or any code logic.
  </action>
  <acceptance_criteria>
    - `grep -c 'CRÍTICO\|carrega o\|Agora adiciona\|Extensões suportadas\|Fallback manual\|páginas\|Erro ao extrair\|Erro ao ler\|forçado\|Sem conteúdo\|sem conteúdo' ingest/ingest.py` equals 0
    - `ingest/ingest.py` is valid Python AST
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('ingest/ingest.py').read())" && echo "AST_OK"</automated>
  </verify>
  <done>All Portuguese comments and log messages in ingest.py translated to English; file remains valid Python AST</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new code, features, or trust boundaries — pure comment and string translation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | ingest/*.py | mitigate | slopcheck: re-run classifier tests after task 1, full suite after plan completion |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase |
</threat_model>

<verification>
```bash
source .venv/bin/activate && python -m pytest tests/test_classifier.py -x --timeout=60 2>&1 | tail -10
source .venv/bin/activate && python -m pytest -x --timeout=60 -k "test_classifier or test_ingest" 2>&1 | tail -10
```
</verification>

<success_criteria>
- `grep -c '[áéíóúãõçàâêîôûü]' ingest/classifier.py ingest/ingest.py` reports 0 matches for Portuguese-specific characters in both files
- Both files pass `ast.parse()` validation
- Classifier tests (`tests/test_classifier.py`) pass with zero failures
- No code logic, regex patterns, function signatures, or data values (product names, alias entries, doc_type strings) were modified
</success_criteria>

<output>
Create `.planning/phases/12-english-comments-docstrings/12-02-SUMMARY.md` when done
</output>
