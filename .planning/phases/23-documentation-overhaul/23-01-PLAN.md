---
phase: 23-documentation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/OPERATIONS.md
  - docs/TROUBLESHOOTING.md
  - docs/INSTRUCTIONS.md
  - docs/INDEX.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - User can navigate to deployment-mode-specific content via INDEX.md
    - Each of OPERATIONS.md, TROUBLESHOOTING.md, INSTRUCTIONS.md has Common + per-mode H2 sections
    - See-also footers cross-reference related mode sections across files
  artifacts:
    - docs/OPERATIONS.md has ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual sections
    - docs/TROUBLESHOOTING.md has ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual sections
    - docs/INSTRUCTIONS.md has ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual sections
    - docs/INDEX.md has a "Deployment Modes" section
  key_links:
    - INDEX.md links to all 3 reorganized files
    - Each mode section in each file links to sibling mode sections in other files
---

<objective>
Reorganize operational docs by deployment mode

Purpose: Enable users to find all guidance for their chosen deployment mode (Docker Compose, Helm, Systemd, Manual) in one place, without restructuring monolithic files.

Output: Updated OPERATIONS.md, TROUBLESHOOTING.md, INSTRUCTIONS.md, INDEX.md
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-documentation-overhaul/23-CONTEXT.md
@docs/INDEX.md
@docs/OPERATIONS.md
@docs/TROUBLESHOOTING.md
@docs/INSTRUCTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Add deployment-mode sections to OPERATIONS.md</name>
  <files>docs/OPERATIONS.md</files>
  <read_first>docs/OPERATIONS.md</read_first>
  <action>
    Read the full file first. Identify existing content and group it by deployment mode.

    Add the following H2 structure at appropriate positions:
    - `## Common` at the top — for content that applies to all modes (env vars, architecture, prerequisites). Move existing general/overview content under this section.
    - `## Docker Compose` — gather existing Docker Compose content here. If none exists, add a placeholder: "Refer to `docker-compose.yml` and `docs/INSTRUCTIONS.md → Docker Compose` for Docker Compose setup."
    - `## Helm` — gather existing Helm content. If none exists, add: "See `deployment/helm/` and `docs/KUBERNETES.md` for Helm-based deployment."
    - `## Systemd` — gather existing systemd content. Reference `scripts/kb-mcp.service`.
    - `## Manual` — gather existing manual/source-based setup content.

    After each mode section (except the last), add a see-also footer:
    `> **See also:** [TROUBLESHOOTING.md → {Mode}](TROUBLESHOOTING.md#{mode-anchor}), [INSTRUCTIONS.md → {Mode}](INSTRUCTIONS.md#{mode-anchor})`

    Content not specific to any mode should remain under `## Common`. Do NOT delete any existing content — only reorganize under new H2 headings. Preserve all existing H3+ subsections, code blocks, and links.
  </action>
  <verify>grep -c '^## Common' docs/OPERATIONS.md && grep -c '^## Docker Compose' docs/OPERATIONS.md && grep -c '^## Helm' docs/OPERATIONS.md && grep -c '^## Systemd' docs/OPERATIONS.md && grep -c '^## Manual' docs/OPERATIONS.md</verify>
  <acceptance_criteria>
    - OPERATIONS.md has ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual sections
    - Each section has relevant content (not empty)
    - See-also footers present after each mode section
    - Zero content deleted — all original content preserved
  </acceptance_criteria>
  <done>All 5 H2 sections present in OPERATIONS.md with mode-appropriate content and see-also footers</done>
</task>

<task type="auto">
  <name>Add deployment-mode sections to TROUBLESHOOTING.md</name>
  <files>docs/TROUBLESHOOTING.md</files>
  <read_first>docs/TROUBLESHOOTING.md</read_first>
  <action>
    Same structure as OPERATIONS.md: Add ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual H2 sections.

    Read the full file first. Group existing troubleshooting topics under the appropriate mode. For each mode section, leave any mode-specific issues and solutions. Put cross-mode/general issues under ## Common.

    After each mode section (except last), add see-also footer:
    `> **See also:** [OPERATIONS.md → {Mode}](OPERATIONS.md#{mode-anchor}), [INSTRUCTIONS.md → {Mode}](INSTRUCTIONS.md#{mode-anchor})`

    Preserve all existing content — only reorganize.
  </action>
  <verify>grep -c '^## Common' docs/TROUBLESHOOTING.md && grep -c '^## Docker Compose' docs/TROUBLESHOOTING.md && grep -c '^## Helm' docs/TROUBLESHOOTING.md && grep -c '^## Systemd' docs/TROUBLESHOOTING.md && grep -c '^## Manual' docs/TROUBLESHOOTING.md</verify>
  <acceptance_criteria>
    - TROUBLESHOOTING.md has all 5 H2 sections
    - Each section has relevant content
    - See-also footers present after each mode section
    - Zero content deleted
  </acceptance_criteria>
  <done>All 5 H2 sections present in TROUBLESHOOTING.md with see-also footers</done>
</task>

<task type="auto">
  <name>Add deployment-mode sections to INSTRUCTIONS.md</name>
  <files>docs/INSTRUCTIONS.md</files>
  <read_first>docs/INSTRUCTIONS.md</read_first>
  <action>
    Same structure: Add ## Common, ## Docker Compose, ## Helm, ## Systemd, ## Manual H2 sections.

    Read the full file first. Group existing setup/configuration instructions under the appropriate mode. Put shared setup steps (prerequisites, environment setup) under ## Common.

    After each mode section (except last), add see-also footer:
    `> **See also:** [OPERATIONS.md → {Mode}](OPERATIONS.md#{mode-anchor}), [TROUBLESHOOTING.md → {Mode}](TROUBLESHOOTING.md#{mode-anchor})`

    Preserve all existing content — only reorganize.
  </action>
  <verify>grep -c '^## Common' docs/INSTRUCTIONS.md && grep -c '^## Docker Compose' docs/INSTRUCTIONS.md && grep -c '^## Helm' docs/INSTRUCTIONS.md && grep -c '^## Systemd' docs/INSTRUCTIONS.md && grep -c '^## Manual' docs/INSTRUCTIONS.md</verify>
  <acceptance_criteria>
    - INSTRUCTIONS.md has all 5 H2 sections
    - Each section has relevant content
    - See-also footers present after each mode section
    - Zero content deleted
  </acceptance_criteria>
  <done>All 5 H2 sections present in INSTRUCTIONS.md with see-also footers</done>
</task>

<task type="auto">
  <name>Enhance INDEX.md with deployment-mode navigation</name>
  <files>docs/INDEX.md</files>
  <read_first>docs/INDEX.md</read_first>
  <action>
    Read the current INDEX.md. Add a new section near the top (after "Getting Started" or at an appropriate position):

    ```
    ## Deployment Modes

    Choose your deployment mode:

    - **Docker Compose** → [OPERATIONS.md → Docker Compose](OPERATIONS.md#docker-compose), [TROUBLESHOOTING.md → Docker Compose](TROUBLESHOOTING.md#docker-compose), [INSTRUCTIONS.md → Docker Compose](INSTRUCTIONS.md#docker-compose)
    - **Helm (Kubernetes)** → [OPERATIONS.md → Helm](OPERATIONS.md#helm), [TROUBLESHOOTING.md → Helm](TROUBLESHOOTING.md#helm), [INSTRUCTIONS.md → Helm](INSTRUCTIONS.md#helm)
    - **Systemd (Bare Metal)** → [OPERATIONS.md → Systemd](OPERATIONS.md#systemd), [TROUBLESHOOTING.md → Systemd](TROUBLESHOOTING.md#systemd), [INSTRUCTIONS.md → Systemd](INSTRUCTIONS.md#systemd)
    - **Manual (Source)** → [OPERATIONS.md → Manual](OPERATIONS.md#manual), [TROUBLESHOOTING.md → Manual](TROUBLESHOOTING.md#manual), [INSTRUCTIONS.md → Manual](INSTRUCTIONS.md#manual)
    ```

    Use file-level links per D-04 (no H2 anchor references — just file names). The anchor-based links above are acceptable as they point to the same file's sections for discoverability, but the primary link should be to the file itself. Keep all existing content intact.
  </action>
  <verify>grep -c 'Deployment Modes' docs/INDEX.md</verify>
  <acceptance_criteria>
    - INDEX.md has a "Deployment Modes" section
    - Each mode links to the 3 reorganized docs
    - Existing content preserved
  </acceptance_criteria>
  <done>"Deployment Modes" section present in INDEX.md with links to all 3 reorganized files</done>
</task>

</tasks>

<verification>
- [ ] All 3 operational files have Common + 4 deployment-mode H2 sections
- [ ] INDEX.md has Deployment Modes section
- [ ] See-also footers present in all 3 operational files
- [ ] No content was deleted — grep for any missing content patterns
</verification>

<success_criteria>
- User can find mode-specific guidance in OPERATIONS.md, TROUBLESHOOTING.md, INSTRUCTIONS.md
- INDEX.md has a deployment-mode navigation section
- See-also footers enable cross-file navigation between modes
- All existing content preserved, only reorganized
</success_criteria>

<output>
After completion, create `.planning/phases/23-documentation-overhaul/23-01-SUMMARY.md`
</output>
