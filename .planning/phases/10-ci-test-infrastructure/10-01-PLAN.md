---
phase: 10-ci-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEBT-02]
user_setup: []

must_haves:
  truths:
    - "helm lint runs in CI on every PR and push to master"
    - "Structural errors in Helm chart are caught before deployment"
    - "CI passes with helm lint reporting no errors"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with helm lint step"
      contains: "helm lint"
    - path: "deployment/helm/kb-rag-mcp/Chart.yaml"
      provides: "Valid Helm chart passing lint"
      min_lines: 10
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "deployment/helm/kb-rag-mcp/"
      via: "helm lint command"
      pattern: "helm.*lint"
---

<objective>
Add `helm lint` validation to the CI pipeline so structural errors in Kubernetes manifests are caught before deployment.

**Purpose:** Helm charts are currently validated only manually (`helm lint` never runs in CI). This creates risk of deploying broken charts to production. A single `helm lint` step in CI catches missing fields, invalid YAML, and template syntax errors.

**Output:** Updated `.github/workflows/ci.yml` with helm lint step; any Chart.yaml issues fixed.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml
@deployment/helm/kb-rag-mcp/Chart.yaml
@deployment/helm/kb-rag-mcp/values.yaml

Existing CI workflow already has test + coverage + SSE handler steps. Add helm lint as a separate job running in parallel.

<interfaces>
From .github/workflows/ci.yml:
```yaml
name: CI
on:
  push: { branches: [master] }
  pull_request: { branches: [master] }
jobs:
  test: { runs-on: ubuntu-latest, strategy.matrix.python-version: ["3.11", "3.12", "3.13"] }
```

From deployment/helm/kb-rag-mcp/Chart.yaml:
```yaml
apiVersion: v2
name: kb-rag-mcp
version: 0.1.0
appVersion: "1.0"
```

From deployment/helm/kb-rag-mcp/values.yaml:
- All config keys defined, Qdrant enabled by default, Redis disabled by default
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add helm lint job to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
    Add a new `helm-lint` job to `.github/workflows/ci.yml` that:
    
    1. Uses `actions/checkout@v4`
    2. Sets up Helm using `azure/setup-helm@v4` with version `3.16.x` (exact version to match the latest stable release available for `azure/setup-helm@v4`; pin to `3.16.x` for reproducibility)
    3. Runs `helm lint deployment/helm/kb-rag-mcp/ --strict` - the `--strict` flag treats warnings as errors
    4. Runs `helm template deployment/helm/kb-rag-mcp/` (dry-run rendering) to catch template syntax errors that `lint` might miss
    5. Both steps run regardless of the matrix - this is a single job, not matrix-strategied
    
    The job should:
    - Run on `ubuntu-latest`
    - Trigger on the same events (push to master + PR to master)
    - Use `actions/checkout@v4`
    - Be named `helm-lint` for clarity
    - The Helm chart is at `deployment/helm/kb-rag-mcp/`
    - Do NOT modify any existing test/coverage/SSE jobs — append the new job after them
    
    Use `azure/setup-helm@v4` (the standard GitHub Action for installing Helm in CI).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/luciano.marinho/git/kb-rag-mcp && grep -q "helm lint" .github/workflows/ci.yml && echo "helm-lint job found ✓"</automated>
    <human-check>Verify .github/workflows/ci.yml has a new `helm-lint` job with setup-helm, helm lint, and helm template steps</human-check>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `helm lint deployment/helm/kb-rag-mcp/ --strict` 
    - `.github/workflows/ci.yml` contains `azure/setup-helm@v4`
    - `.github/workflows/ci.yml` contains `helm template deployment/helm/kb-rag-mcp/`
    - Existing test/coverage/SSE jobs are unchanged
  </acceptance_criteria>
  <done>CI workflow has a `helm-lint` job that runs `helm lint --strict` and `helm template` on every push/PR</done>
</task>

<task type="auto">
  <name>Task 2: Fix any Chart.yaml issues found by helm lint</name>
  <files>deployment/helm/kb-rag-mcp/Chart.yaml</files>
  <read_first>deployment/helm/kb-rag-mcp/Chart.yaml</read_first>
  <action>
    Review `deployment/helm/kb-rag-mcp/Chart.yaml` for common `helm lint` issues:
    
    1. Verify `apiVersion: v2` is present (already is)
    2. Verify `name` and `version` fields are present (already are)
    3. Add `sources:` field listing the project repo URL: `https://github.com/your-org/kb-rag-mcp` (replace with actual repo URL - but this is fine as-is since it's metadata only)
    4. Check that `maintainers` has at least one valid entry (already has a placeholder)
    
    Only fix issues that would cause `helm lint --strict` to fail. Do NOT add unnecessary fields.
    
    If `helm` is not available locally to run the lint, that's fine — the CI check in Task 1 will catch issues. For now, ensure the Chart.yaml is structurally valid (valid YAML, required fields present, no syntax errors).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/luciano.marinho/git/kb-rag-mcp && python3 -c "import yaml; yaml.safe_load(open('deployment/helm/kb-rag-mcp/Chart.yaml')); print('Chart.yaml is valid YAML ✓')"</automated>
  </verify>
  <acceptance_criteria>
    - `deployment/helm/kb-rag-mcp/Chart.yaml` is valid YAML
    - `apiVersion: v2`, `name: kb-rag-mcp`, `version: 0.1.0` all present
    - No syntax errors in the file
  </acceptance_criteria>
  <done>Chart.yaml is structurally valid; helm lint runs in CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → internet | Helm binary is downloaded from GitHub during CI |
| CI runner → deployment/helm/ | CI reads locally checked-out chart files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `azure/setup-helm@v4` action | mitigate | Pin action to `@v4` (stable major version); helm binary is verified by GitHub Actions ecosystem |
| T-10-02 | Tampering | helm install from external repo | accept | helm binary downloaded from official GitHub releases via setup-helm; acceptable risk for CI runner |
| T-10-SC | Tampering | azure/setup-helm@v4 | mitigate | Standard GitHub Action — `[ASSUMED]` legitimate. No blocking human checkpoint needed. |
</threat_model>

<verification>
1. `grep -q "helm lint" .github/workflows/ci.yml` — helm lint step present
2. `grep -q "helm template" .github/workflows/ci.yml` — helm template step present
3. `python3 -c "import yaml; yaml.safe_load(open('deployment/helm/kb-rag-mcp/Chart.yaml'))"` — Chart.yaml valid
</verification>

<success_criteria>
- New `helm-lint` job exists in `.github/workflows/ci.yml` running `helm lint --strict`
- `helm template` dry-run renders successfully in CI
- No existing CI jobs modified
</success_criteria>

<output>
Create `.planning/phases/10-ci-test-infrastructure/10-01-SUMMARY.md` when done
</output>
