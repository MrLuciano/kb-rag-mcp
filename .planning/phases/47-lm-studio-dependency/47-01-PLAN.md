---
phase: 47-lm-studio-dependency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/server.py
  - ingest/cli/check.py
  - tests/test_search.py
  - tests/test_cli_check.py
autonomous: true
requirements: [T-02]

must_haves:
  truths:
    - "Server starts successfully when embedding backend is unreachable (non-fatal warning logs, no crash)"
    - "First search query returns a clear user-facing error instead of a generic crash / stack trace when embedding is down"
    - "kb-ingest check embedding validates embedding backend connectivity with exit code 0/1"
  artifacts:
    - path: "kb_server/server.py:650-730"
      provides: "Graceful error response in _search_kb when get_embedding fails"
      contains: "Embedding backend unavailable"
    - path: "kb_server/server.py:1429-1440"
      provides: "Startup warning logged with OPERATIONS.md reference"
      contains: "OPERATIONS.md"
    - path: "ingest/cli/check.py"
      provides: "kb-ingest check embedding subcommand"
      contains: "check_embedding_service"
    - path: "tests/test_search.py"
      provides: "Test that embedding failure returns user-friendly message"
      pattern: "def test_search.*unavailable"
    - path: "tests/test_cli_check.py"
      provides: "Tests for kb-ingest check embedding subcommand"
      pattern: "def test_check_embedding"
  key_links:
    - from: "kb_server/server.py:_search_kb"
      to: "kb_server/embed_client.py:get_embedding"
      via: "try/except around await get_embedding(query)"
      pattern: "get_embedding"
    - from: "kb_server/server.py:main()"
      to: "kb_server/health.py:check_embedding_service"
      via: "startup health check"
      pattern: "check_embedding_service"
    - from: "ingest/cli/check.py:embedding subcommand"
      to: "kb_server/health.py:check_embedding_service"
      via: "asyncio.run(check_embedding_service())"
      pattern: "check_embedding_service"

user_setup: []
---

<objective>
Graceful embedding backend fallback: when LM Studio / Ollama / OpenAI-compat is unreachable, the server stays up, search returns a clear error message instead of a crash, and `kb-ingest check embedding` validates backend connectivity.

**Purpose:** Eliminate server crashes on startup when embedding backend is down; give users actionable diagnostic messages.
**Output:** Modified `server.py` (graceful error + OPERATIONS.md link in startup warning), new `kb-ingest check embedding` subcommand, corresponding tests.
</objective>

<execution_context>
@/home/admin/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-lm-studio-dependency/47-CONTEXT.md
@kb_server/server.py
@kb_server/embed_client.py
@ingest/cli/check.py
@tests/test_cli_check.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Graceful embedding error response in _search_kb + OPERATIONS.md startup link</name>
  <files>
    kb_server/server.py
    tests/test_search.py
  </files>
  <action>
    Per D-03 (graceful degradation) and D-01 (startup health check):

    1. In `_search_kb()` (`kb_server/server.py` around line 727), wrap `vector = await get_embedding(query)` in a try/except block that catches `Exception`. On failure, return a single-element `[types.TextContent(type="text", ...)]` with the exact message (per D-03):
       `"Embedding backend unavailable — start LM Studio or check EMBED_BACKEND config"`
       Do NOT let the exception propagate to the generic `call_tool` handler — the user-facing message must be this specific string, not a generic error wrapping.

    2. In `main()` (`kb_server/server.py` lines 1433–1437), update the warning log when `embedding_status.healthy` is False to include a reference to OPERATIONS.md. The message should read:
       `f"Embedding backend unreachable: {embedding_status.message} — queries will fail. Configure EMBED_BACKEND, start LM Studio, or see OPERATIONS.md for troubleshooting."`
       Keep the existing `log.warning()` call — just change the message string. Do NOT change the else-branch.

    3. Add a test to the existing search test file (`tests/test_search.py`) — if the file does not exist yet, create it. Use `unittest.mock.patch` to mock `kb_server.server.get_embedding` so it raises a `RuntimeError("All providers failed")`. Invoke the MCP tool dispatch via the server's app instance (or directly call `_search_kb({"query": "test"})`). Verify the return value is a `TextContent` list whose `.text` attribute contains the exact string `"Embedding backend unavailable"`. Use `pytest.mark.asyncio` for the async test.

    Do NOT modify `get_embedding()` or `_dispatch_with_resilience()` — the error propagation path is correct; we are only changing how `_search_kb` surfaces the failure to the user per D-03.
  </action>
  <verify>
    <automated>
      python -m pytest tests/test_search.py -x -q --no-header -k "embedding_unavailable" 2>/dev/null || echo "No existing test file — creating scaffold"
    </automated>
  </verify>
  <done>
    - Search returns "Embedding backend unavailable — start LM Studio or check EMBED_BACKEND config" when embedding is down
    - Startup warning includes "OPERATIONS.md" in the log message
    - Test passes confirming the exact error message is returned
    - No change to get_embedding() or _dispatch_with_resilience()
  </done>
</task>

<task type="auto">
  <name>Task 2: Add kb-ingest check embedding subcommand + tests</name>
  <files>
    ingest/cli/check.py
    tests/test_cli_check.py
  </files>
  <action>
    Per D-02 (extend kb-ingest check for embedding backend validation):

    1. Add a new Click command `embedding` under the existing `check_group()` in `ingest/cli/check.py`. Follow the same pattern as the existing `health` subcommand:
       - Use `@check_group.command(name="embedding")`
       - Accept an optional `--verbose` / `-v` flag (same pattern as `health`)
       - In the command body, use `asyncio.run(check_embedding_service())` imported from `kb_server.health`
       - Wrap the call in try/except to handle import errors gracefully (the ingest CLI may run without the kb_server package available)
       - Print a Rich-formatted result: green "✓ Embedding backend: {backend_name}" on success, red "✗ Embedding backend unavailable — {error message}" on failure
       - Exit code 0 if healthy, 1 if unhealthy

       Keep the existing `health` subcommand unchanged — this is additive.

    2. Add tests in `tests/test_cli_check.py` using the same Click `CliRunner` pattern already established:
       - `test_check_embedding_healthy_exits_zero` — mock `kb_server.health.check_embedding_service` to return `HealthStatus(name="embedding", healthy=True, message="Backend: lmstudio", details={"backend": "lmstudio"})` and verify exit 0 + success output
       - `test_check_embedding_unhealthy_exits_one` — mock to return `HealthStatus(name="embedding", healthy=False, message="connection refused")` and verify exit 1 + error output
       - `test_check_embedding_exception_handled` — mock with `side_effect=Exception("timeout")` and verify exit 1 without raw traceback in output
       - Use `from ingest.cli.main import cli` and `cli_runner.invoke(cli, ["check", "embedding"])` — same pattern as existing tests

    Import `check_embedding_service` lazily inside the command function body (not at module level) so the ingest CLI does not force-load kb_server modules at import time. Use a try/except ImportError with a helpful message if the module is not available.
  </action>
  <verify>
    <automated>
      python -m pytest tests/test_cli_check.py -x -q --no-header -k "check_embedding" 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - `kb-ingest check embedding` works: exits 0 when healthy, 1 when unhealthy
    - `kb-ingest check embedding --verbose` shows backend/model details
    - All 3 new tests pass in test_cli_check.py
    - Existing `kb-ingest check health` is unchanged and all its tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→MCP server | Untrusted search query enters the system |
| MCP server→embedding backend | Outbound HTTP to LM Studio / Ollama / OpenAI-compat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | `_search_kb` error handler | mitigate | Return D-03's user-friendly message instead of raw exception text to avoid leaking config details (provider chain, hostnames, error internals) |
| T-47-02 | Denial of Service | `_dispatch_with_resilience` | accepted | Phase 36 circuit breaker + provider budget already handle repeated-call amplification. This phase only changes error surfacing — no new DoS surface. |
| T-47-SC | Tampering | `pip install` / `requirements.in` | mitigate | No new dependencies introduced. Existing runtime deps subject to prior audit (Phase 2). No slopcheck gate needed. |
</threat_model>

<verification>
- [ ] `pytest tests/test_search.py -x -k "embedding_unavailable"` passes
- [ ] `pytest tests/test_cli_check.py -x -k "check_embedding"` passes
- [ ] `pytest tests/ -x` shows 0 regressions (run at end)
- [ ] Manual: set EMBED_BACKEND to invalid address, start server — should log warning with OPERATIONS.md ref, not crash
- [ ] Manual: `kb-ingest check embedding` exits 0/1 correctly based on backend status
</verification>

<success_criteria>
1. `_search_kb` returns the D-03 message string when embedding backend is unreachable (verified by automated test)
2. `main()` startup warning for embedding failure includes "OPERATIONS.md" reference (verified by visual / grep check)
3. `kb-ingest check embedding` subcommand exists and validates backend connectivity (verified by CLI test)
4. All 666 pre-existing tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/47-lm-studio-dependency/47-01-SUMMARY.md` when done
</output>
