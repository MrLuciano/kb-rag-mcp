---
phase: 39-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/health.py
  - kb_server/health_server.py
  - kb_server/observability/middleware.py
  - kb_server/observability/percentiles.py
  - kb_server/server.py
  - tests/test_health_grafana.py
  - tests/test_request_id_middleware.py
  - tests/test_percentile_metrics.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - METRICS-01
must_haves:
  truths:
    - Grafana connectivity check in health system
    - Request ID middleware on all requests
    - Per-operation percentile latency metrics (p50/p95/p99)
  artifacts:
    - path: "kb_server/health.py"
      provides: "Grafana health check"
      contains: "check_grafana"
      min_lines: 30
    - path: "kb_server/observability/middleware.py"
      provides: "Request ID middleware"
      contains: "RequestIDMiddleware"
      min_lines: 30
    - path: "kb_server/observability/percentiles.py"
      provides: "Percentile latency tracking"
      contains: "PercentileTracker"
      min_lines: 80
---

<objective>
Add Grafana connectivity check to health system, request ID middleware for traceability, and per-operation percentile latency metrics (p50/p95/p99).

Purpose: Phase 39 closes observability backlog items with three independent features in the observability layer: (1) a new `check_grafana()` function in `kb_server/health.py` and integration into the health system, (2) a Starlette middleware generating `X-Request-Id` on every request, and (3) a `PercentileTracker` using sorted-list histograms exposed via existing Prometheus `/metrics` endpoint.

Tech Stack: FastAPI, Starlette middleware, Prometheus, asyncio, uuid, bisect.

Output: Grafana health check, RequestIDMiddleware, PercentileTracker with Prometheus export, health summary API endpoint.
</objective>

<execution_context>
@/home/admin/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@kb_server/health.py
@kb_server/health_server.py
@kb_server/observability/middleware.py
@kb_server/observability/percentiles.py
@kb_server/server.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| health endpoint → public | Health status could leak internal infrastructure details |
| request → middleware | Client-supplied request IDs could collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | health endpoint leaks network topology | mitigate | Health check returns only connectivity status, not full config |
| T-39-02 | Denial of Service | unbounded latency tracking memory | mitigate | PercentileTracker caps at window_size=1000 samples per operation |
| T-39-03 | Tampering | Prometheus double-counting | mitigate | export_prometheus resets all state after each scrape |
</threat_model>

<task type="auto">
  <name>Add Grafana connectivity check to health system</name>
  <files>kb_server/health.py, kb_server/health_server.py, tests/test_health_grafana.py</files>
  <read_first>kb_server/health.py</read_first>
  <action>
Write failing test for check_grafana, then implement check_grafana() in health.py with TCP connection test via asyncio.open_connection, add to check_all_components(), run tests, commit.
  </action>
  <verify>
    <automated>cd /home/admin/kb-rag-mcp && python -m pytest tests/test_health_grafana.py -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `check_grafana()` returns `HealthStatus` with `healthy=True` on successful TCP connection
    - `check_grafana()` returns `HealthStatus` with `healthy=False` and error message on connection failure
    - Returns `healthy=True` with "Not configured" when `GRAFANA_URL` env var is empty
    - Included in `check_all_components()` result list
    - All tests pass (test_check_grafana_success, test_check_grafana_failure)
  </acceptance_criteria>
  <done>
    check_grafana function implemented and tested. Committed: `feat(39): add Grafana connectivity health check`
  </done>
</task>

<task type="auto">
  <name>Request ID middleware</name>
  <files>kb_server/observability/middleware.py, tests/test_request_id_middleware.py</files>
  <read_first>kb_server/observability/middleware.py</read_first>
  <action>
Write failing tests for request ID generation, preservation, and isolation. Create RequestIDMiddleware in middleware.py using Starlette BaseHTTPMiddleware, ContextVar for propagation, and get_current_request_id() helper. Run tests, commit.
  </action>
  <verify>
    <automated>cd /home/admin/kb-rag-mcp && python -m pytest tests/test_request_id_middleware.py -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Middleware generates UUID v4 if client does not send X-Request-Id
    - Middleware preserves client-sent X-Request-Id header
    - X-Request-Id is unique per request (no collisions across concurrent requests)
    - Response includes X-Request-Id header matching the request
    - ContextVar propagates request ID to downstream handler code
    - get_current_request_id() returns the active request ID from context
  </acceptance_criteria>
  <done>
    RequestIDMiddleware implemented and tested. Committed: `feat(39): add request ID middleware with ContextVar`
  </done>
</task>

<task type="auto">
  <name>Per-operation percentile metrics (p50/p95/p99)</name>
  <files>kb_server/observability/percentiles.py, kb_server/server.py, kb_server/health_server.py, tests/test_percentile_metrics.py</files>
  <read_first>kb_server/observability/percentiles.py</read_first>
  <action>
Write failing tests for PercentileTracker. Create PercentileTracker in percentiles.py using bisect.insort for sorted-list histogram, get_stats(), reset(), export_prometheus(). Integrate with health_server.py /metrics endpoint. Add latency tracking to server.py tool handlers (search_kb, list_documents, get_chunk, kb_stats). Run tests, commit.
  </action>
  <verify>
    <automated>cd /home/admin/kb-rag-mcp && python -m pytest tests/test_percentile_metrics.py -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PercentileTracker.record() stores latency samples per operation key
    - get_stats() returns count, p50, p95, p99 from sorted samples
    - Returns zeros for empty/unregistered operations
    - export_prometheus() generates Prometheus gauge lines for p50/p95/p99 per operation
    - export_prometheus() resets all data after export
    - window_size bounds memory growth
    - /metrics endpoint includes percentile data in Prometheus format
    - server.py tool handlers wrap core logic with latency recording
  </acceptance_criteria>
  <done>
    PercentileTracker implemented, integrated with metrics endpoint and server tool handlers. Committed: `feat(39): add per-operation percentile latency tracking`
  </done>
</task>

<task type="auto">
  <name>Expose health summary via admin API</name>
  <files>kb_server/health_server.py</files>
  <read_first>kb_server/health_server.py</read_first>
  <action>
Add health summary endpoint to admin API router or document that /health/detailed already provides this. Integration note for Phase 28c (Admin SPA Shell) when the admin API router is set up.
  </action>
  <verify>
    <automated>cd /home/admin/kb-rag-mcp && python -m pytest -x -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Health summary is accessible via an API endpoint
    - Returns aggregated health status from all check functions
    - If no admin router exists, /health/detailed already provides equivalent data
  </acceptance_criteria>
  <done>
    Health summary endpoint noted for Phase 28c integration. Committed (if modified): `feat(39): expose health summary via /api/v1/health`
  </done>
</task>

<verification>
- `.venv/bin/python -m pytest tests/test_health_grafana.py -x -v`
- `.venv/bin/python -m pytest tests/test_request_id_middleware.py -x -v`
- `.venv/bin/python -m pytest tests/test_percentile_metrics.py -x -v 2>&1 | tail -20`
- Full suite: `.venv/bin/python -m pytest -x --timeout=60 2>&1 | tail -20` (no regressions)
</verification>

<success_criteria>
- check_grafana() returns HealthStatus with correct healthy/message fields
- All requests receive X-Request-Id header, preserved from client or generated
- PercentileTracker records latency and exports p50/p95/p99 Prometheus metrics
- All new tests pass with 90%+ branch coverage
- No regressions in existing test baseline
</success_criteria>

<output>

- `kb_server/health.py` — adds `check_grafana()` function and updates `check_all_components()`
- `kb_server/observability/middleware.py` — new file with `RequestIDMiddleware` and `get_current_request_id()`
- `kb_server/observability/percentiles.py` — new file with `PercentileTracker` and `get_percentile_tracker()` singleton
- `kb_server/health_server.py` — integrates percentile export into `/metrics` endpoint
- `kb_server/server.py` — adds latency tracking to all tool handlers
- `tests/test_health_grafana.py` — 2 tests (success, failure)
- `tests/test_request_id_middleware.py` — 3 tests (generation, preservation, isolation)
- `tests/test_percentile_metrics.py` — 5 tests (record, empty, window, reset, prometheus export)
</output>
