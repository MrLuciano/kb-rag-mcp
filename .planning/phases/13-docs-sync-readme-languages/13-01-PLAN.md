---
phase: 13-docs-sync-readme-languages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "README.md feature list reflects all shipped features (Phases 5-12: SSE, Python 3.13, lazy reranker, Helm chart, OTCS auto-tagging, CI/CD gates, English sweep, auto-classification)"
    - "README.md Quick Start and Installation sections reference kb_server/ paths, not server/"
    - "README.md Configuration section includes ALL current environment variables"
    - "README.md Usage section covers kb-rag CLI, ingestion, auto-ingestion, version filtering, metadata overrides, auto-classification"
    - "README.md Production Deployment section references current service architecture and v1.3"
    - "No 'FASE N' labels or 'NEW' badges remain for features shipped prior to Phase 9"
  artifacts:
    - path: "README.md"
      provides: "Refreshed core sections (header through Usage)"
      min_lines: 900
      contains: "kb_server.server, v1.3, 585 tests, Python 3.13, SSE, Helm chart, CI/CD"
  key_links:
    - from: "README.md"
      to: "docs/AUTO_INGESTION.md"
      via: "link reference"
      pattern: "AUTO_INGESTION"
    - from: "README.md"
      to: "docs/METADATA_OVERRIDES.md"
      via: "link reference"
      pattern: "METADATA_OVERRIDES"
    - from: "README.md"
      to: "docs/VERSION_FILTERING.md"
      via: "link reference"
      pattern: "VERSION_FILTERING"
---

<objective>
Refresh the top half of README.md (header through Usage section) to reflect all changes shipped since v1.0 — Phases 5-12 features, Python 3.13 support, current CLI, and current architecture paths.

**Purpose:** New users see an accurate, up-to-date README that reflects the current state of the project. Stale references to `server/` (deleted), old test counts (491 → 585), and missing features are corrected.

**Output:** Updated `README.md` with refreshed core sections.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (lines 249-283: Phase 13 goal, milestones v1.0-v1.3)
@.planning/phases/13-docs-sync-readme-languages/RESEARCH.md (all: current state analysis)

# Key facts for this plan:
# - Test count: 585 (up from 491)
# - Python support: 3.11, 3.12, 3.13
# - Module: kb_server/ (not server/ — legacy directory deleted in Phase 1)
# - CLI entry: `kb-rag` (setup.py console_scripts)
# - Version: v1.3 milestone (ROADMAP), setup.py says "1.0.0" (keep for now)
# - Features shipped since README was last updated (v1.0):
#   Phase 5: SSE transport, Python 3.13 CI matrix
#   Phase 6: 585 tests (up from 491), full mock isolation
#   Phase 7: Coverage gate (90% branch), logging audit
#   Phase 8: OTCS auto-tagging (10 products), kb-rag status CLI, English docstrings, docs refresh
#   Phase 9: Cross-encoder lazy loading (~10s faster startup), kb-rag check health CLI, pre-flight health checks
#   Phase 10: CI helm lint, logging audit --fail-under, real qdrant imports
#   Phase 11: Auto-classification (vendor, subsystem, version + document metadata extraction)
#   Phase 12: 0 Portuguese across all source files, English CI gate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README.md header, features, TOC, and Quick Start through Production Deployment</name>
  <files>README.md</files>
  <action>
    Read the current README.md (1638 lines). Modify lines 1-310 (header through "Production Deployment" section):

    1. **Header (lines 1-13):** Keep the language selector links. Add "585 tests" to the tagline. Update the feature list:
       - Remove "(NEW)" suffix from Auto-ingestion, Version filtering, Metadata overrides (these shipped in v1.0 era)
       - Add these new features with `✅` prefix in the bullet list:
         - `✅ **585 tests** — Full mock isolation, no external deps needed for unit tests`
         - `✅ **CI/CD pipeline** — Coverage gate (90% branch), logging audit, Helm lint, English audit`
         - `✅ **SSE transport** — Starlette 1.0.0 with stable disconnect handling`
         - `✅ **Python 3.13** — CI matrix tests on 3.11, 3.12, 3.13`
         - `✅ **Auto-classification** — Vendor, product, subsystem, and version inference from filenames and metadata`
         - `✅ **Lazy cross-encoder** — ~10s faster server startup, model loads on first reranking query`
         - `✅ **Kubernetes/Helm** — Helm chart for multi-replica deployment`
         - `✅ **English sweep** — Zero Portuguese across all source files, enforced by CI`
       - Move multi-backend, batch processing, operations to after the new items

    2. **TOC (lines 33-49):** Keep structure unchanged. Add "Auto-Classification" under Usage if a sub-section will cover it.

    3. **Quick Start (lines 52-143):**
       - Update prerequisites: add "Python 3.13" to "Python 3.11+"
       - In Option 1 (one-command setup): update script output description, mention `kb-rag check health`
       - In Option 3 (manual setup): change step 4 from `python -m kb_server.server` (it might already be correct, ensure it is)
       - In "Connect your AI assistant": ensure stdio command uses `/path/to/kb-rag-mcp/.venv/bin/python -m kb_server.server` (not `server.server`)
       - In verify section: mention `kb-rag check health` as alternative

    4. **Production Deployment (lines 146-310):**
       - Update "What Gets Installed" to include coverage gate service, English audit
       - Keep service management section accurate — verify service names match current systemd units
       - Update version references from v0.9.0 / v1.0 to v1.3
       - Fix the duplicate "systemd Services (4):" block (lines 183-188) — deduplicate, keep one clean list
       - Update the "See docs/FASE9_COMPLETION.md" reference on line 308 to point to `docs/OPERATIONS.md` (the canonical production deploy doc)
       - Update resource requirements table to reflect current measurements
       - Production Checklist: SSE transport is not optional — remove SSL/TLS caveat

    **Avoid:**
    - Do NOT change the license or contributing sections (handled in Plan 13-02)
    - Do NOT change the pt-BR section (handled in Plan 13-03)
    - Do NOT change setup.py version — this is a docs-only phase
    - Do NOT add emojis where not already present
    - Do NOT rename internal anchor links that would break TOC navigation
  </action>
  <verify>
    <automated>
      # Verify key updates exist
      grep -c "585 tests" README.md || echo "MISSING: test count" 
      grep -c "Python 3.13" README.md || echo "MISSING: Python 3.13"
      grep -c "SSE transport" README.md || echo "MISSING: SSE"
      grep -c "kb_server" README.md || echo "MISSING: kb_server paths"
      grep -c "v1.3" README.md || echo "MISSING: v1.3 milestone"
      grep -c "Auto-classification" README.md || echo "MISSING: auto-classification"
      grep -c "lazy.*cross-encoder\|cross-encoder.*lazy" README.md || echo "MISSING: lazy reranker"
      grep -c "Helm\|Helm chart" README.md || echo "MISSING: Helm"
      grep -c "CI/CD" README.md || echo "MISSING: CI/CD"
      grep -c "Zero Portuguese\|English sweep\|0 Portuguese" README.md || echo "MISSING: English sweep"
      # Verify stale terms removed
      if grep -q "FASE [0-9]" README.md; then echo "STALE: FASE labels remain"; else echo "OK: No FASE labels"; fi
      if grep -q "server.server\|server.health_server" README.md; then echo "STALE: server/ paths remain"; else echo "OK: No server/ paths"; fi
      if grep -q "(NEW)" README.md; then echo "STALE: (NEW) badges remain"; else echo "OK: No (NEW) badges"; fi
    </automated>
  </verify>
  <done>README.md header through Production Deployment refreshed with all Phase 5-12 features, current paths, no stale references</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Installation, Configuration, and Usage sections</name>
  <files>README.md</files>
  <action>
    Modify README.md lines 312-576 (Installation through Usage section end):

    1. **Installation (lines 312-382):**
       - Prerequisites: add Python 3.13 alongside 3.11+
       - Step 5 (Start Qdrant): use `docker compose` (not `docker-compose` — modern Docker CLI)
       - Step 6: remove `server.health_server` reference — health server is part of `kb_server.server` now, or use `kb-rag check health`
       - Add macOS support note if applicable

    2. **Configuration — Environment Variables (lines 388-417):**
       - Add new environment variables shipped since v1.0:
         - `EMBED_BATCH_SIZE`, `FILE_BATCH_SIZE`, `QDRANT_BATCH_SIZE` (batch processing, Phase 8)
         - `HTTP_POOL_CONNECTIONS`, `MAX_CONCURRENT_UPLOADS` (connection tuning, Phase 8)
         - `WORKER_POOL_SIZE`, `WORKER_RATE_LIMIT` (worker pool, Phase 8)
         - `WATCH_PATH`, `WATCH_DEBOUNCE_SECONDS`, `WATCH_RECURSIVE`, `WATCH_IGNORE_PATTERNS` (auto-ingestion)
         - `HEALTH_PORT` (health server port)
         - `LOG_LEVEL` (logging control)
         - `QDRANT_PATH` → add note about embedded mode usage
       - Keep existing vars, remove any that no longer apply

    3. **Configuration — MCP Client Configuration (lines 419-469):**
       - Fix client config examples to use `kb_server.server` module path (not `server/server.py`)
       - OpenCode config: ensure it uses `kb_server.server` not `server/server.py`
       - Add a note about SSE URL format for remote deployments

    4. **Usage — Ingesting Documents (lines 475-499):**
       - Add new CLI commands shipped in Phase 8 and Phase 9:
         - `kb-rag status` (formerly `--status` flag — or keep the `python ingest/ingest.py --status` form if still valid)
         - `kb-rag check health`
         - Verify actual CLI: check if `kb-rag` CLI is installed and document its subcommands
       - Add `--vendor` and `--subsystem` ingest parameters if they exist (from Phase 11)
       - Update status examples to use current output format

    5. **Usage — Auto-Ingestion (lines 519-535):**
       - Remove "(FASE 13)" label — it's a standard feature now
       - Keep content accurate, reference `docs/AUTO_INGESTION.md`

    6. **Usage — Version Filtering (lines 537-557):**
       - Remove "(FASE 13)" label
       - Keep content accurate

    7. **Usage — Metadata Overrides (lines 558-575):**
       - Remove "(FASE 13)" label
       - Keep content accurate

    **Avoid:**
    - Do NOT change sections after line 576 (Health Checks onward — handled in Plan 13-02)
    - Do NOT change the Portuguese section
    - Do NOT break existing anchor links used in the TOC
  </action>
  <verify>
    <automated>
      # Verify new env vars added
      for var in EMBED_BATCH_SIZE WATCH_PATH WORKER_POOL_SIZE HEALTH_PORT LOG_LEVEL; do
        grep -q "$var" README.md && echo "OK: $var present" || echo "MISSING: $var"
      done
      # Verify stale labels removed
      grep -c "FASE 13" README.md | xargs -I{} sh -c 'test {} -eq 0 && echo "OK: No FASE 13 labels" || echo "WARNING: {} FASE 13 labels remain"'
      # Verify kb_server paths in config examples
      grep -c "kb_server.server" README.md || echo "MISSING: kb_server.server in client config"
    </automated>
  </verify>
  <done>README.md Installation, Configuration, and Usage sections fully updated with current env vars, CLI commands, and no stale FASE/NEW labels</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → reader | Documentation accuracy boundary — stale/misleading content causes user confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | README.md | mitigate | Do not expose internal `server/` paths, use canonical `kb_server/` throughout |
| T-13-02 | Tampering | Feature claims | mitigate | Verify each feature claim against actual codebase state before writing |

| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. `grep` checks from Task 1 verify pass (features, path corrections, stale label removal)
2. All internal anchor links (`#section-name`) in the TOC navigate to correct sections
3. No `server/` path references remain in the refreshed sections
4. No `FASE` labels or `(NEW)` badges remain
5. Version string mentions v1.3 milestone
</verification>

<success_criteria>
- README.md lines 1-576 reflect all shipped features through v1.3
- Test count shows 585 (not 491)
- Python 3.13 mentioned alongside 3.11
- `kb_server.server` replaces any `server.server` references
- Configuration section has all current env vars
- Stale FASE labels and (NEW) badges removed
- CLI commands updated: `kb-rag` entry point, `kb-rag check health`, `kb-rag status`
</success_criteria>

<output>
Create `.planning/phases/13-docs-sync-readme-languages/13-01-SUMMARY.md` when done
</output>
