---
phase: 13-docs-sync-readme-languages
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "README.md Health Checks section reflects current 5-component check architecture with cross-encoder lazy loading"
    - "README.md MCP Tools section documents vendor, subsystem, and version filter parameters"
    - "README.md Architecture diagram shows kb_server/ (not server/) and lazy cross-encoder"
    - "README.md Development section uses current tooling (flake8, black, mypy, isort) and current test count (585)"
    - "README.md Monitoring section matches 23 current Prometheus metrics"
    - "README.md Operations section references current backup/restore/update scripts"
    - "README.md Troubleshooting section links to docs/TROUBLESHOOTING.md without duplicating content"
    - "README.md License/Contributing sections are complete with correct placeholders filled in"
    - "No server/ paths, FASE labels, or (NEW) badges in any refreshed section"
    - "All inter-document links (docs/) are verified valid"
  artifacts:
    - path: "README.md"
      provides: "Refreshed advanced sections (Health Checks through Contributing)"
      min_lines: 1550
      contains: "kb_server, v1.3, vendor, subsystem, cross-encoder, flake8, 585"
  key_links:
    - from: "README.md"
      to: "docs/TROUBLESHOOTING.md"
      via: "link reference"
      pattern: "TROUBLESHOOTING"
    - from: "README.md"
      to: "docs/OPERATIONS.md"
      via: "link reference"
      pattern: "OPERATIONS"
    - from: "README.md"
      to: "docs/TESTING.md"
      via: "link reference"
      pattern: "TESTING"
---

<objective>
Refresh the bottom half of README.md (Health Checks through License/Contributing) — sections that detail operations, tools, architecture monitoring, and development workflows.

**Purpose:** Existing users and operators see accurate operational details, correct tool documentation, and current development workflows.

**Output:** Complete, polished `README.md` with all sections refreshed end-to-end.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-docs-sync-readme-languages/13-01-SUMMARY.md (only if plan 13-01 completed — provides final state after core section refresh)

# Key updates for this plan:
# - MCP tools now support vendor, subsystem, version filters (Phase 11)
# - Architecture: kb_server/ is canonical, cross-encoder is lazy-loaded (Phase 9)
# - Development: flake8 (not ruff), black line-length 79, isort black profile
# - Monitoring: 23 Prometheus metrics, health endpoint on :8000, structured JSON logging
# - Operations: kb-rag check health CLI, coverage enforcement, English CI audit gate
# - Test count: 585 (core), 3 (SSE handler), 51 (e2e) = 639 total
# - License: "MIT License" with standard MIT text (fill placeholder)
# - Code quality commands: coverage report, logging audit, English audit scripts exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Health Checks, Service Management, and MCP Tools sections</name>
  <files>README.md</files>
  <action>
    Read README.md lines 576-790. Modify:

    1. **Health Checks (lines 578-650):**
       - Verify all endpoint paths match current `kb_server/health_server.py` implementation
       - Add note about `kb-rag check health` CLI alternative (shipped in Phase 9)
       - Update component table to include reference to cross-encoder (loaded lazily, not checked at health probe)
       - Change `python3 -m server.health_server` to `kb-rag check health` or `python -m kb_server.health_server`
       - If any deprecated endpoints exist, remove them

    2. **Service Management (lines 652-743):**
       - Update service list to reflect current systemd units (check `scripts/kb-mcp.service` for actual content)
       - Remove or update references to `kb-rag-scheduler` if it's still a placeholder
       - Keep `kb-rag-watcher` as an existing service (not "NEW")
       - Verify auto-restart configuration values match actual systemd unit files

    3. **MCP Tools (lines 746-790):**
       - Add `vendor` parameter to `search_kb` (Phase 11: auto-classification)
       - Add `subsystem` parameter to `search_kb` (Phase 11)
       - Add `version` parameter — it IS shipped, remove "(NEW in FASE 13)" label
       - Update return fields to include `vendor`, `subsystem`, `version` (Phase 11)
       - Update parameter description for `filter_type` to include current supported types

    **Avoid:**
    - Do NOT change sections after line 790 (Architecture onward — handled in Task 2)
    - Do NOT introduce new (NEW) badges
    - Do NOT use FASE numbering anywhere
  </action>
  <verify>
    <automated>
      # Verify MCP tool params updated
      grep -c "vendor" README.md | xargs -I{} echo "vendor mentions: {}"
      grep -c "subsystem" README.md | xargs -I{} echo "subsystem mentions: {}"
      grep -c "(NEW in FASE)" README.md || echo "OK: No (NEW in FASE) labels"
      # Verify server path is kb_server
      grep -c "kb_server.health_server\|kb_server.server" README.md || echo "MISSING: kb_server paths"
      if grep -q "server.health_server" README.md; then echo "STALE: old health server path"; else echo "OK: No old health server path"; fi
    </automated>
  </verify>
  <done>README.md Health Checks, Service Management, and MCP Tools sections refreshed with current endpoints, tools, and parameters</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Architecture, Development, Documentation, Monitoring, Operations, Troubleshooting, License, and Contributing</name>
  <files>README.md</files>
  <action>
    Read README.md lines 790-1260. Modify:

    1. **Architecture (lines 792-814):**
       - Update ASCII diagram: `server/server.py` → `kb_server/server.py`
       - Update component list:
         - Remove FASE labels from all component descriptions
         - Add "Cross-encoder reranker (lazy-loaded)" under Classifier
         - Add "Auto-classification: vendor, subsystem, version inference"
         - Update component descriptions to reflect current state

    2. **Development (lines 816-858):**
       - Test run command: update `--cov=ingest --cov=server` → `--cov=kb_server --cov=ingest`
       - Code quality: current tools are `black`, `isort` (black profile), `flake8`, `mypy`
       - Add coverage command: `pytest --cov=kb_server --cov=ingest --cov-branch --cov-report=term-missing`
       - Add logging audit: `python scripts/logging-audit.py`
       - Add English audit: `python scripts/docstring-audit.py --check-inline`
       - Update test count references to current (585 core tests)
       - Note: pip-tools workflow is `pip-compile requirements.in && pip-sync requirements.txt`

    3. **Documentation (lines 860-884):**
       - Remove FASE completion report references (they're archived)
       - Keep docs/ links that still exist and are accurate
       - Add links to: OPERATIONS.md, ARCHITECTURE.md, INDEX.md, REFERENCE.md
       - Remove stale doc links if files no longer exist

    4. **Monitoring (lines 886-1022):**
       - Verify all 23 metrics still exist in current `observability/metrics.py`
       - Remove any metric that no longer exists, add new ones if any
       - Update Prometheus config reference paths
       - Keep Grafana dashboard reference
       - Update structured logging section for current JSON format

    5. **Operations (lines 1024-1211):**
       - Update batch processing env vars if they changed
       - Update worker pool config references
       - Add coverage enforcement info to maintenance section
       - Keep backup/restore/update content accurate

    6. **Troubleshooting (lines 1214-1248):**
       - Keep as a brief summary pointing to `docs/TROUBLESHOOTING.md`
       - Update quick fixes to use current commands
       - Ensure all commands reference `kb_server` not `server`

    7. **License (lines 1250-1252):**
       - Replace `[Your License Here]` with `MIT License` and standard MIT text
       - If there's a `LICENSE` file in the repo root, reference it

    8. **Contributing (lines 1254-1258):**
       - Keep existing content, verify CONTRIBUTING.md exists

    **Avoid:**
    - Do NOT modify the pt-BR section (lines 1262-1638)
    - Do NOT modify sections already handled in Plan 13-01 (verify by reading and keeping those changes)
    - Do NOT add FASE or NEW labels
  </action>
  <verify>
    <automated>
      # Verify key updates
      grep -c "kb_server" README.md | xargs -I{} echo "kb_server references: {}"
      grep -c "flake8" README.md | xargs -I{} echo "flake8 references: {}"
      grep -c "cov=kb_server" README.md | xargs -I{} echo "cov=kb_server: {}"
      grep -c "cross-encoder" README.md | xargs -I{} echo "cross-encoder: {}"
      grep -c "MIT License" README.md | xargs -I{} echo "MIT License: {}"
      grep -c "logging-audit" README.md | xargs -I{} echo "logging-audit: {}"
      grep -c "docstring-audit" README.md | xargs -I{} echo "docstring-audit: {}"
      # Verify no stale content
      if grep -q "cov=server" README.md; then echo "STALE: cov=server"; else echo "OK: No cov=server"; fi
      if grep -q "Your License Here" README.md; then echo "STALE: license placeholder"; else echo "OK: License filled"; fi
      # Verify all docs/ links resolve
      grep -oP 'docs/[a-zA-Z_]+\.md' README.md | sort -u | while read -r link; do
        test -f "$link" && echo "Exists: $link" || echo "MISSING: $link"
      done
    </automated>
  </verify>
  <done>README.md Architecture through Contributing sections refreshed with current tooling, metrics, and operational details</done>
</task>

<task type="auto">
  <name>Task 3: Final polish pass — links, anchor tags, version, and consistency</name>
  <files>README.md</files>
  <action>
    Perform a final end-to-end pass on the entire English portion of README.md:

    1. **Anchor link verification:** Every TOC entry `(#section-name)` must match an actual `<a name="section-name">` or `# section-name` heading anchor in the file. Fix any broken anchors.
    2. **Version string consistency:** Ensure all version references say "v1.3" (not v0.9.0, not v1.0). Do NOT change setup.py version — only the README text.
    3. **Link validation:** Every `docs/FILENAME.md` link must point to a file that exists in `docs/` directory. Remove or update dead links.
    4. **Consistency pass:**
       - All `server/` → `kb_server/` paths fixed
       - All `docker-compose` → `docker compose` (modern CLI)
       - No FASE numbering remains
       - No `(NEW)` badges remain
       - Language in pt-BR section separator is unchanged
    5. **Typos and formatting:**
       - Check for markdown formatting issues (unclosed code blocks, broken lists)
       - Ensure all bash code blocks have `bash` language tag
       - Ensure consistent spacing between sections (--- separators)
  </action>
  <verify>
    <automated>
      # Comprehensive validity checks
      echo "=== Anchor verification ==="
      # Check TOC anchors exist as headings
      grep -oP '(?<=\[)[^]]+(?=\])\(#[^)]+\)' README.md | while read -r entry; do
        anchor=$(echo "$entry" | grep -oP '(?<=\(#).+?(?=\))')
        heading=$(echo "$entry" | grep -oP '.+(?=\(#)')
        if grep -qi "$heading" README.md; then :; else echo "Possible missing heading: $heading (anchor: $anchor)"; fi
      done 2>/dev/null
      
      echo "=== Link validation ==="
      grep -oP 'docs/[a-zA-Z_\.]+\.md' README.md | sort -u | while read -r f; do
        test -f "$f" && echo "OK: $f" || echo "DEAD: $f"
      done
      
      echo "=== Version check ==="
      grep "v1.3" README.md | head -3
      if grep -q "v0\.9\|v1\.0\.0" README.md; then echo "STALE VERSION: found v0.9 or v1.0.0 ref"; else echo "OK: No stale versions"; fi
      
      echo "=== FASE check ==="
      grep -c "FASE" README.md | xargs -I{} echo "FASE mentions (should be 0): {}"
      
      echo "=== server/ path check ==="
      # Exclude the pt-BR section which references server/ in a translated context
      grep -n "server\." README.md | grep -v "kb_server\|pt-BR\|português\|# 🇧🇷" | head -5
    </automated>
  </verify>
  <done>README.md English sections are fully polished: all links valid, anchors match TOC, version consistent (v1.3), no stale references, formatting clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → operator | Operational commands must be accurate — wrong commands cause failures |
| README.md → developer | Tooling commands must match actual CI/CD configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | Health endpoint docs | mitigate | Verify endpoint paths against actual `kb_server/health_server.py` |
| T-13-04 | Tampering | Code quality commands | mitigate | Verify tool flags match `.flake8`, `pyproject.toml` config |
| T-13-05 | Tampering | Prometheus metrics | mitigate | Verify metric names against `observability/metrics.py` |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. All grep checks in tasks pass
2. No FASE, (NEW), server/ paths remain in English sections
3. All docs/ links resolve to existing files
4. `kb_server` path is used consistently
5. License placeholder replaced with MIT text
6. Test commands use correct module paths
</verification>

<success_criteria>
- All MCP tools documented with vendor, subsystem, version parameters
- Architecture diagram uses kb_server/ paths
- Development section uses flake8, black, isort, mypy with correct flags
- Monitoring section matches current 23 metrics
- License section has MIT text instead of placeholder
- All 20+ docs/FILENAME.md links verified valid
- No `server/` paths, FASE labels, or (NEW) badges remain in English sections
</success_criteria>

<output>
Create `.planning/phases/13-docs-sync-readme-languages/13-02-SUMMARY.md` when done
</output>
