---
phase: 13-docs-sync-readme-languages
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - README.pt-BR.md
  - README.es.md
autonomous: false
requirements: [DOCS-02]
must_haves:
  truths:
    - "READme.pt-BR.md structure matches English README.md: same sections in same order"
    - "READme.pt-BR.md contains full translations of all sections that were missing (Health Checks, Service Management, Production Deployment, Development, Monitoring, Operations)"
    - "READme.pt-BR.md Portuguese is natural, fluent, region-appropriate (pt-BR)"
    - "READme.es.md follows same structure as English README.md"
    - "READme.es.md Spanish is natural, fluent (universal Spanish, neutral register)"
    - "Both translations use kb_server/ paths, not server/"
    - "Both translations reference v1.3 milestone"
    - "Both translations have no FASE labels or (NEW) badges"
    - "Both translations have complete License and Contributing sections"
    - "README.md language-selector links at the top link to both translations"
  artifacts:
    - path: "README.pt-BR.md"
      provides: "Synced Portuguese translation matching English structure"
      min_lines: 1550
      contains: "kb_server, v1.3, Health Checks, Service Management, Production Deployment, Monitoramento"
    - path: "README.es.md"
      provides: "Spanish translation matching English structure"
      min_lines: 1500
      contains: "kb_server, v1.3, Health Checks, Service Management, Production Deployment, Monitoreo"
  key_links:
    - from: "README.pt-BR.md"
      to: "docs/TROUBLESHOOTING.md"
      via: "link reference"
      pattern: "TROUBLESHOOTING"
    - from: "README.es.md"
      to: "docs/TROUBLESHOOTING.md"
      via: "link reference"
      pattern: "TROUBLESHOOTING"
    - from: "README.md"
      to: "README.pt-BR.md"
      via: "language selector link"
      pattern: "README.pt-BR"
    - from: "README.md"
      to: "README.es.md"
      via: "language selector link"
      pattern: "README.es"
---

<objective>
Sync README.pt-BR.md structure with the refreshed English README.md, translate all missing sections, and create a new README.es.md Spanish translation following the same structure.

**Purpose:** Portuguese readers have a complete, up-to-date translation matching the English README's depth and structure. Spanish-speaking users have a native-language entry point.

**Output:** Updated `README.pt-BR.md` and new `README.es.md`.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-docs-sync-readme-languages/13-02-SUMMARY.md (provides final refreshed English README structure)

# Current state of README.pt-BR.md (625 lines):
# Has these sections: About, Quick Start (2 options), Installation, MCP Client Config, Basic Usage, MCP Tools, Architecture, Metrics & Monitoring, Documentation, Troubleshooting, License, Contributing
#
# MISSING from pt-BR (present in English README):
# - Production Deployment (automated install, systemd, backup/restore/update)
# - Health Checks (endpoints, component checks, CLI alternative)
# - Service Management (systemd commands, individual services, logs)
# - Development (test commands, code quality, adding deps)
# - Monitoring (Prometheus metrics, alerting rules, Grafana, structured logging, log rotation)
# - Operations (backup/restore/update details, maintenance, performance tuning)
# - Configuration section is shorter than English
# - Usage section doesn't have Auto-Ingestion, Version Filtering, Metadata Overrides sub-sections
# - Feature list is shorter (missing: SSE, lazy reranker, Helm, CI/CD, auto-classification, etc.)

# Structure to match (English README after Plans 13-01 and 13-02):
# 1. Header + Features + Language selector
# 2. TOC
# 3. Quick Start (3 options + connect + verify)
# 4. Production Deployment (install, services, health checks, backup/restore, update, uninstall, resources, security, checklist)
# 5. Installation
# 6. Configuration (env vars + MCP client config)
# 7. Usage (Ingest, Dir Structure, Auto-Ingestion, Version Filtering, Metadata Overrides)
# 8. Health Checks (endpoints, component checks, scripts, manual)
# 9. Service Management (basic commands, individual services, logs, status, auto-restart)
# 10. MCP Tools (search_kb, list_documents, get_chunk, kb_stats)
# 11. Architecture
# 12. Development (requirements, running tests, code quality, adding deps)
# 13. Documentation
# 14. Monitoring (Prometheus metrics, config, alerting rules, Grafana, structured logging, log rotation)
# 15. Operations (backup/restore, updates, maintenance, performance tuning, troubleshooting)
# 16. Troubleshooting (brief)
# 17. License
# 18. Contributing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure and expand README.pt-BR.md to match English structure, translate new sections</name>
  <files>README.pt-BR.md</files>
  <action>
    Read the final English README.md (after Plans 13-01 and 13-02) to get the full structure. Then rewrite README.pt-BR.md:

    1. **Header + Language selector:**
       - Update language selector to include `[English](#english) | [Português (Brasil)](#português-brasil) | [Español](#español)`
       - Add `<a name="português-brasil"></a>` anchor
       - Update feature list to match English: add SSE, Python 3.13, Helm, CI/CD, lazy reranker, auto-classification, English sweep — all in natural Portuguese
       - Remove "(NOVO)" badges — same logic as English refresh
       - Remove FASE references

    2. **Restructure all sections** to match English README.md section order exactly:
       - Quick Start → 3 options (not 2) + Connect AI Assistant + Verify
       - **NEW: Production Deployment** — Full translation of automated install, systemd services, backup/restore, update, uninstall, resources table, security features, production checklist
       - Installation → update for Python 3.13, modern docker compose
       - Configuration → expand to full env vars table matching English
       - Usage → add Auto-Ingestion (Ingestão Automática), Version Filtering (Filtragem por Versão), Metadata Overrides (Sobrescrita de Metadados)
       - **NEW: Health Checks** — endpoints, component checks, scripts, manual
       - **NEW: Service Management** — systemd commands, individual services, logs, auto-restart
       - MCP Tools → add vendor, subsystem, version parameters
       - Architecture → update diagram to use kb_server/, add lazy reranker, auto-classification
       - **NEW: Development** — test commands with kb_server/, code quality, adding deps
       - Documentation → update docs/ links, remove FASE completion references
       - **NEW: Monitoring** — Prometheus metrics, alerting rules, Grafana, structured logging, log rotation
       - **NEW: Operations** — backup/restore details, updates, maintenance, performance tuning
       - Troubleshooting → brief summary (linking to docs/TROUBLESHOOTING.md)
       - License → "Licença MIT" with standard MIT text in Portuguese
       - Contributing → "Contribuições são bem-vindas! Leia CONTRIBUTING.md primeiro."

    **Translation quality rules:**
    - Use natural Brazilian Portuguese (pt-BR), not machine-translate-ese
    - Technical terms: keep English terms in backticks (e.g., `kb_server`, `kb-rag`, `Qdrant`)
    - Translate prose and descriptions, keep commands and code blocks in English
    - Keep all `docs/FILENAME.md` links pointing to the same English docs (they serve both audiences)
    - Use `você` (formal-you), not `tu` (informal)
    - Consistent with existing pt-BR translation style (check current README.pt-BR.md for established patterns)

    **Avoid:**
    - Do NOT change the English sections in README.md
    - Do NOT translate file paths, command names, env var names, or code blocks
    - Do NOT add FASE or (NOVO) labels
    - Do NOT use `server/` paths — use `kb_server/`
  </action>
  <verify>
    <automated>
      # Verify structure matches English
      echo "=== Section count check ==="
      grep "^### " README.pt-BR.md | wc -l | xargs -I{} echo "pt-BR sections: {}"
      grep "^### " README.md | wc -l | xargs -I{} echo "EN sections: {}"
      
      echo "=== Key new sections check ==="
      grep -c "Produção\|Deploy.*Produção\|Implanta" README.pt-BR.md | xargs -I{} echo "Production deployment: {}"
      grep -c "Health Checks\|Verificação.*Saúde\|Saúde" README.pt-BR.md | xargs -I{} echo "Health checks: {}"
      grep -c "Service Management\|Gerenciamento.*Serviço\|Gestão.*Serviço" README.pt-BR.md | xargs -I{} echo "Service management: {}"
      grep -c "Desenvolvimento" README.pt-BR.md | xargs -I{} echo "Development: {}"
      grep -c "Monitoramento\|Métricas Prometheus" README.pt-BR.md | xargs -I{} echo "Monitoring: {}"
      grep -c "Operações\|Manutenção" README.pt-BR.md | xargs -I{} echo "Operations: {}"
      
      echo "=== Stale content check ==="
      grep -c "NOVO" README.pt-BR.md | xargs -I{} echo "(NOVO) badges: {}"
      if grep -q "FASE" README.pt-BR.md; then echo "WARNING: FASE labels remain"; else echo "OK: No FASE labels"; fi
      if grep -q "server.server\|server.health_server" README.pt-BR.md; then echo "WARNING: server/ paths remain"; else echo "OK: No server/ paths"; fi
      
      echo "=== Path check ==="
      grep -c "kb_server" README.pt-BR.md | xargs -I{} echo "kb_server references: {}"
      
      echo "=== License check ==="
      grep -c "MIT\|Licença" README.pt-BR.md | xargs -I{} echo "License mentions: {}"
    </automated>
  </verify>
  <done>README.pt-BR.md fully restructured and expanded to match English README with all sections translated to natural pt-BR</done>
</task>

<task type="auto">
  <name>Task 2: Create README.es.md — Spanish translation matching English README structure</name>
  <files>README.es.md</files>
  <action>
    Create new file `README.es.md` following the exact same structure as the English `README.md`:

    1. **Header + Language selector:**
       ```
       # Servidor MCP KB-RAG
       
       **[English](#english) | [Português (Brasil)](#português-brasil) | [Español](#español)**
       
       ---
       
       <a name="español"></a>
       ## 🇪🇸 Español
       ```
       - Feature list translated to Spanish
       - Same icons/emojis as English
       - No "(NUEVO)" badges — same approach as English refresh

    2. **Full translation of EVERY section** in the same order as English README.md:
       - TOC (índice)
       - Quick Start (Inicio Rápido) — 3 options + Connect + Verify
       - Production Deployment (Despliegue en Producción)
       - Installation (Instalación)
       - Configuration (Configuración)
       - Usage (Uso) — with Auto-Ingestion, Version Filtering, Metadata Overrides
       - Health Checks (Verificaciones de Salud)
       - Service Management (Gestión de Servicios)
       - MCP Tools (Herramientas MCP)
       - Architecture (Arquitectura)
       - Development (Desarrollo)
       - Documentation (Documentación)
       - Monitoring (Monitoreo)
       - Operations (Operaciones)
       - Troubleshooting (Solución de Problemas)
       - License (Licencia) — "Licencia MIT" with standard MIT text in Spanish
       - Contributing (Contribuciones)

    **Translation quality rules:**
    - Use neutral universal Spanish (no regionalisms from any one country)
    - Use `usted` (formal-you) consistently
    - Technical terms in English in backticks
    - Keep ALL code blocks, commands, env vars, file paths in English (untouched)
    - Keep ALL `docs/FILENAME.md` links pointing to the same English docs
    - Translate only prose, descriptions, labels, section headers
    - Use approved Spanish tech terms:
      - "despliegue" (not "deploy")
      - "ingesta" (for ingestion) — or "incorporación"
      - "búsqueda semántica" (semantic search)
      - "almacén de vectores" (vector store)
      - "fragmento" or "fragmento" (chunk)
      - "clasificación automática" (auto-classification)
      - "integración continua/despliegue continuo" (CI/CD)
      - "métrica" (metric)

    **Avoid:**
    - Do NOT translate code blocks, commands, env vars, file paths
    - Do NOT modify English README.md or README.pt-BR.md
    - Do NOT use `server/` paths — use `kb_server/`
    - Do NOT add FASE or (NUEVO) labels
    - Do NOT add emojis beyond what exists in the English README
  </action>
  <verify>
    <automated>
      # File creation check
      test -f README.es.md && echo "OK: File created" || echo "MISSING: File not created"
      
      # Structure check
      echo "=== Sections ==="
      grep "^### " README.es.md | wc -l | xargs -I{} echo "es sections: {}"
      
      echo "=== Key sections present ==="
      grep -c "Inicio Rápido\|Rápido\|rápido" README.es.md | xargs -I{} echo "Quick Start: {}"
      grep -c "Producción\|Despliegue" README.es.md | xargs -I{} echo "Production: {}"
      grep -c "Instalación" README.es.md | xargs -I{} echo "Installation: {}"
      grep -c "Configuración" README.es.md | xargs -I{} echo "Configuration: {}"
      grep -c "Uso\|Ingesta" README.es.md | xargs -I{} echo "Usage: {}"
      grep -c "Verificación.*Salud\|Salud" README.es.md | xargs -I{} echo "Health: {}"
      grep -c "Servicio\|Gestión" README.es.md | xargs -I{} echo "Service management: {}"
      grep -c "Herramientas MCP\|MCP" README.es.md | xargs -I{} echo "MCP Tools: {}"
      grep -c "Arquitectura" README.es.md | xargs -I{} echo "Architecture: {}"
      grep -c "Desarrollo" README.es.md | xargs -I{} echo "Development: {}"
      grep -c "Monitoreo\|Métricas" README.es.md | xargs -I{} echo "Monitoring: {}"
      grep -c "Operaciones" README.es.md | xargs -I{} echo "Operations: {}"
      grep -c "Problemas\|Solución" README.es.md | xargs -I{} echo "Troubleshooting: {}"
      grep -c "Licencia" README.es.md | xargs -I{} echo "License: {}"
      grep -c "Contribuciones" README.es.md | xargs -I{} echo "Contributing: {}"
      
      echo "=== Quality checks ==="
      if grep -q "FASE\|NUEVO\|NOVO" README.es.md; then echo "WARNING: Stale labels found"; else echo "OK: No stale labels"; fi
      grep -c "kb_server\|kb-rag" README.es.md | xargs -I{} echo "kb_server/kb-rag refs: {}"
      grep -c "v1.3" README.es.md | xargs -I{} echo "v1.3 references: {}"
      if grep -q "server.server\|server.health_server" README.es.md; then echo "WARNING: server/ paths"; else echo "OK: No server/ paths"; fi
      
      echo "=== Line count ==="
      wc -l README.es.md
    </automated>
  </verify>
  <done>README.es.md created with complete Spanish translation matching English README structure and content</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify translations quality and completeness</name>
  <what-built>
    - Restructured and expanded README.pt-BR.md (~1500+ lines)
    - New README.es.md (~1500+ lines)
    - Updated language selector links in README.md
  </what-built>
  <how-to-verify>
    1. Open README.md and click the language selector links — verify all three work (EN, PT-BR, ES)
    2. Open README.pt-BR.md:
       - Scroll through and verify all sections from the English README are present in Portuguese
       - Check that Portuguese reads naturally (not machine-translated)
       - Verify code blocks and commands are unchanged from English
    3. Open README.es.md:
       - Verify all sections from the English README are present in Spanish  
       - Check Spanish reads naturally (neutral register)
       - Verify code blocks and commands are unchanged from English
    4. Spot-check technical accuracy:
       - Search for `kb_server` in both translations — should appear in code blocks and command examples
       - Search for `server.` in both translations — should NOT appear
       - Search for `FASE` in both translations — should NOT appear
    5. Verify line counts are reasonable (~1500+ lines each)
  </how-to-verify>
  <resume-signal>Type "approved" if translations are accurate and complete, or describe specific issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation → reader | Mistranslated technical content causes operational errors |
| Translation → reader | Culturally inappropriate language causes confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | pt-BR translation | mitigate | Keep code blocks/commands in English; verify all EN section headers have PT equivalents |
| T-13-07 | Tampering | es translation | mitigate | Keep code blocks/commands in English; verify all EN section headers have ES equivalents |
| T-13-08 | Information Disclosure | Both translations | mitigate | Do not translate internal paths or secrets |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. Automated grep checks pass for both translations
2. Human verification confirms natural language quality
3. All three language-selector links work from README.md
4. Code blocks/commands identical to English across both translations
5. No stale labels (FASE, (NOVO), (NUEVO)) or server/ paths in either translation
</verification>

<success_criteria>
- README.pt-BR.md: ~1500+ lines, all English sections present in Portuguese, natural pt-BR prose
- README.es.md: ~1500+ lines, all English sections present in Spanish, neutral Spanish prose
- README.md language selector links to all three translations
- Code blocks and commands unchanged from English in both translations
- License, Contributing sections fully translated
- No server/ paths, FASE labels, or (NEW) badges in either translation
</success_criteria>

<output>
Create `.planning/phases/13-docs-sync-readme-languages/13-03-SUMMARY.md` when done
</output>
