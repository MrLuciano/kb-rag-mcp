---
context: research
phase: pre-milestone-planning
task: 0
total_tasks: 0
status: paused
last_updated: 2026-06-13T02:35:59.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Scope explosion — the user added 5+ major features after the milestone was planned. The full scope now includes: Streamable HTTP, Auth/User CRUD API, Admin SPA, Grafana embed, Admin config settings page, Monitor lights bar, Ingestion tab (direct + scheduled + monitor), RAGAS tab (golden set upload), Browser cleanup. This is too large for a single spec — must be decomposed into sub-projects.
- [ ] CONSTRAINT: Brainstorming HARD-GATE — do NOT write any code, scaffold any project, or take any implementation action until a design has been presented and the user has approved it. The ONLY skill to invoke after brainstorming is writing-plans.
- [ ] CONSTRAINT: Visual companion accepted to be continued — browser mockups available for UI design. Use the visual companion for layout questions, mockups, and architecture diagrams.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Scope creep in single session | Adding features mid-session without formal decomposition | blocking | Must decompose scope into sub-projects before any design work. Architecture-first, not feature-first. |
| Premature execution | Writing code before design approval | blocking | Brainstorming HARD-GATE blocks any implementation until design is approved by user |
| Visual companion enabled | Browser mockups available for UI design work | advisory | Use visual companion for layout mockups, architecture diagrams, and design options |

<current_state>
We are in pre-milestone planning for v0.1.5 (Streamable HTTP & Management Platform). The milestone was switched from v0.1.4, planning files committed (RESEARCH.md, REQUIREMENTS.md, ROADMAP.md updated). Research is complete (5 parallel researchers + 4 codebase explorers). The brainstorming skill was loaded and step 1 (explore context) is complete. Step 2 (offer visual companion) was done — user declined. We are at step 3 (asking clarifying questions), which was interrupted when the user requested a pause. Visual companion was accepted to be continued — use browser mockups when designing UI layouts.

The full requested scope includes:
1. Phase 28 (reopened): MCP Streamable HTTP Transport
2. Phase 28b: Auth & User Management REST API (CRUD users/keys, roles, GDPR compliance)
3. Phase 28c: Admin SPA Panel (Alpine.js+HTMX tabbed UI at /admin/)
4. Phase 38: Grafana Dashboard Embedding (iframe in monitoring tab)
5. NEW: Admin config settings page — change env vars from UI, hot-reload, fallback
6. NEW: Monitor lights bar — component health status indicators
7. NEW: Ingestion tab — direct ingest + schedule batch + monitor progress
8. NEW: RAGAS tab — upload/change golden set, evaluate performance
9. NEW: Browser cleanup — remove failed/ingested data from existing browser section
10. Phase 39: Observability Backlog (health/readiness, request ID, percentile metrics)
11. Phase 40: Configuration Backlog (hot-reload, config API)
12. Phase 41: Provider Alias

Items 5-9 were added by the user after the initial milestone planning was complete.
</current_state>

<completed_work>

Completed:
- v0.1.4 milestone finalized, tagged, archived
- v0.1.5 milestone switched (PROJECT.md, STATE.md, ROADMAP.md updated)
- v0.1.4 phase directories archived to .planning/archive/v0.1.4/
- 5 parallel researchers completed (Grafana embed, auth patterns, SPA architecture, MCP Streamable HTTP, GDPR compliance)
- 4 parallel codebase explorers completed (UI structure, ingestion pipeline, RAGAS evaluation, health/metrics)
- RESEARCH.md written with all findings
- REQUIREMENTS.md written with all phase requirements
- ROADMAP.md updated with v0.1.5 phases
- Brainstorming skill loaded
- Visual companion offered and accepted to be continued
- All commits: fc54436 (milestone switch), 3576821 (requirements/research/roadmap)
</completed_work>

<remaining_work>

1. Decompose scope into sub-projects — the scope is too large for a single spec
2. Brainstorming: ask clarifying questions about new features (one at a time)
3. Propose 2-3 architectural approaches for each sub-project
4. Present design sections, get user approval per section
5. Write design spec to `docs/superpowers/specs/YYYY-MM-DD-admin-platform-design.md`
6. Spec self-review (check for placeholders, contradictions, ambiguity)
7. User reviews written spec
8. Invoke writing-plans skill to create implementation plans
9. Execute plans for each phase (Phase 28, 28b, 28c, 38, 39, 40, 41 + new features)
</remaining_work>

<decisions_made>

- v0.1.4 finalized and tagged as shipped — all 15 phases (23-37) complete
- Phase 28 (Streamable HTTP) reopened — documented as complete but zero code exists
- SPA: HTMX + Alpine.js (no build step, CDN scripts) — consistent with existing Jinja2+HTMX setup
- Auth: passwordless API key + JWT session cookie for browser — builds on existing AuthRegistry
- Grafana embed: anonymous Viewer + iframe with CSP frame-src — simplest for internal tool
- GDPR: data minimization (username+role+key hash only), erasure state machine, Article 15/20 endpoints
</decisions_made>

<blockers>

- Scope too large for single spec — must decompose into sub-projects before design work
- No clarifying questions answered yet — user requested pause before answering
- Use visual companion for UI mockups when resuming
</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` — current milestone goals and scope
2. `.planning/REQUIREMENTS.md` — all v0.1.5 phase requirements
3. `.planning/RESEARCH.md` — findings from 5 parallel researchers
4. `.planning/STATE.md` — current project state
5. `.planning/ROADMAP.md` — phase breakdown and backlog
6. `.planning/HANDOFF.json` — machine-readable state

## Critical Anti-Patterns (do NOT repeat these)

- **SCOPE EXPLOSION**: User added features mid-session without formal re-scoping. Future sessions must decompose scope into sub-projects before any design or implementation.
- **NO DESIGN BEFORE CODE**: Brainstorming HARD-GATE requires user-approved design before ANY code. Do not write implementation code.
- **VISUAL COMPANION ENABLED**: User accepted visual companion to be continued. Use browser mockups for UI layout questions, architecture diagrams, and design options.

## Infrastructure State

- Git branch: master
- Last commit: 3576821 (docs: add v0.1.5 requirements, research, roadmap)
- Uncommitted: `.planning/phases/*` deletions (unstaged, from archive move in fc54436)
- No running servers or background processes
- `.venv/` virtual environment present at project root
- Qdrant: Docker compose available but not running (needs `docker compose up -d` for execution)

## Pre-Execution Critique Required

**YES** — this session pauses before any design has been approved.

- Design artifact: NONE — still pre-design (brainstorming step 3)
- Critique focus: Does the current REQUIRMENTS.md capture all user-requested features? Is the scope decomposition correct?
- Gate: Do NOT begin execution until brainstorming completes (design approved → spec written → plans created)

<context>
This session started with the new-milestone workflow for v0.1.5. The initial scope was: Phase 28 (Streamable HTTP), SPA-01 (Management Panel), OBS-01/02, CONF-01/02, METRICS-01, PROV-01. The user then significantly expanded scope: admin config settings page, monitor lights bar, ingestion tab (direct+scheduled+monitor), RAGAS tab (golden set upload), browser cleanup (remove data). The 5 parallel researchers produced solid findings. 4 codebase explorers mapped the existing system comprehensively. Brainstorming started but only reached step 3 (ask clarifying questions) before the pause command. The mental model was: decompose scope into sub-projects (too large for one spec), then ask clarifying questions about the first sub-project, get design approval, write spec, invoke writing-plans, then execute each phase. The user seemed interested in moving fast but wanted proper research and planning before execution.
</context>

<next_action>
Start with: Load the brainstorming skill context, then ask the first clarifying question about how to decompose the scope into sub-projects. The full feature list from the user is:
1. Admin config settings page (change env vars from UI)
2. Monitor lights bar (component health)
3. Ingestion tab (direct + schedule + monitor)
4. RAGAS tab (golden set upload + evaluation)
5. Browser cleanup (remove ingested/failed data)
6. Plus all original: Streamable HTTP, auth/users, admin panel, Grafana embed, personal areas, GDPR

Ask: "The scope has 5 major new features on top of the original plan. Should I decompose into sub-projects first, or would you like to start with a specific feature as the highest priority?"
</next_action>
