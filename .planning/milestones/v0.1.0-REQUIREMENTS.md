# Requirements Archive: kb-rag-mcp v0.1.0

**Milestone:** v0.1.0 — Release-Readiness
**Archived:** 2026-05-19
**Status:** ✅ ALL 15 v1 REQUIREMENTS SHIPPED

---

# Requirements: kb-rag-mcp

**Defined:** 2026-05-19
**Core Value:** AI assistants stop hallucinating about closed-source products — every answer is grounded in the team's actual documentation.

## v1 Requirements — All Complete

### Codebase Cleanup

- [x] **CLEAN-01**: Delete `server/` legacy module entirely; all imports and tests point to `kb_server/` only
  - *Outcome: Validated — `server/` deleted; zero import references remain*
- [x] **CLEAN-02**: Fix hybrid search sparse path — `HybridSearcher` performs real dense+BM25 RRF fusion, not dense-only fallback
  - *Outcome: Validated — fastembed BM25 model loaded; RRF fusion unit test passes*
- [x] **CLEAN-03**: Single `bootstrap_env()` function in `config/` replaces 6+ copy-pasted `load_dotenv` blocks across entry points
  - *Outcome: Validated — single canonical `bootstrap_env()` in `config/`*
- [x] **CLEAN-04**: Remove `ingest/registry.py` (v1 registry); only `ingest/core/metadata.py` remains
  - *Outcome: Validated — `ingest/registry.py` deleted; `IngestRegistry` lives in `ingest/core/metadata.py`*
- [x] **CLEAN-05**: Batch ingest computes real SHA-256 checksums per file (not hardcoded `"batch"` placeholder)
  - *Outcome: Validated — SHA-256 deduplication test proves identical files merge, different files don't*

### Data Integrity & Security

- [x] **DATA-01**: File watcher `on_deleted` handler removes vectors from Qdrant when source file is deleted
  - *Outcome: Validated — integration test covers deletion path*
- [x] **DATA-02**: `.env` and `config/.env.*` files removed from git tracking; only `config/.env.template` is committed
  - *Outcome: Validated — `git ls-files | grep .env` returns only template*
- [x] **DATA-03**: `CONTRIBUTING.md` documents git history cleanup steps for teams that committed secrets (using `git-filter-repo`)
  - *Outcome: Validated — `CONTRIBUTING.md` written with step-by-step guide*

### Testing

- [x] **TEST-01**: `kb_server/` package achieves ≥80% branch coverage
  - *Outcome: Validated — 88% branch coverage achieved (target was 80%)*
- [x] **TEST-02**: End-to-end integration tests cover full path: ingest a document → query via `search_kb` MCP tool → verify result
  - *Outcome: Validated — `test_search_integration.py` covers this path*
- [x] **TEST-03**: Integration tests cover multi-collection routing: route to correct collection, fallback on missing collection
  - *Outcome: Validated — `test_collection_routing_integration.py` covers both cases*
- [x] **TEST-04**: GitHub Actions CI pipeline runs full test suite on every push/PR to `master`
  - *Outcome: Validated — `.github/workflows/ci.yml` created*

### Deployment

- [x] **DEPL-01**: Production `Dockerfile` with multi-stage build producing a slim image
  - *Outcome: Validated — two-stage build; runtime uses python:3.11-slim + libgomp1 + curl*
- [x] **DEPL-02**: Quick-start script (`scripts/quickstart.sh`): clone → configure `.env` → start services → verify health
  - *Outcome: Validated — 126-line script covers full setup flow*
- [x] **DEPL-03**: `README.md` end-to-end getting-started guide: install, configure, ingest docs, connect AI tool
  - *Outcome: Validated — README Quick Start rewritten with 3 options + SSE/stdio client config*

## v2 Requirements (Deferred)

### Distribution
- **DIST-01**: Docker Compose with Qdrant + kb-rag-mcp + optional Redis (full stack one-command)
- **DIST-02**: Published Docker image on Docker Hub or GHCR
- **DIST-03**: PyPI package for `kb_server` library

### Features
- **FEAT-01**: Web UI for document browsing and search
- **FEAT-02**: OAuth / SSO support for team sharing
- **FEAT-03**: Streaming ingest from external HTTP sources

## Traceability — Final Status

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| CLEAN-01 | Phase 1 | Complete |
| CLEAN-02 | Phase 1 | Complete |
| CLEAN-03 | Phase 1 | Complete |
| CLEAN-04 | Phase 1 | Complete |
| CLEAN-05 | Phase 1 | Complete |
| DATA-01 | Phase 2 | Complete |
| DATA-02 | Phase 2 | Complete |
| DATA-03 | Phase 2 | Complete |
| TEST-01 | Phase 3 | Complete |
| TEST-02 | Phase 3 | Complete |
| TEST-03 | Phase 3 | Complete |
| TEST-04 | Phase 3 | Complete |
| DEPL-01 | Phase 4 | Complete |
| DEPL-02 | Phase 4 | Complete |
| DEPL-03 | Phase 4 | Complete |

**Final coverage: 15/15 v1 requirements shipped.**

---
*Archived: 2026-05-19 at v0.1.0 milestone close*
