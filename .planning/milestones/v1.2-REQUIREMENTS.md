# Requirements Archive: v1.2 Tech Debt & Classification

**Archived:** 2026-05-27
**Milestone:** v1.2 Tech Debt & Classification
**Status:** ✅ All 9 requirements met

---

## Operational Debt

- [x] **DEBT-01**: Cross-encoder model loads lazily on first `predict()` call, not at import time — saves ~500MB memory and ~10s startup latency
- [x] **DEBT-02**: Helm chart is validated with `helm lint` in CI pipeline — catches structural errors before deployment
- [x] **DEBT-03**: MagicMock pollution from `qdrant_client` sys.modules stubs is resolved across the test suite — enum values compare correctly without `getattr(x, 'value', x)` workaround
- [x] **DEBT-04**: Server startup performs pre-flight health check — warns if Qdrant or LM Studio are unreachable before accepting queries
- [x] **DEBT-05**: Logging coverage is enforced via CI gate (`--fail-under` threshold) — prevents regression below current baseline
- [x] **DEBT-06**: LM Studio embedding dependency is documented with recommended fallback/startup options in operations guide

## Auto-Classification

- [x] **CLASSIFY-01**: Documents are auto-classified with Vendor, Product, Subsystem, and Version attributes — inferred from directory hierarchy and filename patterns, extending existing OTCS product detection
- [x] **CLASSIFY-02**: Classification gaps are filled by extracting metadata (title, subject, author) and first-page content from PDF/DOCX files — no LLM dependency
- [x] **CLASSIFY-03**: Extended classification is backward-compatible with existing OTCS auto-tagging — existing `infer_product()` and `infer_doc_type()` signatures unchanged

---

## Traceability

| REQ-ID | Phase | Plan | Status | Outcome |
|--------|-------|------|--------|---------|
| DEBT-01 | Phase 9 | 09-01 | ✅ Complete | Cross-encoder lazy loading implemented with 4 regression tests |
| DEBT-04 | Phase 9 | 09-02 | ✅ Complete | Pre-flight health checks + `kb-ingest check health` CLI command |
| DEBT-06 | Phase 9 | 09-03 | ✅ Complete | LM Studio docs added to OPERATIONS.md (4 backends documented) |
| DEBT-02 | Phase 10 | 10-01 | ✅ Complete | `helm lint --strict` job added to CI pipeline |
| DEBT-03 | Phase 10 | 10-02 | ✅ Complete | Real qdrant_client imports in 3 test files, MagicMock stubs removed |
| DEBT-05 | Phase 10 | 10-03 | ✅ Complete | `--fail-under` flag + CI enforcement for logging coverage (40% threshold) |
| CLASSIFY-01 | Phase 11 | 11-01 | ✅ Complete | Vendor/Subsystem classification + Phase 11.1 search integration |
| CLASSIFY-02 | Phase 11 | 11-02 | ✅ Complete | Metadata extraction from PDF/DOCX + enrich_classification() |
| CLASSIFY-03 | Phase 11 | 11-01+02 | ✅ Complete | Backward compatible — existing signatures unchanged, 585 tests pass |

## Notes

- CLASSIFY-01 required Phase 11.1 (decimal phase) for full completion — vendor/subsystem fields were stored but not accessible in search results until Phase 11.1 remediation
- Phase 11.1 was discovered via milestone audit workflow (`/gsd-audit-milestone`) which found the critical integration gap
- All 9 requirements shipped with test verification (585 passing tests, 90%+ branch coverage)
