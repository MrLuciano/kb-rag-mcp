# Milestone v1.1: Quality & Operational Excellence

**Status:** ✅ SHIPPED 2026-05-23
**Phases:** 5-8
**Total Plans:** 10 (2+3+2+3)

## Overview

Second milestone focused on operational maturity and code quality. Established 90% branch coverage enforcement, full test isolation from external services, Python 3.13 compatibility, OTCS product auto-tagging, and English-only codebase with comprehensive docstrings.

## Phases

### Phase 5: SSE Stability & Python 3.13 Compatibility

**Goal:** Ensure the MCP SSE server works correctly on starlette 1.0.0 and Python 3.13, with regression tests that prevent the NoneType crash from recurring.

**Milestone:** v1.1
**Requirements:** SSE-01, SSE-02, COMPAT-01, COMPAT-02

**Success criteria:**

1. `GET /sse` followed by client disconnect produces no `TypeError: NoneType` crash in server logs
2. `POST /messages/` (with trailing slash) returns `202 Accepted` with no redirect chain
3. Full test suite passes on Python 3.11 and Python 3.13 in CI matrix
4. No `DeprecationWarning` from Python 3.13 in the test run

**Plans:** 2 plans

Plans:

- [x] 05-01-PLAN.md — SSE regression tests (unit + integration), starlette version pin
- [x] 05-02-PLAN.md — CI matrix (3.11, 3.12, 3.13), dependency compatibility audit

**Completed:** 2026-05-21

**Delivered:**
- 3 new SSE handler regression tests (NoneType crash, disconnect handling, trailing-slash POST)
- Starlette pinned to `>=1.0.0` in requirements.in
- CI matrix testing across Python 3.11, 3.12, 3.13
- SSE tests run in separate pytest process (stub conflict isolation)
- 495 tests passing, 5 skipped, 0 failures

---

### Phase 6: Test Coverage & Isolation

**Goal:** Every Python module has a unit test file; all external dependencies (Qdrant, LM Studio, Redis) are mocked so tests run in any environment without infrastructure.

**Milestone:** v1.1
**Requirements:** TEST-01, TEST-02, TEST-03

**Success criteria:**

1. `pytest -m "not integration"` completes successfully with no Qdrant, LM Studio, or Redis running
2. Every module under `kb_server/` and `ingest/` has at least one `tests/unit/test_<module>.py`
3. Integration tests are tagged `@pytest.mark.integration` and excluded from unit run
4. Test count increases by ≥50 new unit tests vs v1.0 baseline (491)

**Plans:** 3 plans

Plans:

- [x] 06-01-PLAN.md — Mock infrastructure: conftest fixtures + registered pytest markers
- [x] 06-02-PLAN.md — kb_server test classifier + integration tagging audit
- [x] 06-03-PLAN.md — ingest integration tagging + full isolation verification

**Wave Dependencies:**

**Wave 2** *(blocked on Wave 1 completion)*

**Cross-cutting constraints:**

- Every Python module has a dedicated unit test file (TEST-01)
- `pytest -m "not integration"` requires no Qdrant, LM Studio, or Redis (TEST-02)
- All integration tests are tagged `@pytest.mark.integration` (TEST-03)

**Completed:** 2026-05-22

**Delivered:**
- 3 session-scoped mock fixtures in tests/conftest.py (Qdrant, embed client, Redis)
- `mock_qdrant_client` is autouse; `mock_embed_client` and `mock_redis_cache` are opt-in
- 3 custom pytest markers registered: `integration`, `PHASE12`, `cli`
- 26 new classifier tests in tests/test_classifier.py
- Full isolation verified: 518/518 unit tests pass with `-m "not integration"`
- Grand total: 576 tests (525 core + 51 e2e/SSE)

---

### Phase 7: Logging, Quality Gate & Coverage Enforcement

**Goal:** All public methods emit structured logs; CI enforces ≥90% branch coverage on `kb_server/` and `ingest/` and fails the build on regression.

**Milestone:** v1.1
**Requirements:** LOG-01, LOG-02, QUAL-01, QUAL-02

**Success criteria:**

1. `pytest --cov=kb_server --cov=ingest --cov-fail-under=90` exits 0 on CI
2. `pyproject.toml` `fail_under = 90` is set and a deliberate coverage regression causes CI to fail
3. Every public method in `kb_server/` has at least one log entry (verified by audit script)
4. Logging audit report committed to `docs/logging-audit.md`

**Plans:** 2 plans

Plans:

- [x] 07-01-PLAN.md — Quality gate: set `fail_under = 90` in `pyproject.toml`, update CI workflow with coverage enforcement on PR-to-master
- [x] 07-02-PLAN.md — Logging coverage audit + gap fill: audit all public methods, add missing log calls to kb_server/ and ingest/, commit audit report

**Wave Dependencies:**

**Wave 1** *(both plans are independent — no blocking dependencies)*

**Cross-cutting constraints:**

- Quality gate must cover both `kb_server/` and `ingest/` (D-01, D-02)
- Coverage exclusions are inline `# pragma: no cover` only — no centralized excludes (D-05)
- Both `pyproject.toml` `fail_under` and CI `--cov-fail-under` must be set (D-06)
- Coverage enforcement runs on PR to master only (D-07)
- CI coverage step covers both `--cov=kb_server --cov=ingest` (D-08)

**Completed:** 2026-05-23

**Delivered:**
- `pyproject.toml`: `[tool.coverage.report] fail_under = 90`, `branch = true`
- CI coverage enforcement step gated on PR-to-master
- `scripts/logging-audit.py` — AST-based logging coverage scanner
- Added logging to 10 kb_server modules + ingest registry
- No centralized coverage excludes (inline `# pragma: no cover` only)

---

### Phase 8: Ingest Improvements & Documentation

**Goal:** OTCS documents are auto-tagged by product area; operators have a CLI status command; key documentation is updated for v1.1.

**Milestone:** v1.1
**Requirements:** INGEST-01, INGEST-02, DOC-01, DOC-02

**Success criteria — all met:**

1. ✅ `3-0117 Content Server WebReport Design.pdf` auto-assigns `product=WebReports`
2. ✅ `kb-ingest status` prints per-source file/chunk/error counts with `--source` filter
3. ✅ All public functions/classes have English Google-style docstrings (0 gaps, 0 Portuguese)
4. ✅ `docs/` updated: ARCHITECTURE.md (Mermaid), OPERATIONS.md (remote deploy), INDEX.md, REFERENCE.md

**Plans:** 3 plans — all executed 2026-05-23

Plans:

- [x] 08-01-PLAN.md — OTCS product auto-tagging via directory/filename detection
- [x] 08-02-PLAN.md — CLI status command with Rich table output
- [x] 08-03-PLAN.md — Docstring sweep + documentation refresh

**Completed:** 2026-05-23

**Delivered:**

- 10 OTCS product areas auto-detectable (WebReports, xECM, Workflow, CSIDE, ContentServer, Brava, OT2, DocumentViewer, APIGateway, ArchiveCenter)
- 18 directory aliases + 10 filename patterns in classifier.py
- `kb-ingest status` with Rich table (Source/Files/Chunks/Errors/Last Ingest)
- `scripts/docstring-audit.py` — AST-based docstring coverage scanner
- 105 docstring gaps fixed (32 MISSING + 73 PORTUGUESE → 0)
- All 4 main docs refreshed for v1.1 accuracy (ARCHITECTURE.md, OPERATIONS.md, INDEX.md, REFERENCE.md)

---

## Milestone Summary

**Timeline:** 8 days (2026-05-14 → 2026-05-23)

**Statistics:**
- 4 phases, 10 plans, ~40 tasks
- 75 files modified
- +4,980 insertions, -3,106 deletions
- 13,457 Python LOC total
- 16 feature commits

**Key Decisions:**

- **Decision:** Pin starlette >=1.0.0 | **Rationale:** Prevent NoneType crash regression in SSE handler
- **Decision:** Mock fixtures with autouse for Qdrant, opt-in for embed/redis | **Rationale:** Balance isolation with test clarity
- **Decision:** 90% branch coverage on both kb_server/ and ingest/ | **Rationale:** Prevent quality regression
- **Decision:** PR-to-master gate for coverage enforcement | **Rationale:** Allow exploration on feature branches
- **Decision:** Inline `# pragma: no cover` only, no centralized excludes | **Rationale:** Require justification at use site
- **Decision:** OTCS aliases inline in classifier.py | **Rationale:** Avoid file proliferation for simple mappings
- **Decision:** English-only docstrings enforced by CI audit | **Rationale:** Eliminate Portuguese drift

**Issues Resolved:**

- SSE NoneType crash when client disconnects before handler returns Response
- Test suite requires Qdrant/LM Studio/Redis to run (blocks CI/CD)
- No coverage enforcement allowing quality regression
- OTCS documents require manual `--product` flag during ingest
- 105 missing or Portuguese docstrings (32 missing + 73 Portuguese)
- Documentation stale with v1.0 test counts and outdated paths

**Issues Deferred:**

- None — all v1.1 requirements completed

**Technical Debt Incurred:**

- SSE tests must run in separate pytest process (stub conflict with test_smoke.py)
- Logging coverage audit threshold set to 40% (baseline, not aspirational)

---

_For current project status, see .planning/ROADMAP.md_
