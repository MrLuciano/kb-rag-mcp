---
phase: 32-api-key-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/auth_registry.py
  - kb_server/auth.py
  - kb_server/server.py
  - ingest/cli/auth.py
  - ingest/cli/main.py
  - tests/test_auth_registry.py
  - tests/test_server_streamable_http.py
  - tests/test_sse_handler.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - HTTP MCP endpoints can require API keys without affecting stdio transport
    - API keys are persisted only as hashes with revocation and scope metadata
    - Operators can create, list, and revoke keys from the CLI without reusing plaintext secrets after creation
  artifacts:
    - path: "kb_server/auth_registry.py"
      provides: "SQLite-backed API key persistence"
      contains: "api_keys"
      min_lines: 35
    - path: "kb_server/auth.py"
      provides: "HTTP request auth guard helpers"
      contains: "Authorization"
      min_lines: 25
---

<objective>
Add optional API key authentication for MCP HTTP transports while preserving existing stdio behavior and infrastructure health endpoints.

Purpose: Phase 32 introduces the auth data model, transport enforcement, and operational CLI needed for production self-hosted deployments. The design must remain additive and avoid breaking current unauthenticated local/stdin workflows.

Output: Auth registry and request guard modules, server integration for `/mcp`, `/sse`, and `/messages`, CLI key management, and tests proving auth applies only where intended.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-api-key-authentication/32-CONTEXT.md
@kb_server/server.py
@kb_server/kb_registry.py
@kb_server/health_server.py
@ingest/cli/main.py
@tests/test_server_streamable_http.py
@tests/test_sse_handler.py
</context>

<tasks>

<task type="auto">
  <name>Create API key registry and auth helpers</name>
  <files>kb_server/auth_registry.py, kb_server/auth.py, tests/test_auth_registry.py</files>
  <read_first>
    kb_server/kb_registry.py
    .planning/phases/32-api-key-authentication/32-CONTEXT.md
  </read_first>
  <action>
    Create a dedicated auth persistence layer separate from ingest metadata. Add a new `kb_server/auth_registry.py` that stores hashed API keys, scopes, revocation state, timestamps, and optional KB binding. Add `kb_server/auth.py` with helpers for parsing API key credentials from HTTP requests, verifying hashes, and returning auth decisions.

    Keep raw keys out of persistent storage and logging. The registry should support global keys and per-KB keys so future multi-tenant enforcement can build on it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_auth_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - API keys are hashed before persistence and registry rows track scope/revocation metadata
    - Verification helpers can distinguish valid, revoked, and unknown keys
    - Tests cover create, lookup, revoke, and scoped-key behavior
  </acceptance_criteria>
  <done>Auth registry and verification helpers exist with focused tests</done>
</task>

<task type="auto">
  <name>Protect MCP HTTP transports with optional API key enforcement</name>
  <files>kb_server/server.py, tests/test_server_streamable_http.py, tests/test_sse_handler.py</files>
  <read_first>
    kb_server/server.py
    kb_server/auth.py
    kb_server/health_server.py
    tests/test_server_streamable_http.py
    tests/test_sse_handler.py
  </read_first>
  <action>
    Integrate auth into the HTTP-facing MCP transport paths in `kb_server/server.py`. Ensure `/mcp`, `/sse`, and the mounted `/messages/` transport path are consistently guarded when auth is enabled, while stdio transport remains unaffected.

    Make the enforcement optional and backward compatible when auth is disabled. Be explicit about whether `/health` remains open; do not accidentally break health or metrics probe behavior.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_sse_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Enabled auth rejects unauthenticated requests on `/mcp`, `/sse`, and `/messages/`
    - Disabled auth preserves existing server behavior
    - stdio transport behavior is unchanged
    - Tests cover allowed and denied cases for both Streamable HTTP and SSE paths
  </acceptance_criteria>
  <done>HTTP MCP transports are guarded consistently when auth is enabled</done>
</task>

<task type="auto">
  <name>Add CLI key management for operators</name>
  <files>ingest/cli/auth.py, ingest/cli/main.py, tests/test_cli.py</files>
  <read_first>
    ingest/cli/main.py
    ingest/cli/reclassify.py
    kb_server/auth_registry.py
    tests/test_cli.py
  </read_first>
  <action>
    Add a dedicated CLI command group for auth management and register it in `ingest/cli/main.py`. Implement create, list, and revoke flows in `ingest/cli/auth.py` using the new auth registry.

    The create flow should print the plaintext key once and never persist or re-display it later. Revoke/list flows should use operator-friendly output and support destructive confirmation patterns consistent with the existing CLI style.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `kb-rag auth` appears in CLI help
    - Create/list/revoke flows work against the auth registry without exposing stored plaintext keys
    - Tests cover command discovery and the primary key-management flows
  </acceptance_criteria>
  <done>Operator CLI for API key management exists and is tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → MCP server | Untrusted clients may attempt unauthorized access |
| operator CLI → auth registry | Sensitive credential lifecycle operations write server auth state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | API key persistence | mitigate | Store only key hashes; reveal plaintext key once at creation |
| T-32-02 | Elevation of Privilege | unguarded MCP transport path | mitigate | Guard `/mcp`, `/sse`, and `/messages/` consistently when auth is enabled |
| T-32-03 | Denial of Service | breaking health probes | mitigate | Keep health/metrics behavior explicit and covered by tests |

</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_auth_registry.py -q`
- `.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_sse_handler.py -q`
- `.venv/bin/python -m pytest tests/test_cli.py -q`
</verification>

<success_criteria>
- Optional API key auth exists for HTTP transports with zero regression to stdio
- Auth state is safely persisted and revocable
- Operators can manage keys through the CLI
</success_criteria>

<output>
After completion, create `.planning/phases/32-api-key-authentication/32-01-SUMMARY.md`
</output>
