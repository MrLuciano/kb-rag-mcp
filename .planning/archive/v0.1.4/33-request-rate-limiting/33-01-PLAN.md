---
phase: 33-request-rate-limiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/worker/limiter.py
  - kb_server/server.py
  - observability/metrics.py
  - tests/test_server_streamable_http.py
  - tests/test_server_tools.py
  - tests/test_worker_system.py
autonomous: true
requirements:
  - RATE-01
  - RATE-02
  - RATE-03
must_haves:
  truths:
    - MCP request handling enforces token-bucket throttling at the server boundary
    - Rate limiting is observable through Prometheus metrics
    - Transport-specific rejection behavior is explicit and tested
  artifacts:
    - path: "ingest/worker/limiter.py"
      provides: "Reusable token-bucket rate limiter"
      contains: "RateLimiter"
      min_lines: 20
    - path: "kb_server/server.py"
      provides: "Server-side request throttling"
      contains: "rate"
      min_lines: 25
---

<objective>
Enforce request rate limiting on MCP transports and tool execution paths using the existing token-bucket limiter pattern.

Purpose: Phase 33 protects the MCP server from abusive or accidental request floods while preserving clear behavior across HTTP and stdio transports. It should reuse current limiter infrastructure rather than invent a second algorithm.

Output: Server-integrated rate limiting, metrics for allowed/delayed/rejected requests, and tests covering transport-specific behavior.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-request-rate-limiting/33-CONTEXT.md
@ingest/worker/limiter.py
@kb_server/server.py
@observability/metrics.py
@tests/test_server_streamable_http.py
@tests/test_server_tools.py
</context>

<tasks>

<task type="auto">
  <name>Adapt the existing token-bucket limiter for server request use</name>
  <files>ingest/worker/limiter.py, tests/test_worker_system.py</files>
  <read_first>
    ingest/worker/limiter.py
    .planning/phases/33-request-rate-limiting/33-CONTEXT.md
    tests/test_worker_system.py
  </read_first>
  <action>
    Refine the current limiter implementation so it can be safely reused by server request handling. Keep the token-bucket algorithm, but make the API practical for per-subject request limiting and explicit rejection paths needed by MCP transports.

    Preserve existing ingest-side limiter semantics while adding the minimum new capabilities needed for server use, such as subject namespacing or non-blocking rejection support.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worker_system.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing limiter behavior remains valid for ingest worker use
    - Limiter exposes a clear path for immediate reject vs wait behavior needed by server requests
    - Tests cover token accounting and non-blocking rejection behavior
  </acceptance_criteria>
  <done>Limiter is reusable by both ingest workers and server-side request control</done>
</task>

<task type="auto">
  <name>Apply rate limiting to MCP request handling paths</name>
  <files>kb_server/server.py, tests/test_server_streamable_http.py, tests/test_server_tools.py</files>
  <read_first>
    kb_server/server.py
    ingest/worker/limiter.py
    tests/test_server_streamable_http.py
    tests/test_server_tools.py
  </read_first>
  <action>
    Integrate rate limiting into `kb_server/server.py` at the request choke points used by MCP transports and/or tool dispatch.

    Ensure HTTP-facing requests can fail with an HTTP-appropriate rejection, while stdio/tool invocations return a structured MCP/tool error instead of assuming an HTTP response exists. Do not silently throttle only one transport path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_server_tools.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Repeated requests from the same subject are throttled according to configured limits
    - HTTP transports expose an explicit rejection path rather than hanging indefinitely
    - stdio/tool invocations degrade via structured error content instead of HTTP-specific behavior
    - Tests cover success and throttle cases for server request handling
  </acceptance_criteria>
  <done>MCP request paths are rate limited with transport-appropriate rejection behavior</done>
</task>

<task type="auto">
  <name>Add observability for rate-limited traffic</name>
  <files>observability/metrics.py, kb_server/server.py, tests/test_server_streamable_http.py</files>
  <read_first>
    observability/metrics.py
    kb_server/server.py
    tests/test_health_server.py
  </read_first>
  <action>
    Extend Prometheus metrics to track allowed, delayed, and rejected requests for server-side rate limiting. Use labels that make operational sense (for example limiter name, transport, or endpoint) without exploding cardinality.

    Update the server integration to emit these metrics whenever throttling decisions occur.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_health_server.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Metrics module exposes counters/gauges/histograms for rate-limit decisions
    - Server records rate-limit outcomes when requests are allowed or rejected
    - Tests validate that throttled flows integrate with existing metrics exposure paths
  </acceptance_criteria>
  <done>Rate limiting emits Prometheus-visible telemetry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request volume → MCP server | Untrusted request traffic can overload server resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Denial of Service | request flood | mitigate | Token-bucket rate limiting at server boundary |
| T-33-02 | Repudiation | missing throttle observability | mitigate | Emit metrics for allowed/rejected/delayed decisions |
| T-33-03 | Tampering | transport-specific error mismatch | mitigate | Test HTTP and stdio rejection semantics separately |

</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_worker_system.py -q`
- `.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_server_tools.py -q`
- `.venv/bin/python -m pytest tests/test_server_streamable_http.py tests/test_health_server.py -q`
</verification>

<success_criteria>
- Server-side request throttling exists and reuses the project’s limiter pattern
- Rate-limit outcomes are observable through Prometheus metrics
- Transport-specific throttle behavior is documented by tests
</success_criteria>

<output>
After completion, create `.planning/phases/33-request-rate-limiting/33-01-SUMMARY.md`
</output>
