---
phase: 23-documentation-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.pt-BR.md
  - README.es.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - README top section is a concise quickstart with "pick your deployment mode" table
    - README.pt-BR.md and README.es.md have parallel two-tier structure
    - Detailed deployment content has moved from README to docs/ files
  artifacts:
    - README.md has quickstart at top, links to docs/ for detailed content
    - README.pt-BR.md has quickstart at top, links to docs/ for detailed content
    - README.es.md has quickstart at top, links to docs/ for detailed content
  key_links:
    - Quickstart section links to each deployment mode's docs
---

<objective>
Restructure README to two-tier format

Purpose: Reduce README from 50K monolith to a concise quickstart with pick-your-mode table, moving detailed content to docs/ files. Same for pt-BR and ES translations.

Output: Restructured README.md, README.pt-BR.md, README.es.md
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-documentation-overhaul/23-CONTEXT.md
@README.md
@README.pt-BR.md
@README.es.md
@docs/INDEX.md
@docs/INSTRUCTIONS.md
@docs/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Restructure README.md to two-tier format</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    Read the full 1931-line README.md. Restructure it as follows:

    **Top section (quickstart, ~50-80 lines):**
    1. Keep the project title, badges, and 2-3 sentence value proposition
    2. Add a "Quickstart" section with a pick-your-deployment-mode table:
       ```
       | Mode | Best for | Get started |
       |------|----------|-------------|
       | Docker Compose | Local dev / small teams | `docker compose up -d` → [docs/](docs/INSTRUCTIONS.md#docker-compose) |
       | Helm (Kubernetes) | Production clusters | `helm install` → [docs/KUBERNETES.md](docs/KUBERNETES.md) |
       | Systemd | Bare metal / VMs | `sudo cp kb-mcp.service /etc/systemd/system/` → [docs/INSTRUCTIONS.md#systemd](docs/INSTRUCTIONS.md#systemd) |
       | Manual (Source) | Development / customization | `python -m kb_server.server` → [docs/INSTRUCTIONS.md#manual](docs/INSTRUCTIONS.md#manual) |
       ```
    3. Add a "Next Steps" paragraph: "Choose your deployment mode above, then refer to the detailed guides in `docs/`."
    4. Add a "Documentation Index" section linking to docs/INDEX.md

    **Below the quickstart:**
    Replace the remaining detailed content with a terse bulleted section structure:
    - "**Architecture** → See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)"
    - "**Configuration** → See [docs/OPERATIONS.md → Common](docs/OPERATIONS.md#common)"
    - "**API Reference** → See [docs/REFERENCE.md](docs/REFERENCE.md)"
    - "**Troubleshooting** → See [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)"
    - etc.

    **Move detailed content:**
    Content that is duplicated in docs/ files should be removed from README. Content that is ONLY in README should either be moved to the appropriate docs/ file or kept if it's essential quickstart material. Do NOT delete any content that doesn't exist elsewhere — move it to the right docs/ file first.

    Preserve any important tables, code examples, and configuration references that are not already in docs/. Keep the existing "Features" section if present — it's useful for discovery.

    Final README should be ~200-300 lines (down from 1931).
  </action>
  <verify>
    wc -l README.md && head -30 README.md | grep -qi 'quickstart\|deployment\|mode' && grep -q 'docs/INDEX.md' README.md && grep -q 'docs/OPERATIONS.md' README.md
  </verify>
  <acceptance_criteria>
    - README.md is 200-300 lines (down from 1931)
    - First ~80 lines have quickstart with deployment mode table
    - Detailed content moved to docs/ files, not deleted
    - Links to docs/INDEX.md and relevant docs/ files
  </acceptance_criteria>
  <done>README.md restructured to ~200-300 lines with quickstart + docs/ links</done>
</task>

<task type="auto">
  <name>Restructure README.pt-BR.md to two-tier format</name>
  <files>README.pt-BR.md</files>
  <read_first>README.pt-BR.md</read_first>
  <action>
    Follow the same two-tier restructuring as README.md:
    - Top section: quickstart with deployment mode table (translated to pt-BR)
    - Below: terse section structure linking to docs/
    - Remove or redirect detailed content to docs/ files
    - Preserve content that doesn't exist elsewhere

    Use the same mode table format, translated:
    ```
    | Modo | Ideal para | Comece aqui |
    |------|-----------|-------------|
    | Docker Compose | Desenvolvimento local / equipes pequenas | ...
    ```

    Target: ~200-300 lines (down from 1551).
  </action>
  <verify>wc -l README.pt-BR.md && head -30 README.pt-BR.md | grep -qi 'modo\|quickstart\|comece' && grep -q 'docs/INDEX.md' README.pt-BR.md</verify>
  <acceptance_criteria>
    - README.pt-BR.md is ~200-300 lines
    - Opening section has translated quickstart with mode table
    - Detailed content redirected to docs/ files
  </acceptance_criteria>
  <done>README.pt-BR.md restructured to ~200-300 lines with translated quickstart</done>
</task>

<task type="auto">
  <name>Restructure README.es.md to two-tier format</name>
  <files>README.es.md</files>
  <read_first>README.es.md</read_first>
  <action>
    Follow the same two-tier restructuring as README.md:
    - Top section: quickstart with deployment mode table (translated to ES)
    - Below: terse section structure linking to docs/
    - Remove or redirect detailed content to docs/ files
    - Preserve content that doesn't exist elsewhere

    Use the same mode table format, translated:
    ```
    | Modo | Ideal para | Empiece aquí |
    |------|-----------|-------------|
    | Docker Compose | Desarrollo local / equipos pequeños | ...
    ```

    Target: ~200-300 lines (down from 1551).
  </action>
  <verify>wc -l README.es.md && head -30 README.es.md | grep -qi 'modo\|quickstart\|empiece' && grep -q 'docs/INDEX.md' README.es.md</verify>
  <acceptance_criteria>
    - README.es.md is ~200-300 lines
    - Opening section has translated quickstart with mode table
    - Detailed content redirected to docs/ files
  </acceptance_criteria>
  <done>README.es.md restructured to ~200-300 lines with translated quickstart</done>
</task>

</tasks>

<verification>
- [ ] All 3 READMEs are ~200-300 lines
- [ ] Each has a deployment mode table in the top section
- [ ] Each links to docs/INDEX.md and relevant docs/ files
- [ ] No content was permanently deleted — only moved to docs/
- [ ] Quickstart content is accurate and matches actual deployment commands
</verification>

<success_criteria>
- User can quickly choose a deployment mode from any README (EN/PT-BR/ES)
- Detailed content accessible via docs/ sub-pages
- Consistent structure across all 3 languages
</success_criteria>

<output>
After completion, create `.planning/phases/23-documentation-overhaul/23-02-SUMMARY.md`
</output>
