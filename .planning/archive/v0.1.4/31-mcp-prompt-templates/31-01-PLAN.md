---
phase: 31-mcp-prompt-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/prompts.py
  - kb_server/server.py
  - tests/test_server_prompts.py
  - tests/test_server_discoverability.py
  - docs/REFERENCE.md
  - docs/INSTRUCTIONS.md
autonomous: true
requirements:
  - MCPPROMPTS-01
  - MCPPROMPTS-02
  - MCPPROMPTS-03
must_haves:
  truths:
    - MCP clients can discover prompt templates via prompts/list
    - MCP clients can request concrete rendered prompt content via get_prompt
    - Prompt support is additive and does not change existing tool/resource behavior
  artifacts:
    - path: "kb_server/prompts.py"
      provides: "Prompt template definitions and rendering helpers"
      contains: "extract_answer"
      min_lines: 30
    - path: "kb_server/server.py"
      provides: "MCP prompt registration"
      contains: "list_prompts"
      min_lines: 25
---

<objective>
Add MCP prompt template support so clients can discover and request grounded answer and summarization prompts directly from the server.

Purpose: Phase 31 should expose the MCP `prompts/list` and `get_prompt` capabilities using prompt templates that align with current KB search semantics, citation style, and discoverability conventions. The change must be additive only.

Output: A dedicated `kb_server/prompts.py` module, prompt registration in `server.py`, tests for prompt discovery/rendering, and documentation updates.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-mcp-prompt-templates/31-CONTEXT.md
@kb_server/server.py
@kb_server/filter_terms_cache.py
@tests/test_server_discoverability.py
@tests/test_server_tools.py
@docs/REFERENCE.md
@docs/INSTRUCTIONS.md
@.venv/lib/python3.11/site-packages/mcp/server/lowlevel/server.py
@.venv/lib/python3.11/site-packages/mcp/types.py
</context>

<tasks>

<task type="auto">
  <name>Create prompt template module with grounded-answer and summarize-documents renderers</name>
  <files>kb_server/prompts.py, tests/test_server_prompts.py</files>
  <read_first>
    kb_server/server.py
    kb_server/filter_terms_cache.py
    .planning/phases/31-mcp-prompt-templates/31-CONTEXT.md
    .venv/lib/python3.11/site-packages/mcp/types.py
  </read_first>
  <action>
    Create `kb_server/prompts.py` to hold prompt definitions and render helpers outside `server.py`.

    Implement at least two prompts:
    - `extract_answer`: grounded answer prompt that references current citation conventions and chunk identifiers
    - `summarize_documents`: summary prompt for retrieved KB content

    The module should expose a stable registry and renderer interface that `server.py` can call. Prompt arguments should be explicit and compatible with MCP prompt argument types (for example question/query, result_count/top_k, and optional filter hints).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_prompts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `kb_server/prompts.py` exists with a prompt registry and render helpers for grounded-answer and summarize-documents prompts
    - Prompt text references existing KB citation semantics and does not invent incompatible response shapes
    - Tests validate prompt rendering for valid arguments and empty/default argument cases
  </acceptance_criteria>
  <done>Prompt template module exists with tested render helpers</done>
</task>

<task type="auto">
  <name>Register MCP prompt capabilities in server.py</name>
  <files>kb_server/server.py, tests/test_server_discoverability.py, tests/test_server_prompts.py</files>
  <read_first>
    kb_server/server.py
    kb_server/prompts.py
    .venv/lib/python3.11/site-packages/mcp/server/lowlevel/server.py
    .venv/lib/python3.11/site-packages/mcp/types.py
    tests/test_server_discoverability.py
  </read_first>
  <action>
    Add MCP prompt handlers in `kb_server/server.py` using the actual low-level server decorators supported by the installed SDK:
    - `@app.list_prompts()` to advertise available prompts
    - `@app.get_prompt()` to return rendered prompt content

    Keep the implementation additive only. Existing `list_tools`, `list_resources`, `read_resource`, and `call_tool` behavior must remain unchanged. Route prompt rendering through `kb_server/prompts.py` rather than embedding prompt bodies directly in `server.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_prompts.py tests/test_server_discoverability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - MCP server advertises prompt capabilities via `list_prompts`
    - `get_prompt` returns rendered content for known prompts and a clear error path for unknown prompt names
    - Existing tool/resource tests still pass or are explicitly preserved in prompt-related test coverage
  </acceptance_criteria>
  <done>Server exposes prompt discovery and rendering via the installed MCP SDK decorators</done>
</task>

<task type="auto">
  <name>Document prompt capabilities and verify additive compatibility</name>
  <files>docs/REFERENCE.md, docs/INSTRUCTIONS.md, tests/test_server_discoverability.py, tests/test_server_tools.py</files>
  <read_first>
    docs/REFERENCE.md
    docs/INSTRUCTIONS.md
    tests/test_server_discoverability.py
    tests/test_server_tools.py
    kb_server/server.py
  </read_first>
  <action>
    Update documentation to describe the new MCP prompt capabilities, their names, arguments, and intended use.

    Add or extend tests so discoverability coverage proves prompt support is additive: clients can still list tools/resources and use existing handlers while prompt support is present. Keep documentation aligned with actual prompt names and arguments implemented in `kb_server/prompts.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_server_prompts.py tests/test_server_discoverability.py tests/test_server_tools.py -q</automated>
  </verify>
  <acceptance_criteria>
    - REFERENCE.md and INSTRUCTIONS.md document prompt capabilities with exact prompt names and argument expectations
    - Discoverability tests prove prompts coexist with existing tools/resources
    - No existing MCP surface is removed or silently renamed
  </acceptance_criteria>
  <done>Prompt docs and additive-compatibility coverage are in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → prompt arguments | Untrusted client input selects prompt name and arguments |
| prompt templates → LLM client workflows | Prompt text influences downstream model behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | prompt name/argument handling | mitigate | Validate prompt names and render only known prompt definitions |
| T-31-02 | Information Disclosure | prompt text | mitigate | Keep prompts grounded in existing KB metadata conventions; do not expose secrets or hidden server state |
| T-31-03 | Repudiation | additive compatibility regression | mitigate | Add discoverability tests proving tools/resources remain unchanged while prompts are added |

</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_server_prompts.py -q`
- `.venv/bin/python -m pytest tests/test_server_prompts.py tests/test_server_discoverability.py -q`
- `.venv/bin/python -m pytest tests/test_server_prompts.py tests/test_server_discoverability.py tests/test_server_tools.py -q`
</verification>

<success_criteria>
- MCP prompt capabilities are discoverable and render prompt content through the installed SDK APIs
- Prompt templates are grounded in current KB search/citation conventions
- Existing MCP tools/resources/transports continue to behave the same after prompt support is added
</success_criteria>

<output>
After completion, create `.planning/phases/31-mcp-prompt-templates/31-01-SUMMARY.md`
</output>
