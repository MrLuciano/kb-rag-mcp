---
phase: 34-upload-index-quotas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/core/metadata.py
  - ingest/ingest.py
  - ingest/worker/worker.py
  - ingest/worker/executor.py
  - ingest/job/manager.py
  - ingest/cli/job.py
  - tests/test_ingest_registry.py
  - tests/test_worker_system.py
  - tests/test_job_system.py
autonomous: true
requirements:
  - QUOTA-01
  - QUOTA-02
  - QUOTA-03
must_haves:
  truths:
    - Quota limits and usage are stored durably in metadata storage
    - Direct ingest and worker/job paths enforce the same quota rules
    - Operators can inspect or modify quota configuration from CLI surfaces
  artifacts:
    - path: "ingest/core/metadata.py"
      provides: "Quota schema and usage persistence"
      contains: "quota"
      min_lines: 35
    - path: "ingest/ingest.py"
      provides: "Direct-ingest quota enforcement"
      contains: "quota"
      min_lines: 20
---

<objective>
Add durable upload and index quota enforcement across all ingest entry points.

Purpose: Phase 34 ensures no ingest path can bypass configured limits for files, bytes, chunks, documents, or character volume. Quota state must be persisted and manageable by operators.

Output: Metadata schema for quotas and usage, enforcement in direct and worker/job ingest flows, CLI/admin handling, and regression coverage.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-upload-index-quotas/34-CONTEXT.md
@ingest/core/metadata.py
@ingest/ingest.py
@ingest/worker/worker.py
@ingest/worker/executor.py
@ingest/job/manager.py
@ingest/cli/job.py
</context>

<tasks>

<task type="auto">
  <name>Add quota and usage persistence to metadata storage</name>
  <files>ingest/core/metadata.py, tests/test_ingest_registry.py</files>
  <read_first>
    ingest/core/metadata.py
    .planning/phases/34-upload-index-quotas/34-CONTEXT.md
    tests/test_ingest_registry.py
  </read_first>
  <action>
    Extend metadata storage with durable quota definitions and usage counters for ingest-related limits such as files, bytes, chunks, documents, and characters. Keep the schema compatible with existing jobs/files tables and provide helper methods for reading limits, updating usage, and resetting or recalculating usage when needed.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ingest_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - MetadataStore persists quota settings and current usage counters
    - Schema initialization and migration are safe for existing metadata databases
    - Tests cover quota read/write and usage update behavior
  </acceptance_criteria>
  <done>Quota state is durably stored in metadata storage</done>
</task>

<task type="auto">
  <name>Enforce quotas in direct ingest and worker/job execution paths</name>
  <files>ingest/ingest.py, ingest/worker/worker.py, ingest/worker/executor.py, ingest/job/manager.py, tests/test_worker_system.py, tests/test_job_system.py</files>
  <read_first>
    ingest/ingest.py
    ingest/worker/worker.py
    ingest/worker/executor.py
    ingest/job/manager.py
    ingest/validation/size.py
    tests/test_worker_system.py
    tests/test_job_system.py
  </read_first>
  <action>
    Add quota enforcement before expensive processing in every ingest path:
    - direct ingest from `ingest.py`
    - worker validation / execution
    - job creation or execution surfaces

    Reuse existing validation concepts where possible, but ensure direct ingest cannot bypass limits that worker-driven ingest would reject. Return actionable failure messages describing which limit was exceeded.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worker_system.py tests/test_job_system.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Direct ingest rejects over-limit work before embeddings/upserts
    - Worker/job ingest rejects the same over-limit work using the same quota state
    - Tests prove there is no bypass between direct and job-based ingestion paths
  </acceptance_criteria>
  <done>Quota enforcement is consistent across all ingest entry points</done>
</task>

<task type="auto">
  <name>Expose operator quota controls via CLI</name>
  <files>ingest/cli/job.py, ingest/cli/main.py, tests/test_cli.py</files>
  <read_first>
    ingest/cli/job.py
    ingest/cli/main.py
    ingest/core/metadata.py
    tests/test_cli.py
  </read_first>
  <action>
    Add CLI surfaces that let operators inspect and modify quota configuration and usage. Keep the UX consistent with existing job and admin commands. If quota management belongs under the job group, add subcommands there; otherwise register a clear quota/admin command group in main CLI.

    Include human-readable reporting of current limits and usage so operators can understand why a quota rejection occurred.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - CLI exposes quota inspection and update operations
    - Quota reporting shows current usage alongside configured limits
    - Tests cover command discovery and representative quota-management flows
  </acceptance_criteria>
  <done>Quota controls are available to operators through CLI surfaces</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest source → ingest pipeline | Untrusted content volume may exceed configured capacity |
| operator CLI → quota configuration | Admin actions can tighten or loosen system capacity controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | oversized ingest workloads | mitigate | Enforce quotas before expensive processing across all ingest paths |
| T-34-02 | Tampering | inconsistent quota usage accounting | mitigate | Persist quota state centrally in metadata storage |
| T-34-03 | Repudiation | unclear operator view of quota state | mitigate | Expose CLI reporting of limits and usage |

</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_ingest_registry.py -q`
- `.venv/bin/python -m pytest tests/test_worker_system.py tests/test_job_system.py -q`
- `.venv/bin/python -m pytest tests/test_cli.py -q`
</verification>

<success_criteria>
- Quotas are persisted durably and enforced in all ingest paths
- No direct-ingest bypass exists relative to worker/job paths
- Operators can inspect and adjust quota settings from the CLI
</success_criteria>

<output>
After completion, create `.planning/phases/34-upload-index-quotas/34-01-SUMMARY.md`
</output>
