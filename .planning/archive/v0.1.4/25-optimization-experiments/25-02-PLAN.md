---
phase: 25-optimization-experiments
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - kb_server/optimization/chunking_experiments.py
  - tests/test_chunking_experiments.py
autonomous: true
requirements:
  - OPT-01

must_haves:
  truths:
    - "User can run chunking experiments with fixed, recursive, and semantic strategies"
    - "Each chunking strategy returns a list of text chunks from a given document"
    - "Experiment re-ingest uses clean=True to avoid polluting the index"
  artifacts:
    - path: "kb_server/optimization/chunking_experiments.py"
      provides: "Chunking strategy abstractions and experiment execution"
      exports:
        - "ChunkingStrategy"
        - "FixedStrategy"
        - "RecursiveStrategy"
        - "SemanticStrategy"
        - "ChunkingEngine"
    - path: "tests/test_chunking_experiments.py"
      provides: "Unit tests for chunking strategies"
      min_lines: 60
  key_links:
    - from: "chunking_experiments.py"
      to: "ingest/ingest.py:chunk_text"
      via: "RecursiveStrategy reuses RecursiveCharacterTextSplitter"
      pattern: "RecursiveCharacterTextSplitter"
    - from: "chunking_experiments.py"
      to: "docling.chunking.HybridChunker"
      via: "SemanticStrategy"
      pattern: "HybridChunker"
    - from: "ChunkingEngine"
      to: "kb_server/optimization/config.py"
      via: "reads CHUNK_STRATEGIES"
      pattern: "CHUNK_STRATEGIES"
---

<objective>
Implement chunking strategy experiments with a common abstraction for fixed, recursive, and semantic chunking, plus a ChunkingEngine that orchestrates re-ingest with different parameters.

Purpose: Fulfill OPT-01 by enabling systematic comparison of chunk sizes and strategies against a golden dataset.
Output: chunking_experiments.py with three strategy implementations and ChunkingEngine, plus comprehensive tests.
</objective>

<execution_context>
@/home/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-optimization-experiments/25-RESEARCH.md

# Prior plan summary (Wave 1 infrastructure)
@.planning/phases/25-optimization-experiments/25-01-SUMMARY.md

# Reference implementations
@kb_server/optimization/config.py
@kb_server/optimization/metric_computer.py
@kb_server/optimization/result_store.py
@ingest/ingest.py
@kb_server/evaluation/dataset.py
@kb_server/evaluation/ragas_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement chunking strategies and ChunkingEngine</name>
  <files>
    kb_server/optimization/chunking_experiments.py
  </files>
  <read_first>
    kb_server/optimization/chunking_experiments.py
    kb_server/optimization/config.py
    kb_server/optimization/metric_computer.py
    kb_server/optimization/result_store.py
    ingest/ingest.py
    kb_server/evaluation/dataset.py
  </read_first>
  <action>
    Replace the stub `kb_server/optimization/chunking_experiments.py` with:

    - `ChunkingStrategy(ABC)` abstract base class with method:
      - `split(self, text: str, file_type: str = "txt") -> list[str]` — returns list of chunk strings
    - `FixedStrategy(ChunkingStrategy)`:
      - `__init__(self, chunk_size: int = 600, chunk_overlap: int = 80)`
      - Manual sliding-window split: chunks = [text[i:i+chunk_size] for i in range(0, len(text), chunk_size - chunk_overlap)]
      - Drop chunks shorter than 30 characters (same threshold as ingest.py)
    - `RecursiveStrategy(ChunkingStrategy)`:
      - `__init__(self, chunk_size: int = 600, chunk_overlap: int = 80, separators: Optional[list[str]] = None)`
      - Uses `langchain_text_splitters.RecursiveCharacterTextSplitter` with the same separator priority as ingest.py: `["\n\n", "\n", ". ", " ", ""]`
      - Falls back to manual split if langchain is not installed (ImportError catch)
    - `SemanticStrategy(ChunkingStrategy)`:
      - `__init__(self, max_tokens: int = 512)`
      - Uses `docling.chunking.HybridChunker` if available
      - IMPORTANT: HybridChunker may require a docling Document object, not plain text. If `chunk()` accepts plain text, use it directly. If it requires a Document, wrap with a lightweight conversion: `from docling.datamodel.document import ConversionResult; from docling.chunking import HybridChunker; chunker = HybridChunker(max_tokens=max_tokens); chunks = chunker.chunk(text)` — if this fails, fall back to RecursiveStrategy with chunk_size=512
      - Log a warning if fallback occurs so the user knows
      - Return list of chunk text strings
    - `ChunkingEngine` class:
      - `__init__(self, strategy: ChunkingStrategy, vector_store: Any, dataset: GoldenDataset)`
      - `async def run_experiment(self, docs_path: Path, top_k: int = 5, clean: bool = True) -> dict[str, float]`:
        1. If clean=True: clear the temporary experiment collection (use `kb_docs_experiment_{uuid}` or `QDRANT_COLLECTION` env var with `_experiment` suffix)
        2. Re-ingest docs using the strategy's `split()` method instead of `ingest.py:chunk_text()` — this requires a lightweight re-ingest function within the engine that extracts text, chunks with the strategy, embeds, and upserts
        3. Run each query from the dataset through the vector store's search
        4. Collect retrieved doc filenames (source_file from payload)
        5. Compute IR metrics via `MetricComputer.compute_all()`
        6. Return dict with keys: `recall_at_k`, `mrr`, `ndcg_at_k`, plus `chunk_count`, `strategy_name`
      - `async def _ingest_with_strategy(self, docs_path: Path) -> int` — private helper that walks docs_path, extracts text per file type, chunks with strategy, gets embeddings via `get_embeddings_batch`, upserts to vector store
      - The engine must use `clean=True` by default (per RESEARCH.md Pitfall 1)
      - Use a temporary collection name: `os.getenv("QDRANT_COLLECTION", "kb_docs") + "_experiment"` to avoid polluting production
    - `create_strategy(name: str, **kwargs) -> ChunkingStrategy` factory function:
      - Accepts "fixed", "recursive", "semantic"
      - Passes kwargs (chunk_size, overlap, max_tokens) to the appropriate constructor
      - Raises ValueError for unknown strategy names
    - Module docstring with `PHASE 25: Optimization Experiments` tag
    - Logger named `log = logging.getLogger("kb-mcp.optimization")`
    - Follow Black line-length 79, snake_case, PascalCase conventions
    - Import organization: stdlib → third-party → local (config, metric_computer, dataset)
     - Do NOT import heavy models at module level; lazy-load inside strategy methods
     - Do NOT modify the production `ingest.py` pipeline; the engine does its own lightweight extraction
     - Import `Optional` from `typing` (pre-3.10 style per CONVENTIONS.md)
   </action>
   <verify>
     <automated>python -c "from kb_server.optimization.chunking_experiments import ChunkingEngine, create_strategy; print('OK')"</automated>
   </verify>
   <done>
     - chunking_experiments.py contains `ChunkingStrategy`, `FixedStrategy`, `RecursiveStrategy`, `SemanticStrategy`, `ChunkingEngine`, `create_strategy`
    - `create_strategy("fixed", chunk_size=256)` returns a FixedStrategy instance
    - `create_strategy("semantic", max_tokens=512)` returns a SemanticStrategy instance
    - `FixedStrategy(chunk_size=10).split("hello world test")` returns `["hello worl", "d test"]` (exact non-overlapping chunks of 10 characters)
    - `RecursiveStrategy().split("Paragraph 1.\n\nParagraph 2.", "txt")` respects paragraph boundaries
    - `ChunkingEngine` uses a temporary collection name with `_experiment` suffix
    - `pytest tests/test_chunking_experiments.py` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for chunking strategies</name>
  <files>
    tests/test_chunking_experiments.py
  </files>
  <read_first>
    kb_server/optimization/chunking_experiments.py
    kb_server/optimization/config.py
    tests/test_metric_computer.py
  </read_first>
  <action>
    Create `tests/test_chunking_experiments.py` with:
    - `test_fixed_strategy_basic` — split a 100-char text with chunk_size=30, overlap=10 → verify chunk count and content
    - `test_fixed_strategy_small_chunks_dropped` — text with chunks < 30 chars are dropped
    - `test_recursive_strategy_split` — verify paragraph boundaries preserved with `\n\n` separator
    - `test_recursive_strategy_fallback_on_import_error` — mock `RecursiveCharacterTextSplitter` as unavailable, verify manual fallback works
    - `test_semantic_strategy_import` — verify HybridChunker is imported and used (mock if needed)
    - `test_semantic_strategy_fallback` — mock HybridChunker to raise ImportError, verify fallback to recursive
    - `test_create_strategy_fixed` — `create_strategy("fixed", chunk_size=256)` returns FixedStrategy
    - `test_create_strategy_recursive` — `create_strategy("recursive", chunk_size=512)` returns RecursiveStrategy
    - `test_create_strategy_semantic` — `create_strategy("semantic", max_tokens=256)` returns SemanticStrategy
    - `test_create_strategy_invalid` — `create_strategy("invalid")` raises ValueError
    - `test_chunking_engine_init` — `ChunkingEngine(strategy, mock_store, mock_dataset)` stores dependencies
    - `pytestmark = pytest.mark.PHASE25` at module level
    - All tests use mock vector_store and dataset (no real Qdrant needed)
    - Mock `get_embeddings_batch` if needed for engine tests
    - Follow existing test file conventions (snake_case, descriptive names, assert exact values)
    - All tests must pass in under 10 seconds total
  </action>
  <verify>
    <automated>python -m pytest tests/test_chunking_experiments.py -x -v</automated>
  </verify>
  <done>
    - test_chunking_experiments.py has 10+ tests, all passing
    - All tests run without external services (Qdrant, LM Studio, docling models)
    - Tests cover FixedStrategy, RecursiveStrategy, SemanticStrategy, create_strategy, and ChunkingEngine init
    - `pytest tests/test_chunking_experiments.py` exits 0 with 10+ passed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChunkingEngine → Qdrant | Re-ingest creates temporary collection; must not touch production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Denial of Service | ChunkingEngine._ingest_with_strategy | mitigate | Default `clean=True` and `_experiment` collection suffix prevent index pollution |
| T-25-04 | Information Disclosure | ChunkingEngine logs | accept | Log level INFO for operations, no PII in paths |
| T-25-05 | Tampering | SemanticStrategy fallback | mitigate | Log warning when HybridChunker unavailable; deterministic fallback to RecursiveStrategy |
</threat_model>

<verification>
- `python -m pytest tests/test_chunking_experiments.py -x` passes
- `flake8 kb_server/optimization/chunking_experiments.py tests/test_chunking_experiments.py` reports zero errors
- `python -c "from kb_server.optimization.chunking_experiments import ChunkingEngine, create_strategy; print('OK')"` succeeds
</verification>

<success_criteria>
- chunking_experiments.py replaces the NotImplementedError stub with full implementations
- Three strategies (fixed, recursive, semantic) are implemented and tested
- ChunkingEngine can run experiments with clean re-ingest to a temporary collection
- test_chunking_experiments.py has 10+ passing tests
- No production collections are modified by experiment code
</success_criteria>

<output>
Create `.planning/phases/25-optimization-experiments/25-02-SUMMARY.md` when done
</output>
