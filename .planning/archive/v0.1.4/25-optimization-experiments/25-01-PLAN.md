---
phase: 25-optimization-experiments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/optimization/config.py
  - kb_server/optimization/metric_computer.py
  - kb_server/optimization/result_store.py
  - tests/test_metric_computer.py
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "Experiment configuration defaults are centralized in config.py"
    - "IR metrics (recall@K, MRR, NDCG) are computed deterministically from search results and expected docs"
    - "Experiment results are persisted to CSV/JSON with run metadata for comparison"
  artifacts:
    - path: "kb_server/optimization/config.py"
      provides: "Experiment parameter defaults and validation"
      exports:
        - "ExperimentConfig"
        - "CHUNK_STRATEGIES"
        - "SCORING_VARIANTS"
    - path: "kb_server/optimization/metric_computer.py"
      provides: "IR metric computation"
      exports:
        - "MetricComputer"
        - "recall_at_k"
        - "mean_reciprocal_rank"
        - "ndcg_at_k"
    - path: "kb_server/optimization/result_store.py"
      provides: "Experiment result persistence and comparison"
      exports:
        - "ExperimentResultStore"
        - "load_results"
    - path: "tests/test_metric_computer.py"
      provides: "Unit tests for metric computation"
      min_lines: 80
  key_links:
    - from: "metric_computer.py"
      to: "sklearn.metrics.ndcg_score"
      via: "import"
      pattern: "from sklearn.metrics import ndcg_score"
    - from: "result_store.py"
      to: "kb_server/evaluation/exporter.py"
      via: "reuse ResultsExporter patterns"
      pattern: "csv.DictWriter|json.dump"
---

<objective>
Create the core infrastructure for optimization experiments: centralized configuration, deterministic IR metric computation, and result persistence with comparison support.

Purpose: Provide the foundational building blocks that chunking and scoring experiments will use to measure and compare retrieval quality.
Output: Four files (config, metric_computer, result_store, tests) with clean interfaces and deterministic behavior.
</objective>

<execution_context>
@/home/admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-optimization-experiments/25-RESEARCH.md

# Reference implementations for patterns
@kb_server/evaluation/exporter.py
@kb_server/evaluation/dataset.py
@kb_server/retrieval/hybrid_search.py
@kb_server/retrieval/reranker.py
@ingest/ingest.py
@qa/report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py and metric_computer.py</name>
  <files>
    kb_server/optimization/config.py
    kb_server/optimization/metric_computer.py
  </files>
  <read_first>
    kb_server/optimization/__init__.py
    kb_server/optimization/chunking_experiments.py
    kb_server/optimization/scoring_experiments.py
    kb_server/evaluation/dataset.py
    qa/report.py
    kb_server/retrieval/hybrid_search.py
    ingest/ingest.py
  </read_first>
  <action>
    Create `kb_server/optimization/config.py` with:
    - `ExperimentConfig` dataclass holding default parameters (chunk_size: 600, chunk_overlap: 80, top_k: 5, dense_weight: 0.7, sparse_weight: 0.3, rrf_k: 60, distance_metric: "COSINE", reranker_enabled: True)
    - `CHUNK_STRATEGIES` dict mapping strategy names to constructor callables: "fixed" (manual size/overlap), "recursive" (RecursiveCharacterTextSplitter), "semantic" (HybridChunker from docling)
    - `SCORING_VARIANTS` dict mapping variant names to parameter dicts: "dense_only", "hybrid_default", "hybrid_dense_heavy", "sparse_heavy"
    - Validation function `validate_experiment_params(params: dict) -> list[str]` that checks chunk_size > 0, overlap < chunk_size, dense_weight + sparse_weight <= 1.0
    - All defaults overridable via env vars with prefix `OPT_` (e.g., `OPT_CHUNK_SIZE`, `OPT_TOP_K`)
    - Module docstring with `PHASE 25: Optimization Experiments` tag
    - Logger named `log = logging.getLogger("kb-mcp.optimization")`
    - Type hints on all function signatures, `Optional[str]` for nullable params (pre-3.10 style)
    - Follow Black line-length 79, snake_case, PascalCase conventions from CONVENTIONS.md

    Create `kb_server/optimization/metric_computer.py` with:
    - `MetricComputer` class with methods:
      - `recall_at_k(retrieved_docs: list[str], expected_docs: list[str], k: int = 5) -> float` — fraction of expected docs in top-k
      - `mean_reciprocal_rank(retrieved_docs_per_query: list[list[str]], expected_docs_per_query: list[list[str]]) -> float` — standard MRR formula
      - `ndcg_at_k(relevance_scores: list[list[float]], retrieved_scores: list[list[float]], k: int = 5) -> float` — wraps `sklearn.metrics.ndcg_score`
      - `compute_all(query_results: list[dict], dataset: GoldenDataset, k: int = 5) -> dict[str, float]` — runs recall@k, MRR, NDCG for a full experiment run
    - Standalone module-level functions for each metric (same signatures) so tests can call them without instantiating the class
    - `compute_all` accepts a list of query result dicts, each with keys: `query`, `retrieved_docs` (list of source filenames), `expected_docs` (from dataset)
    - Import `ndcg_score` from `sklearn.metrics`; do NOT re-implement NDCG manually
    - Use binary relevance (1 if doc in expected_docs, 0 otherwise) for NDCG gains
    - Guard against empty inputs returning 0.0, not raising
    - Module docstring with `PHASE 25: Optimization Experiments` tag
    - Logger named `log = logging.getLogger("kb-mcp.optimization")`
    - Follow Black line-length 79, snake_case conventions
    - Do NOT add any LLM-as-judge logic here; IR metrics are deterministic
  </action>
  <verify>
    <automated>python -c "from kb_server.optimization.config import ExperimentConfig; from kb_server.optimization.metric_computer import MetricComputer; print('OK')"</automated>
  </verify>
  <done>
    - config.py contains `ExperimentConfig`, `CHUNK_STRATEGIES`, `SCORING_VARIANTS`, and validation function
    - metric_computer.py contains `MetricComputer` class with `recall_at_k`, `mean_reciprocal_rank`, `ndcg_at_k`, `compute_all`
    - All functions have type hints and module docstring with PHASE 25 tag
    - `python -c "from kb_server.optimization.config import ExperimentConfig; print(ExperimentConfig().chunk_size)"` outputs 600
    - `python -c "from kb_server.optimization.metric_computer import recall_at_k; print(recall_at_k(['a','b'], ['b','c'], k=2))"` outputs 0.5
  </done>
</task>

<task type="auto">
  <name>Task 2: Create result_store.py and test_metric_computer.py</name>
  <files>
    kb_server/optimization/result_store.py
    tests/test_metric_computer.py
  </files>
  <read_first>
    kb_server/optimization/config.py
    kb_server/optimization/metric_computer.py
    kb_server/evaluation/exporter.py
    kb_server/evaluation/dataset.py
  </read_first>
  <action>
    Create `kb_server/optimization/result_store.py` with:
    - `ExperimentResultStore` class:
      - `__init__(self, output_dir: Path = Path("data/experiments"))` — creates directory if missing
      - `save(run_id: str, params: dict, metrics: dict, strategy: str, variant: str) -> Path` — writes a JSON file named `{run_id}.json` with fields: `run_id`, `timestamp` (ISO8601), `strategy`, `variant`, `params`, `metrics`
      - `load(run_id: str) -> dict` — reads a saved run JSON
      - `list_runs() -> list[dict]` — returns all run metadata (no metrics) sorted by timestamp descending
      - `compare(run_ids: list[str]) -> dict` — loads specified runs, returns dict with `runs` (list of full records) and `deltas` (dict of metric_name -> list of (run_id, value) tuples)
      - `to_csv(output_path: Path) -> None` — exports all run summaries to CSV with columns: `run_id`, `timestamp`, `strategy`, `variant`, and one column per metric
      - `baseline()` -> Optional[dict] — returns the run marked as baseline (run_id == "baseline" or first run if none marked)
     - Use `csv.DictWriter` for CSV export (same pattern as `kb_server/evaluation/exporter.py`)
     - Use `json.dump` with indent=2 for JSON export
     - All file operations use `Path` objects and `encoding="utf-8"`
     - Import `Optional` from `typing` (pre-3.10 style per CONVENTIONS.md)
     - Module docstring with `PHASE 25: Optimization Experiments` tag
    - Logger named `log = logging.getLogger("kb-mcp.optimization")`
    - Follow Black line-length 79, snake_case conventions

    Create `tests/test_metric_computer.py` with:
    - `test_recall_at_k_perfect` — expected docs all in top-k → 1.0
    - `test_recall_at_k_zero` — no expected docs in top-k → 0.0
    - `test_recall_at_k_partial` — 2 expected, 1 found in top-3 → 0.5
    - `test_recall_at_k_empty_expected` — empty expected list → 0.0 (no division by zero)
    - `test_mrr_first_position` — relevant doc at rank 1 → 1.0
    - `test_mrr_second_position` — relevant doc at rank 2 → 0.5
    - `test_mrr_no_relevant` — no relevant docs → 0.0
    - `test_mrr_multiple_queries` — averages correctly across multiple queries
    - `test_ndcg_at_k_perfect` — relevance [1,1,0,0,0], scores [0.9,0.8,0.7,0.6,0.5] with k=5 → > 0.9
    - `test_ndcg_at_k_zero` — all zero relevance → 0.0
    - `test_metric_computer_compute_all` — mock query_results + expected_docs, verify all three metrics returned
    - `pytestmark = pytest.mark.PHASE25` at module level
    - Follow existing test conventions (snake_case test names, assert equality with exact floats)
    - All tests must pass without any external services (pure Python, no Qdrant/LLM needed)
  </action>
  <verify>
    <automated>python -m pytest tests/test_metric_computer.py -x -v</automated>
  </verify>
  <done>
    - result_store.py contains `ExperimentResultStore` with save, load, list_runs, compare, to_csv, baseline methods
    - test_metric_computer.py has 11+ tests, all passing
    - `python -c "from kb_server.optimization.result_store import ExperimentResultStore; s = ExperimentResultStore(); print(s.list_runs())"` outputs []
    - CSV export produces file with headers: run_id, timestamp, strategy, variant, and metric columns
    - `pytest tests/test_metric_computer.py` exits 0 with 11 passed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Experiment CLI → Qdrant | Experiment re-ingest modifies collection data; isolation required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | result_store CSV/JSON | accept | Offline analysis only; no integrity guarantees needed for experiment artifacts |
| T-25-02 | Denial of Service | experiment_runner re-ingest | mitigate | Plan 25-04 mandates temporary collection per run (kb_docs_experiment_{run_id}) with cleanup after |
| T-25-03 | Information Disclosure | result_store metrics | accept | No PII in IR metrics; experiment data is internal |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required (RESEARCH.md confirms all pre-installed) |
</threat_model>

<verification>
- `python -m pytest tests/test_metric_computer.py -x` passes
- `flake8 kb_server/optimization/config.py kb_server/optimization/metric_computer.py kb_server/optimization/result_store.py tests/test_metric_computer.py` reports zero errors
- `python -c "from kb_server.optimization import config, metric_computer, result_store"` succeeds (no import errors)
</verification>

<success_criteria>
- All three infrastructure files (config, metric_computer, result_store) exist and are importable
- test_metric_computer.py has 11+ passing tests covering recall@K, MRR, NDCG, and MetricComputer.compute_all
- Result store can save, load, list, and compare experiment runs without errors
- Configuration defaults are overridable via env vars and validated
</success_criteria>

<output>
Create `.planning/phases/25-optimization-experiments/25-01-SUMMARY.md` when done
</output>
