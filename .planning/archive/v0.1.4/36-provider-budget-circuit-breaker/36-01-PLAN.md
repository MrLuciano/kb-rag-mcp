---
phase: 36-provider-budget-circuit-breaker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb_server/circuit_breaker.py
  - kb_server/provider_budget.py
  - kb_server/embed_client.py
  - observability/metrics.py
  - tests/test_provider_budget.py
  - tests/test_circuit_breaker.py
  - tests/test_embed_client_unit.py
autonomous: true
requirements:
  - PROVBUD-01
  - PROVBUD-02
  - PROVBUD-03
must_haves:
  truths:
    - Provider calls enforce budget and breaker state per backend
    - Failing providers transition through closed/open/half-open states with cooldown and fallback
    - Existing embed client APIs remain intact while new telemetry is emitted
  artifacts:
    - path: "kb_server/circuit_breaker.py"
      provides: "Circuit breaker state machine"
      contains: "HALF_OPEN"
      min_lines: 30
    - path: "kb_server/provider_budget.py"
      provides: "Provider budget accounting"
      contains: "budget"
      min_lines: 25
---

<objective>
Add provider budget enforcement and circuit breaker resilience to embedding backends.

Purpose: Phase 36 prevents repeated provider failures and overuse from cascading through the embedding path. It should preserve the current embed client API while making provider behavior more observable and resilient.

Output: Separate budget and circuit-breaker modules, embed-client integration, metrics, and tests covering fallback and state transitions.
</objective>

<execution_context>
@/home/luck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-provider-budget-circuit-breaker/36-CONTEXT.md
@kb_server/embed_client.py
@observability/metrics.py
@tests/test_embed_client_unit.py
</context>

<tasks>

<task type="auto">
  <name>Create provider budget and circuit breaker primitives</name>
  <files>kb_server/provider_budget.py, kb_server/circuit_breaker.py, tests/test_provider_budget.py, tests/test_circuit_breaker.py</files>
  <read_first>
    kb_server/embed_client.py
    .planning/phases/36-provider-budget-circuit-breaker/36-CONTEXT.md
  </read_first>
  <action>
    Create separate modules for provider budget accounting and circuit breaker state transitions. Keep them framework-free and async-safe enough for concurrent embed client use. The breaker should support closed, open, and half-open behavior with cooldown, while the budget layer tracks per-provider usage within configured limits.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_provider_budget.py tests/test_circuit_breaker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Budget module tracks provider usage against configurable thresholds
    - Circuit breaker supports closed/open/half-open transitions with cooldown behavior
    - Tests cover core state transitions and budget exhaustion behavior
  </acceptance_criteria>
  <done>Reusable budget and breaker primitives exist with direct tests</done>
</task>

<task type="auto">
  <name>Integrate provider resilience into embed_client without breaking its API</name>
  <files>kb_server/embed_client.py, tests/test_embed_client_unit.py</files>
  <read_first>
    kb_server/embed_client.py
    kb_server/provider_budget.py
    kb_server/circuit_breaker.py
    tests/test_embed_client_unit.py
  </read_first>
  <action>
    Wrap backend dispatch in `embed_client.py` with provider budget and circuit-breaker decisions. Preserve the current public function signatures and behavior shape while adding fallback behavior and clear handling for breaker-open or budget-exhausted conditions.

    Keep health-check and normal embed paths compatible with current tests and avoid introducing provider-specific branching everywhere else in the codebase.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_embed_client_unit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing embed client public APIs remain unchanged
    - Provider calls respect budget and breaker state before dispatch
    - Fallback behavior or failure paths are explicit and covered by tests
  </acceptance_criteria>
  <done>Embed client uses provider budget and breaker primitives without API regressions</done>
</task>

<task type="auto">
  <name>Add resilience telemetry for provider behavior</name>
  <files>observability/metrics.py, kb_server/embed_client.py, tests/test_health_server.py</files>
  <read_first>
    observability/metrics.py
    kb_server/embed_client.py
    tests/test_health_server.py
  </read_first>
  <action>
    Extend metrics to capture provider budget usage, breaker opens, fallback events, and provider-side failures. Integrate metric emission into the embed client in a way that does not break existing metrics usage.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_embed_client_unit.py tests/test_health_server.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Metrics module exposes provider resilience telemetry
    - Embed client emits those metrics during budget, breaker, and fallback events
    - Tests prove metrics integration does not break health/metrics exposure flows
  </acceptance_criteria>
  <done>Provider resilience is observable through existing Prometheus integration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embed client → external provider | External provider availability and behavior are outside local control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | repeated provider failures | mitigate | Circuit breaker blocks repeated failing calls during cooldown |
| T-36-02 | Tampering | inconsistent provider budget state under concurrency | mitigate | Keep budget/breaker logic isolated and test state transitions directly |
| T-36-03 | Repudiation | hidden provider degradation | mitigate | Emit resilience telemetry for fallback and breaker events |

</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_provider_budget.py tests/test_circuit_breaker.py -q`
- `.venv/bin/python -m pytest tests/test_embed_client_unit.py -q`
- `.venv/bin/python -m pytest tests/test_embed_client_unit.py tests/test_health_server.py -q`
</verification>

<success_criteria>
- Provider budgets and breaker state exist per backend
- Embed client uses those controls without changing its public contract
- Provider degradation is visible through metrics
</success_criteria>

<output>
After completion, create `.planning/phases/36-provider-budget-circuit-breaker/36-01-SUMMARY.md`
</output>
