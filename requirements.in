# ── Core MCP ─────────────────────────────────────────────────────
mcp>=1.0.0

# ── Vector Store ────────────────────────────────────────────────
qdrant-client>=1.9.0
fastembed>=0.2.0          # FASE 12: BM25 sparse vectors for hybrid search

# ── HTTP client ─────────────────────────────────────────────────
httpx>=0.27.0

# ── Embedding backends ──────────────────────────────────────────
lmstudio>=1.0.0          # gaming machine — SDK nativo LM Studio
# ollama>=0.2.0          # proxmox — descomente se usar Ollama
# openai>=1.30.0         # alternativa OpenAI compat

# ── Extratores de documentos ────────────────────────────────────
python-docx>=1.1.0        # DOCX
openpyxl>=3.1.0           # XLSX
python-pptx>=0.6.23       # PPTX
pymupdf>=1.24.0           # PDF (fallback rápido)
# docling>=2.0.0          # PDF avançado — descomente para PDFs complexos

# ── Chunking ───────────────────────────────────────────────────
langchain-text-splitters>=0.2.0

# ── SSE transport & Health API ──────────────────────────────────
uvicorn>=0.30.0
starlette>=0.37.0
fastapi>=0.109.0          # Health check HTTP server

# ── Utils ──────────────────────────────────────────────────────
python-dotenv>=1.0.0
psutil>=5.9.0             # System monitoring for cache auto-tuning
sentence-transformers>=2.2.0  # FASE 12: Cross-encoder reranking
watchdog>=3.0.0           # FASE 13: File system monitoring

# ── Dev/Test Only ──────────────────────────────────────────────
pytest>=7.2.1
mypy>=1.6.0
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0
pytest-asyncio>=1.3.0
prometheus-client>=0.20.0
rich>=13.0.0
