# ── RAG Evaluation (optional, ~500 MB) ───────────────────────────────────────
# Install separately: pip install -r requirements-eval.txt
# Not needed for production server operation.
#
# Usage:
#   pip install -r requirements-eval.txt
#   python -m server.evaluation.ragas_pipeline

ragas>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
